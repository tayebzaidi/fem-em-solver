# Weekly progress against cost

Append-only. One `## YYYY-MM-DD` section per weekly planning review, written
by docs/automation/weekly-review.md step 2b: the mechanical counts from
`scripts/automation/weekly_counts.py` pasted verbatim, then the review's own
rows — newly supported workflows, uncertainties resolved (negative results
included), defects retired, repeated failure, lost slots, cost — ending in a
three-line **decision**: one activity to continue, one to change or stop, and
the evidence for each.

Rules of the file:

- **No single score.** Commit count, queue depth and machine time spent are
  context, never success. An XL / XXL window is reported by what it decided.
- **`unavailable` stays `unavailable`.** Token usage is not recorded (the
  launchers run `claude -p` in text mode); it is never estimated here.
- **Evidence or nothing.** Every row cites a chunk ID, a `log:line` or a
  commit. Ansys figures never appear — qualitative verdict only.
- The interval is "since the last weekly-review commit", measured, not seven
  days.

## 2026-09-19

### Mechanical counts — 2026-09-13 14:50Z → 2026-09-20 02:01Z (6.47 days; last weekly-review commit 63eb49c)

*Generated by `scripts/automation/weekly_counts.py`; counts only, no judgement.*

**Sessions and lost slots**

| Session kind | Count | Outcomes | Wall-minutes |
|---|---:|---|---:|
| daily-review | 6 | ok ×6 | 100 |
| implementer | 22 | ok ×22 | 544 |
| weekly-review | 1 | NO FOOTER (died or still running) ×1 | 0 |
| xl-run | 5 | ok ×3, no-window ×2 | 81 |
| xxl-run | 1 | ok ×1 | 12 |

- Sessions that did not end `ok` (lost or skipped slots): **1**. Before 2026-09-19 a failed launch wrote no footer at all, so older rows read `NO FOOTER`.

**Compute**

| Chunk family | Harness runs | Non-zero exits | Elapsed (min) |
|---|---:|---:|---:|
| ANS | 14 | 3 | 117.4 |
| EX | 172 | 41 | 79.7 |
| TH | 16 | 1 | 23.3 |
| PORT | 11 | 1 | 22.6 |
| WF | 6 | 0 | 22.0 |
| OPS | 47 | 10 | 12.0 |
| POST | 3 | 1 | 4.5 |
| WEEKLY | 5 | 0 | 0.0 |
| PLAN | 1 | 0 | 0.0 |
| **all** | 275 | 57 | 281.6 |

- Elapsed is harness wall-clock, not core-time: ranks are not in the index, so core-hours are **unavailable** without reading each log's command line. A non-zero exit is a run, not a verdict (negative controls and refused windows exit non-zero by design).
- `xl` windows (`xl-ledger.md`): **3** ran, 1.33 h of box time; 0 row(s) with no elapsed (never started, or killed). What each window *decided* is the review's to state — windows run is not progress.
- `xxl` windows (`xxl-ledger.md`): **1** ran, 0.19 h of box time; 0 row(s) with no elapsed (never started, or killed). What each window *decided* is the review's to state — windows run is not progress.

**Attempts journal**

- Entries by heading outcome: complete ×46, incomplete ×2, other ×1  *(heading keywords — a heuristic; read the entry before citing it)*
- Headings recording a flip to ✅: `EX-54`, `EX-55`, `EX-56`, `EX-58`, `EX-59`, `EX-60`, `OPS-47`, `OPS-51`, `OPS-53`, `PORT-14`, `PORT-15`, `POST-6`, `TH-14`, `WF-6`  *(the journal's claim; §4 compliance is the audit's, not this count's)*
- Chunks with ≥ 2 non-complete entries: none
- Chunks with ≥ 4 entries of any outcome (where the slots went): `EX-57` ×14

**Defects and parked work**

- known-issues headings added since the interval began: **4** opened, **0** retired (`git diff` on `known-issues.md`; a retitled heading counts as both)
- `attempt/*` and `recovered/*` branches present now: 4 (attempt/TH-15-step2proper-20260907T213739Z, attempt/WF-6-step4b-20260908T004458Z, attempt/WF-6-step4c-20260908T050135Z, attempt/WF-6-step4e-20260908T170345Z)
- Commits: 113 (docs 40, feat 23, test 16, ops 12, fix 9, chore 6, record 2, agents 2, meas 1, tool 1, probe 1) — context only, **not a progress measure**

**Tokens**

- unavailable — the launchers run `claude -p` in text mode, which records no usage. Session wall-minutes above are the only proxy, and a poor one. Recording real usage means `--output-format json` in the launchers: an operator decision, not made.


### The review's rows

*(The `weekly-review` row above reading `NO FOOTER ×1` is this session, still
running when the script ran — not a lost slot.)*

- **Newly supported workflows** — one, narrowly: *tune the F-small 4-leg
  birdcage's leg capacitors in the circuit layer and reproduce the tuned
  `S₁₁` in-model* (`PORT-14` ✅ `b82975a`, `PORT-15` ✅ `3e6519a`; runnable as
  `EX-58`). "Tuned" is a series resonance at one frequency on one fixture,
  not a match, not a mode claim (`TH-17` open). Nothing at human scale.
- **Uncertainties resolved** — (1) `ANS-2` adjudicated **AGREE** on rows 1–3
  (`f5759f5`): the first externally checked coil-driven SAR statement.
  (2) `ANS-3` **AGREE** on the mutual-coupling row, both AED orders; its S
  rows **DISAGREE** and the defect is ours — the current-drive route's S is
  not the 50 Ω S-matrix (known-issues 2026-09-19, `PORT-20`). (3) `ANS-4`
  order-matched rungs (`xl` 09-16/17/18): 128 MHz AGREE stands and the
  congruent cut is not a degree-2 systematic (3b); at 64 MHz the
  pre-registered private rule **fires on the self class** and at 10 MHz the
  degree-1 AGREE is re-read as partly coincidental (3c's public rule) —
  negative, banked, `PORT-21`. (4) `WF-7` 0b (`xxl` 09-19): human-scale order
  sensitivity 5.50 % — same class as F-small ⇒ branch (a), degree 2 is the
  human-scale order, priced at 106.1 GiB / 348 s per single drive.
  (5) `TH-14`: copper via Leontovich is gated three ways (`68ff988`).
  (6) `TH-17` step 1(i): negative — the eigen-pencil is cheap (47.8 s) and
  finds no mode at 64 MHz (`f085e51`). (7) `ANS-2` step 4: cost probe fired
  its STOP rule, re-priced to `xl` (`32f4eae`).
- **Defects retired** — by this interval's slots: the `TH-15` terminal-power
  excess (`9f93db6`), the clipped-legend entry (`f96502f`), the EX-14/EX-17
  VTX read-back regression (`OPS-48`). The 09-19 rotation moved 60-odd
  already-retired headings to the archive; that is housekeeping, not
  retirement. Three opened (`post4_step5_probe` pins, `B_DG1` 4e-6
  irreproducibility, a red `OPS-44` pin test on `main`), two more by this
  review.
- **Repeated failure** — none by the script's rule. `EX-57` ×14 is not
  failure, it is **where drained slots went**: from 09-14 07:30 to 09-18
  the §9 queue ran dry of physics because every open anchor was
  weekly-owned and the weekly now runs once in 6.5 days; ≈ 8 of 22
  implementer sessions were wholly or mostly setup figures.
- **Lost slots** — none died (22/22 implementer, 6/6 daily `ok`). Two `xl`
  nights had no window queued (before the 09-15 FIFO). The real loss is the
  row above: fired slots with no physics to take.
- **Cost** — as printed: 281.6 harness-minutes, 1.33 h `xl` + 0.19 h `xxl`
  box time, 544 implementer wall-minutes; tokens and core-hours
  **unavailable**.
- **Decision** —
  **Continue:** the nightly `xl` FIFO on the `ANS-4` order-matched family —
  three windows, three rulings (one stands, two fire their rules), zero
  tokens; evidence: row (3).
  **Change:** stop letting weekly-owned anchors starve §9 — this review
  enumerates every live chain ≥ 3 open steps ahead at execution-ready grain
  (§10), and `EX-57` stops being the default sink: if ≥ 4 implementer
  sessions next interval are again mostly fallback figures, the next weekly
  cuts a slot from the crontab rather than feed it; evidence: ≈ 8 of 22
  sessions, row "Repeated failure".
  **Evidence that would change "continue":** an `xl` window whose ruling
  no chunk or verdict consumes within one weekly.
