# XL-tier ledger — one run per week, 512 GiB / 16 ranks / 2 h

Operator directive 2026-09-05 (interactive session; PROJECT_PLAN §5.1). The
`fem-em-solver-xl` compose service (profile `xl`, 512 G limit) exists for
**one** run per 7 days, commissioned **only by the weekly planning review**,
which names the chunk and its pre-registered readout. The bash guard
(`scripts/automation/hooks/bash_guard.py`) reads the most recent date in this
table and denies any XL command inside 7 days of it; `run_and_log.sh` appends
the row automatically when a harness command targets the XL service. Fill in
the last four columns by hand from the log after the run, and commit the row
with the log.

| Date (UTC) | Chunk | Log | Ranks | Cells | Peak memory (GiB) | Elapsed (s) | Readout |
|---|---|---|---:|---:|---:|---:|---|
| 2026-09-09 | ANS-4-step2b | `20260909T143756Z_ANS-4-step2b.log` | 16 | 116 085 | ~16.6 observed (see note) | 249 | degree 2 moves the three C4 classes of S at 128 MHz by **6.0900 / 5.3841 / 6.6959 %** off the degree-1 record; all imported gates green on both rungs, degree-2 class spreads *tighter* (0.0191–0.0334 % vs 0.0654–0.1012 %). **The slot was not needed:** 249 s against the predicted 1 100–1 300 s and ~16.6 GiB against ≳ 49 — this case fits the heavy tier and the ordinary 128 G service. |

**Note on the memory column (2026-09-09).** The step-2b module carries no
in-run `ru_maxrss` print, so 16.6 GiB is a `docker stats` reading taken by the
operator's interactive session while the degree-2 solve was running, **not a
measured peak** — the true peak is at or above it and was not instrumented. Any
future XL commissioning that leans on this figure should print `ru_maxrss`
instead. It is nonetheless a decade under the ordinary service's own 128 G
limit, which is the load-bearing part: this run did not need the tier.
| 2026-09-09 | TH-11-step5d | `20260909T145530Z_TH-11-step5d.log` | 8 | 2 808 204 | 260.9 (spot) | — | **KILLED, no footer — uncountable.** Wrapper killed at ~40 min; the container-side `timeout`/`mpiexec`/8 ranks survived it with nothing consuming their output, so the run could produce no evidence and was stopped. Only durable reading: 260.9 GiB from the cgroup. Cause and the three fixes: `OPS-43`. |
| 2026-09-09 | TH-11-step5d | `20260909T153910Z_TH-11-step5d.log` | 8 | 2 808 204 | 263.4 (container lifetime — spans the killed attempt, see note) | 4838 | **17 passed / 1 skipped, Status 0 — the rung solved for the first time.** Loaded 2331.7 s + free 2257.6 s. 64 MHz ladder h 0.005 → 0.0025 → 0.00125: **+10.2698 % → +2.8063 % → +0.3824 %**; three-rung fit **p = 1.623, d₀ = −0.7834 %**; two-rung bracket **[−2.0415 %, −0.4256 %]**, overlapping step 4's 10 MHz [−2.1492, −0.9050] and 30 MHz [−3.3675, −0.3812]. |

**Note on the 2026-09-09 peaks.** Both rows share one container lifetime
(started 14:55:23Z, before the first attempt), and `memory.peak` cannot be reset
on this kernel, so **263.4 GiB is the maximum over both runs**, not this run's
own peak. Same workload at the same rank count, so the figure is representative
— but it is not attributable, and §5.1 now requires restarting the service
before an `xl` window so the next one is.
| 2026-09-10 | ANS-4-step2d | `20260910T151845Z_ANS-4-step2d.log` | — | — | — | 0 | **DID NOT START — no compute, no slot spent in substance.** `docker compose` was permission-denied from inside `xl-run.sh` (the socket is reachable from a direct agent command but not from a script in the sandbox); the window died in 0 s *after* the harness had already appended this row. Row kept because the ledger records attempts, not successes. `xl-run.sh` now preflights docker before anything consumes the slot. |
| 2026-09-10 | ANS-4-step2d | `20260910T152049Z_ANS-4-step2d.log` (footerless; full output recovered to `20260910T180911Z_ANS-4-step2d-capture.log`) | 16 | 592 744 (finest rung) | **290.2** (clean — service restarted immediately before the window) | 7225 | **14 passed / 1 failed in 7225.01 s (2:00:25).** Ladder h=0.015 deg 1 → h=0.015/0.0075/0.005 deg 2 at 116 085 / 116 085 / 281 728 / 592 744 cells and 0.14 / 0.74 / 1.80 / **3.79 M unknowns**; four drives 12.0 / 169.0 / 1119.2 / **5653.5 s**. The three C4 classes of `S` move monotonically with both order and `h` and the degree-2 sequence converges (successive change 1.19 % → 0.62 %, ratio 1.90). The single failure is a defect in the ladder module's refinement control, not physics — it compared two rungs sharing one mesh (`116085 > 116085`); fixed the same day, every physics gate passed. Ran 25 s past the 2 h ceiling and completed only because the deadline was lifted by hand. |
| 2026-09-16 | ANS-4-step3 | `20260916T070008Z_ANS-4-step3.log` | 16 | 592 744 (degree-2 rung; ×1 rung 116 085) | **280.8** (`memory.peak` 301 547 610 112 B; service recreated before the window, so attributable; summed `ru_maxrss` 282.3) | 1658 | **16 passed, Status 0 — the first FIFO-queued window, run by cron with no session.** 64 MHz, `RUNGSPEC="0.015:1 0.005:2"`: degree-2 rung 3.79 M unknowns, mesh 105.7 s, four drives 1476.9 s (the 2000–3000 s prediction beaten — `PORT-19` reuse); imported `PORT-11` gates green (reciprocity 1.6e-14, σ_max 0.999758, spreads 0.1985 / 0.1715 / 0.1946 %). Degree 1 → 2 move on the driven column **6.51 / 2.60 / 4.11 %** (self / adjacent / opposite; 128 MHz step 2d: 6.09 / 5.38 / 6.70 %). Private AED miss and decision rule: the 09-19 weekly's (`xl-pending.md` entry 2). Columns filled by the 2026-09-16 03:00 review. |
| 2026-09-17 | ANS-4-step3b | `20260917T070008Z_ANS-4-step3b.log` | 16 | 592 550 (degree-2 rung, congruent cut on; ×1 rung 116 118) | **273.1** (`memory.peak` 293 192 646 656 B, `:5953–5954`; service recreated before the window; summed `ru_maxrss` 274.6, `:4824`) | 1539 | **15 passed / 1 skipped, `[capture] rc=0`, Status 0** (`:4977`, `:5955–5959`). 128 MHz, `RUNGSPEC="0.015:1 0.005:2"`, `c4_congruent_sheets=on`: degree-2 rung **3 827 268 dofs** (`:4485`; the readout's `~3,792,320` is a 6.4 × cells estimate, never the problem size), mesh 103.4 s, four drives 1361.4 s (`:4825`). Imported `PORT-11` gates green on the rung (`:4849`): reciprocity 6.3e-15, σ_max 0.999036, class spreads **0.0056 / 0.0088 / 0.0070 %** (band 0.5 %) — an order tighter than the ×1 rung's 0.0739 / 0.0822 / 0.0506 % and than the cut-off degree-2 rungs. Two-rung move on the driven column (h 0.015 → 0.005 **and** degree 1 → 2 together) **5.71 / 4.52 / 6.73 %** (`:4874–4877`; for scale, step 2b's degree-only move on one cut-off mesh was 6.09 / 5.38 / 6.70 % — not the same comparison). The ×1 rung reproduces the 2a″ record to rel ≤ 8.2e-11 (`:4896–4899`) — **printed, not asserted**: the one skip is `test_the_frequency_knob_reaches_the_solve`, by design at `-n 16` (`OPS-41`, record width `-n 8`). Warm FFCx cache (named volume survives the recreate) — not a cold price. Private AED reading and decision rule: the 09-19 weekly's (`xl-pending.md` entry 3). Columns filled by the 2026-09-18 03:00 review from a `log-pathologist` reading. |
| 2026-09-18 | ANS-4-step3c | `20260918T070008Z_ANS-4-step3c.log` | 16 | 592 744 (degree-2 rung, cut off; ×1 rung 116 085) | **284.8** (`memory.peak` 305 814 069 248 B, `:5733–5734`; summed `ru_maxrss` 286.3, `:4604`) | 1603 | **16 passed, 0 skipped, `[capture] rc=0`, Status 0** (`:4757`, `:5735–5739`). 10 MHz, same `RUNGSPEC`, cut off: degree-2 rung **3 828 494 dofs** (`:4265`), mesh 107.0 s, four drives 1419.4 s (`:4605`; factorisation 1263 s, held solves 5.4–7.8 s). Imported gates green (`:4624`): reciprocity 8.1e-14, σ_max 0.999993961, class spreads **0.4451 / 0.2199 / 0.2186 %** — **the self class sits at 89 % of the 0.5 % band** (same mesh at 64 MHz: 0.1985 %; congruent cut at 128 MHz: 0.0056 %). Two-rung move on the driven column **4.41 / 1.35 / 1.08 %** against the window's own ×1 degree-1 rung (`:4652–4654`; the same two-rung move at 64 MHz, step 3: 6.51 / 2.60 / 4.11 %; at 128 MHz on the cut, step 3b: 5.71 / 4.52 / 6.73 %). **Label traps in the log:** the readout heading says "at 128 MHz" (`:4638`, a literal — the data are 10 MHz) and the rung headers say "at -n 2" (`:2416`, `:4585`, a literal — the run is `-n 16`); known-issues 2026-09-18, `OPS-51`. Private AED reading and decision rule: the weekly's (entry 4). Follow-up queued: entry 8 (the same rung on the congruent cut). Columns filled by the 2026-09-18 03:00 review from a `log-pathologist` reading. |
| 2026-09-20 | ANS-4-step3d | `20260920T070008Z_ANS-4-step3d.log` | 16 | 592 550 (degree-2 rung, congruent cut on; ×1 rung 116 118) | **281.1** (`memory.peak` 301 812 256 768 B, `:6034–6035`; service recreated before the window by `xl-run.sh`; summed `ru_maxrss` 282.6, `:4824`) | 1619 | **16 passed, 0 failed, 0 skipped, `[capture] rc=0` last, Status 0** (`:6033`, `:6036`, `:6039–6040`); the summary is rank-identical on all 16 ranks; command byte-identical to `xl-pending.md` entry 5. 64 MHz, `RUNGSPEC="0.015:1 0.005:2"`, `c4_congruent_sheets=on`: degree-2 rung **3 827 268 dofs** (`:4485`, ZMUMPS `N` `:4487`; the readout's `~3,792,320` at `:4825` is the 6.4 × cells estimate, never the problem size), mesh 111.6 s, four drives 1425.5 s (factorisation 1384.3 s; held solves 5.3–5.5 s). Imported `PORT-11` gates green and **asserted** on the rung (`:4858`): reciprocity 1.7e-14, σ_max 0.999756214, class spreads **0.0132 / 0.0126 / 0.0081 %** (band 0.5 %) — ≈ 15× tighter than the same rung with the cut off (step 3, 09-16: 0.1985 / 0.1715 / 0.1946 %), the tightening the cut gave at 128 MHz (3b). Two-rung move on the driven column (h **and** degree together, against the window's own ×1 rung) **6.50 / 2.61 / 4.12 %** (`:4891–4893`; step 3, cut off: 6.51 / 2.60 / 4.11 % — differences ≤ 0.012 percentage points, inside the rungs' own class spreads; a cross-window reading on two meshes, printed both sides, not a gate). **By entry 5's public decision rule: the cut is not a degree-2 systematic at 64 MHz either** — reading offered to the 09-26 weekly, which rules and reads the private column. No skip this time: at 64 MHz `test_the_frequency_knob_reaches_the_solve` takes its knob branch and asserts (every class moves > 1e-2 from the 128 MHz record — the `:4907–4909` "rel 5.7e-01 …" lines against the 2a″ record are that assert passing, not a red). Printed-only control: per-class max \|dS\| against `PORT-11` step 2's 64 MHz 4×4 1.1e-04 / 3.9e-05 / 2.4e-05. No label trap of the 3c kind. Warm FFCx cache — not a cold price; inside the inherited prediction (1 500–2 500 s, 270–300 GiB). **Orphans: not established by the log** — the harness check is silent when clean and nothing checks after the window; what the log shows is a normal `mpiexec` exit. The "orphans 0 / 0" wording in earlier rows rests on the same silence. Columns filled by the 2026-09-20 03:00 review from a `log-pathologist` reading (CONFIRMED countable; command CONFIRMED identical), lines re-read by the review. |
| 2026-09-21 | WF-7-step0c | `20260921T070008Z_WF-7-step0c.log` | 16 | 507 266 (vs the 504 642 record, rel 5.200e-03, INSIDE the imported 1 % band — asserted, `:10420`, `:11908`) | **11.96** (`memory.peak` 12 846 546 944 B, `:11916`; *below* summed `ru_maxrss` 13.842 GiB, `:11903` — shared pages are charged per rank in RSS and once in the cgroup; attributable on `xl-run.sh`'s pre-window recreate, which this log does not itself record) | 228 | **`[capture] rc=0` last, Status 0** (`:11917`, `:11920–11921`); **orphans 0 before / 0 after, measured** (`pgrep -c python3` on both sides, `:35–36`, `:11913–11914` — a positive reading, unlike the 09-20 row); command byte-identical to `xl-pending.md` entry 6. **The price (chain H1): the F-human degree-1 32-drive set is one factorisation plus 31 back-substitutions — 607 039 unknowns, mesh build 140.55 s (cold gmsh, 63 % of the window), drive 1 24.73 s (`:10598`), held drives 0.40–0.57 s each, cumulative 224.19 s = 3.74 min (`:11902`), summed `ru_maxrss` 13.529 → 13.842 GiB.** Against the pre-registered brackets, by the review's arithmetic (the probe prints no cumulative bracket verdict): time **OUTSIDE, below** 5–45 min; memory INSIDE 10–40 GiB. ⇒ by entry 6's decision rule the F-human degree-1 32×32 is not merely an ordinary `xl` job — it fits the **heavy** tier (≤ 12 ranks, 128 G service), which is what chain step H3 is now written against. **Asserted in-window** (probe `:394–428`; entry 6's "nothing asserted beyond the cell band" was wrong and is corrected there): the cell band, **full 32×32 reciprocity 1.084e-14 ≤ the imported 1e-3** (`:11905`, `:11909`), drive 2 ≠ drive 1 by 1.407e-03 > 1e-6. **Printed only:** σ_max 0.999915; `|S_jj|` spread 1.7891e-03 (printed beside `OPPOSITE_SPREAD_BAND` — a self-class spread beside the opposite-class constant, gates nothing); full-column Σ\|S_ij\|² 0.895287–0.896108 over the 32 ports; per-drive terminal accounting residual 4.061e-03–4.624e-03 (stable across drives, unattributed — H3 asserts the exact identity instead); `S_driven(P17)` `0.407423+0.344417j` equals the `-n 8` single-drive step-0 record to six decimals across the width and drive-count change (`:11904`). **Label traps:** 31 × "OUTSIDE measured single-drive price" on held drives compare a back-substitution to a factorising `-n 8` band — an underrun, not an overrun; the "flag-off control" heading is a fixed literal in a run with `FEM_EM_WF7_PORTS=all`. Warm FFCx cache (inferred: ≈ 4.7 s between phase 2 and drive 1) — not a cold-JIT price. Self-consistency on one fixture at degree 1; carries the 5.50 % order caveat (weekly 09-19). Columns filled by the 2026-09-21 03:00 review from a `log-pathologist` reading (CONFIRMED countable; command CONFIRMED identical), lines re-read by the review. |
| 2026-09-22 | ANS-4-step3e | `20260922T070007Z_ANS-4-step3e.log` | 16 | 116 085 / 116 085 / 281 728 / 592 744 (rungs `0.015:1 0.015:2 0.0075:2 0.005:2`, cut off) | **277.44** (`memory.peak` 297 896 943 616 B, `:10233–10234`; summed `ru_maxrss` 278.93, `:8787`) | 2206 | **15 passed / 2 failed on all 16 ranks, `[capture] rc=1`, Status 1** (`:8969–8970`, `:10235–10238`) — **both reds non-physics**: the `OPS-62` refinement guard (`assert 116085 > 116085`, `:8896–8899`; fixed 2026-09-22) and the deliberate `OPS-60` `skrf` red (`:8892–8895`; the image was rebuilt after this window). Command identical to `xl-pending.md` entry 7. 64 MHz degree-2 *h*-ladder, real solves: **139 140 / 745 938 / 1 819 094 / 3 828 494 unknowns** (ZMUMPS `N`, `:2114`, `:4204`, `:6281`, `:8450`; the `~unknowns` prints are 1.2×/6.4× cell estimates), mesh 29.5 / 29.3 / 63.6 / 142.4 s, four drives 4.3 / 52.0 / 309.6 / **1502.0 s** (factorisations 0.85 / 26.2 / 231.0 / 1332.3 s; held 0.1–5.4 s). Imported `PORT-11` gates green and asserted on every rung (`:8812–8815`): class spreads 0.0573 / 0.0599 / 0.0370 %, 0.0229 / 0.0282 / 0.0219 %, 0.0141 / 0.0124 / 0.0176 %, **0.1985 / 0.1715 / 0.1946 %** (the finest cut-off rung reproduces step 3's spreads and two-rung moves 6.5109 / 2.6042 / 4.1072 %, `:8832–8834`). **Successive degree-2 changes are not printed** — the review's hand arithmetic on the printed driven-column entries (`:8823–8834`, module's move convention, coarser rung as reference): S₁₁ 2.815 → 1.497 % (ratio 1.88), S₂₁ 1.570 → 0.816 % (1.92), S₃₁ 1.362 → 0.831 % (1.64). Caveat for the weekly: the finest rung's C4 spread is ≈ 10× the middle rungs', so a ≈ 0.8 % last change read on one C4 member carries ≈ ±0.2 pp of member choice — S₃₁'s ratio could lie anywhere in ≈ 1.3–2.2. Inside entry 7's price (2 000–3 500 s, 270–295 GiB). Label traps: `step2d` / `step3a` tags on a 64 MHz run (`OPS-51` class), an empty Richardson heading (`:8836–8837`), the `rel = 5.7e-01 …` lines are a passing knob assert vs the 128 MHz record (`:8840–8842`). Orphans: not established (no check in the log). Columns filled by the 2026-09-23 03:00 review from a `log-pathologist` reading (CONFIRMED countable; command CONFIRMED identical; its "one failure" claim OVERRULED to two). |
| 2026-09-23 | ANS-4-step3f | `20260923T070007Z_ANS-4-step3f.log` | 16 | 116 118 (×1, cut on; vs 116 085 record, ratio 1.000284) / 592 550 (degree-2 rung, cut on) | **280.28** (`memory.peak` 300 951 412 736 B, `:6098–6099`; summed `ru_maxrss` 281.80, `:4872`) | 1702 | **18 passed, 0 failed, 0 skipped on all 16 ranks, `[capture] rc=0`, Status 0** (`:5046`, `:6100–6103`) — the first `ANS-4` window after `OPS-62` (guard green, `:4923–4924`) and the first on the **rebuilt image** (`[OPS-60] skrf=2.1.0`, `:271–283`; dolfinx 0.11.0.post0, `:287`). Command identical to `xl-pending.md` entry 8. 10 MHz, `RUNGSPEC="0.015:1 0.005:2"`, `c4_congruent_sheets=on`: degree-2 rung **3 827 268 dofs** (`:4535`), mesh 131.3 s, four drives 1476.9 s (factorisation 1305.0 s, held 5.9–6.1 s). Imported `PORT-11` gates green and asserted (`:4951`): reciprocity 1.365e-13, σ_max 0.999993920, **class spreads 0.0303 / 0.0158 / 0.0100 %** (band 0.5 %) against step 3c's cut-off **0.4451 / 0.2199 / 0.2186 %** — ≈ 15× tighter on the self class. ×1 rung 0.0316 / 0.0244 / 0.0115 % (reproduces `PORT-21` step 1b's control′). Two-rung move on the driven column **4.4499 / 1.2966 / 1.2187 %** (`:4963–4965`; 3c, cut off, 4.41 / 1.35 / 1.08 % — each against its own ×1 control, not the same mesh). **By entry 8's public decision rule: the collapse branch** — 3c's 0.4451 % was the cut-off port-sheet asymmetry, not a low-frequency property of the degree-2 solve; the congruent cut is the rung to quote at 10 MHz. Not excluded: a weak residual trend (cut on, degree 2: 0.0056 % at 128 MHz, 0.0303 % at 10 MHz; nothing below 10 MHz measured). Inside entry 8's price (1 500–1 800 s, 270–290 GiB). Label traps: the `step3a` block's "(record width -n 8)" and the 128 MHz "2a″ record" rel lines are a passing knob assert (`:4970–4973`). Orphans: not established. Columns filled by the 2026-09-23 03:00 review from a `log-pathologist` reading (all six claims CONFIRMED). |
| 2026-09-24 | ANS-4-step3g | `20260924T070008Z_ANS-4-step3g.log` | 16 | 116 085 / 116 085 / 281 728 / 592 744 (rungs `0.015:1 0.015:2 0.0075:2 0.005:2`, 10 MHz, cut off; `OPS-62` refinement guard green, `:8828–8831`) | **281.95** (`memory.peak` 302 744 965 120 B, `:10056–10057`; summed `ru_maxrss` 283.45, `:8819`) | 2084 | **18 passed, 0 failed, 0 skipped on all 16 ranks, `[capture] rc=0` last, Status 0** (`:9005`…`:10055`, `:10058–10062`); command CONFIRMED identical to `xl-pending.md` entry 11 (queue file vs entry `cmp`-identical). Rebuilt image (`skrf=2.1.0`, `:286`; dolfinx 0.11.0.post0, `:268`). **139 140 / 745 938 / 1 819 094 / 3 828 494 unknowns** (ZMUMPS `N`, `:2146`, `:4236`, `:6313`, `:8482`), mesh 23.7 / 24.0 / 53.5 / 117.4 s, four drives 3.7 / 45.9 / 299.4 / **1454.7 s** (numeric factorisation 0.75 / 23.0 / 235.6 / 1301.7 s; held 0.0–5.8 s). Imported `PORT-11` gates green and asserted on every rung (`:8860–8863`): Z-class spreads 0.0485 / 0.0326 / 0.0229 %, 0.0351 / 0.0165 / 0.0195 %, **0.4451 / 0.2199 / 0.2186 %** (the finest cut-off rung reproduces 3c's spreads and driven column to all printed digits — a fresh solve, reciprocity 7.559e-14 vs 3c's 8.056e-14). Driven column, `move from control` vs the ×1 degree-1 rung (`:8885–8904`): S₁₁ 13.5702 → 7.6032 → 4.4107 %, S₂₁ 3.9731 → 2.2450 → 1.3475 %, S₃₁ 3.7875 → 2.0614 → 1.0836 %. **Successive degree-2 changes (not printed — the pathologist's arithmetic, S₁₁ re-checked by hand by the 2026-09-25 review; module's convention, coarser rung as reference): S₁₁ 6.969 → 3.606 % (ratio 1.93), S₂₁ 1.813 → 0.934 % (1.94), S₃₁ 1.843 → 1.100 % (1.67).** Against entry 11's rule: ratio ≳ 1.5 on all three ✔; last change below 1.08 % only on S₂₁ — **S₁₁ 3.61 % and S₃₁ 1.10 % are above it ⇒ the "h-converged" branch is not met**; the sequence is monotone and falling, so the "non-monotone" branch is not met either — the weekly rules; 3h (the same ladder on the congruent cut, 09-27) is its control, and the cut cannot explain S₁₁'s 3.6 % (finest-rung S-class spread 0.52 %). Inside entry 11's price (2 000–3 500 s, 270–295 GiB). **Label traps:** `[PORT-9 step3d1]` / `[ANS-4 step2d]` tags on a 10 MHz run (`OPS-51` class); `S-class spread 0.5237 %` (`:8902`) is an unasserted S spread, the gate is the Z spread 0.4451 %; `‖Z − Zᵀ‖/‖Z‖ = 7.742e-04` (`:8815`) is not the reciprocity gate (S: 7.56e-14); `sigma_max=1.000e+00` is 4-digit rounding; empty Richardson heading (`:8906–8907`); the `step3a … 2a″ record … rel = 1.271e+00` lines are the passing frequency-knob assert (`:8919–8922`); `~unknowns` are cell estimates. Orphans: not established (no check in the command). Columns filled by the 2026-09-25 03:00 review from a `log-pathologist` reading (all seven claims CONFIRMED). |
| 2026-09-25 | ANS-2-step4 | `20260925T070008Z_ANS-2-step4.log` | 8 | 719 769 (452 228 phantom) at `phantom_resolution` 0.00125 m — the 09-19 probe's counts reproduced exactly (`:2038`, `:2059`, `:2098`); a candidate record, not asserted (the skip line, `:2105`) | **39.91** (`memory.peak` 42 850 295 808 B, `:2125–2126`; attributable — below the 09-24 window's peak, so the pre-window recreate reset the cgroup; summed `ru_maxrss` not printed) | 1315 | **`[capture] rc=0` last, Status 0** (`:2127–2131`); **orphans 0 before / 0 after, measured** (`:35–36`, `:2123–2124`); command CONFIRMED byte-identical to `xl-pending.md` entry 10. `[ANS-2 step 4] MESH-RECORD ASSERTIONS SKIPPED` printed (`:2105`); the example's 14 asserts green — 10 g C4 pairs 0.0861 / 0.0868 / 0.1751 / 0.0019 % and 1 g pairs ≤ 0.1474 % against the imported 5 % band, the 1 g mis-paired control ≈ 86.9 % (≥ 50 %), coverage identity power 6.82e-14 / mass 3.82e-14 ≤ 1e-10 (`:2100–2104`). **Readout (`:2109–2118`, every figure re-derived from `metrics_h0.00125.json` / `metrics.json` by the pathologist):** driven-point SAR 5.555 / 5.581 / 5.339 / 5.378e-07 against the 0.0025 rung's 5.878 / 5.079 / 5.287 / 5.763e-07 (per-drive −5.49 / +9.87 / +0.98 / −6.69 %); **C4 driven-point spread 15.730 % → 4.528 %**; phantom power moves −0.2748 / −0.1399 / −0.2993 / −0.0636 %. **Public decision-rule arithmetic: (a) holds** (spread < 5 % by 0.47 pp, every power move < 2 %); (c) does not fire; (b) fails on size (< 2 %) whatever the direction — no private reading needed. Ruled (a) by the 2026-09-25 review (§7 `ANS-2`). Price: ≈ 18 % over entry 10's 1 115 s; the probe's "mesh 359.8 s" was gmsh 130.7 s + the `PORT-9` build and sweep, not a mesh time (here gmsh 147.6 s, `:2007`); per-drive MAT-4 solves 202.8 / 216.3 / 208.6 / 182.4 s (`:2062`) — the four drives are solved twice (the `PORT-9` sweep, then MAT-4). **Label traps:** `:2080` "PRE-REGISTERED 1 g VERDICT: (a)" is the MAT-4 module's own label, not entry 10's branch; "ASSERTED" on ball containment (`:2061`), the 10 g control sign (`:2073–2076`) and the `PORT-9` bands (`:2057`) are builder prints — those asserts live in pytest functions the example does not call (the arithmetic holds in each); `:2066` `1.771131e-02` is against step 3f's 0.0075 m record; `:2038` `ratio 6.200362` is a different fixture's record; "step 1 … closes the RUNNABLE HALF" (`:54`, `:2122`) is fixed text; `SyntaxWarning` lines (`:37–52`, script `:572`) harmless. `:2113` carries an AED-derived figure from tracked script text — flagged to the operator, not quoted here. Columns filled by the 2026-09-25 03:00 review from a `log-pathologist` reading (claims 1, 2, 5, 7 CONFIRMED; 3 CONFIRMED with price corrections; 4 OVERRULED in part — the builder labels above). |
