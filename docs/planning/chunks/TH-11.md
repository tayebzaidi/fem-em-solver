# TH-11 — Coil-loading trend across the eddy→displacement transition (`MAT-6`'s ΔR machinery at rising f)

 **Coil-loading trend across the eddy→displacement transition (`MAT-6`'s ΔR machinery at rising f)** | ✅ *(closed 2026-08-18 on step 4's answer + step 5's measured negative — the `GEO-14` precedent; step 1 ✅ 2026-08-13 — 64 MHz feasible at the 10 MHz price, identities to 1e-14, quasi-static ΔR deviation 1.5834% → **10.2698%**, unattributed between physics and 1.26 cells/δ; step 2 ✅ 2026-08-15 — the resolution rung attributes most of it to mesh: **+2.8063%** at 2.52 cells/δ, a −7.4635 pp move, the pre-registered RESOLUTION-DOMINATED band, so no gated trend claim is scopeable yet; step 3 ✅ 2026-08-16 — the 30 MHz mid-point reads **+5.5912%**, giving 1.5834 / 5.5912 / 10.2698% across 10 / 30 / 64 MHz, but cells/δ falls 3.18 / 1.84 / 1.26 in lockstep, so the confound is monotone too and the trend stays a set of points; step 4 ✅ 2026-08-17 — the fixed-f h-ladder reads **flat in f**: refinement moves the deviation −1.87 pp at 10 MHz and −4.48 pp at 30 MHz (−7.46 pp at 64 MHz on record) and the h → 0 brackets overlap at ~−1%, so the "trend" was the resolution term; no gated trend claim is scopeable and §2 stands; step 5 scoped 2026-08-17 review, attempt 1 🚫 2026-08-17 — the third rung is **priced and does not fit a scheduled slot**: 2 807 309 cells (inside the 3.4 M ceiling) but 288.2 s of mesh plus a loaded solve still assembling at the 570 s kill, so §7's probe stop condition fired; module parked on `attempt/TH-11-step5-20260817T123353Z`; **rescoped by the 10:30 review as 5a/5b** — 5a caches the mesh to XDMF and buys the `-n 8` rank change with a measured control (fine-rung +2.8063% reproduced within 0.1 pp), 5b runs the pair off the cache; step 5a ✅ 2026-08-17 — the cache round-trips the 2 807 309-cell rung **exactly** (per-tag owned counts and tag names preserved, mesh 126.4 s replaced by a 14.8 s read) and the `-n 8` fine-rung pair reproduces the `-n 2` record to **+0.00002 pp**, so the rank change is bought; 5b is unblocked and needs one solve per command at ~480 s; step 5b attempt 1 🟡 2026-08-18 — the loaded/free split is **exact** (fine rung reproduced to the last digit, drive surrogate 0.000e+00) and the cache reads back at `-n 12`, but the third-rung solve was **OOM-killed with the container** at 518 s, so the rung is memory-bound at 64 GiB, not time-bound: the review's lever (b) more ranks is the wrong one and (c) shrinking the rung is now live; module parked on `attempt/TH-11-step5b-20260818T004000Z`; step 5b attempt 2 🟡 2026-08-18 — the peak is now **measured**: at `-n 8` the same solve drove `memory.peak` to **64.00 GiB, exactly `memory.max`**, and ran past `timeout -k 30 560` without returning, so `-n 12`'s OOM and `-n 8`'s overrun are one wall with two failure modes and **no rank count affords 2 807 309 cells on this box** — §7's stop condition fires and (c) shrinking to ~1.4 M cells is the review's call; parked on `attempt/TH-11-step5b-20260818T024200Z`; **rescoped 2026-08-18 03:00 review as step 5c** — the ~1.4 M rung (`near ≈ 0.0018`, non-2 `ratio`) end to end off the parked branch, 480 s ceilings, `memory.peak` printed every command; step 5c attempt 1 🚫 2026-08-18 — **the stop condition fired at 0.99 M cells**: the rung meshes to 994 258 cells and its loaded solve alone pegs `memory.peak` at `memory.max` = 64.00 GiB (identity family green at 1e-9 on the solve that completed), so the wall is superlinear in cells — 0.42 M comfortable / 0.99 M pegged / 2.81 M OOM, MUMPS fill-in; **step 5 closed as a measured negative, adjudicated 2026-08-18 10:30 review** — no affordable third rung exists (a rung between 0.42 M and 0.99 M is ratio ≈ 1.2, difference signal at the 0.01 pp run-to-run floor), no 5d scoped, no 64 MHz bracket; the surviving axis is `TH-12` step 2, which names this swap. **Chunk closed:** the trend question is answered — the apparent frequency trend was the resolution term (step 4), no gated trend claim is scopeable, and §2 carries the negative.* **Step 5d COMMISSIONED 2026-09-09 (operator-interactive, `xl`) — the closure's premise expired, the closure's finding did not.** Step 5 closed on "**no rung affordable on this box**": 0.42 M cells comfortable, 0.99 M pegged at `memory.peak` = `memory.max` = **64.00 GiB exactly**, 2 807 309 cells OOM-killed at every legal rank count. That was a statement about a 64 GiB ceiling. §5.1's XL tier is **512 GiB — 8× it** — and the 0.99 M reading is *censored* (clipped at the limit, so it is a lower bound on demand, not a measurement of it), which is exactly why no honest extrapolation to 2.81 M was ever possible. This is the tier's intended case and the one the first slot was reserved for in spirit: high tet count, memory-bound, blocking a §2 claim. **Pre-registered, before the run:** one window against `fem-em-solver-xl`, `TH11_STEP5_RUNG=third TH11_STEP5_MODE=full`, `-n 8` — **not** `-n 16`: step 5a *bought* the `-n 2` → `-n 8` rank change with a measured control (fine rung reproduced to +0.00002 pp), 16 is unlicensed on this ladder and more ranks raise MUMPS fill-in, which is the wall itself. `timeout -k 60 7200`. The window reads the container's cgroup `memory.peak` after pytest and prints it — the module carries no instrumentation, and the `ANS-4` step-2b ledger note says an XL commissioning may not lean on an uninstrumented figure again. **Asserted** (all existing, all imported, none moved): the ≤ 3 400 000-cell ceiling, the complex-power identity on both solves, the free solve dissipating exactly nothing, both solves driven by the same projected current, the loaded coil dissipating and expelling flux. **Printed, gated by nothing:** cells and cells/δ at 64 MHz, `memory.peak`, and the three-rung Aitken fit `d(h) = d₀ + C·hᵖ` over **+10.2698 % / +2.8063 % / (this rung)** at fixed refinement ratio 2. **Decision rule, written before the run:** (a) the triple is monotone and the fit returns `p` in (0.5, 3) ⇒ the 64 MHz h → 0 bracket **exists** for the first time, and a *review* — not this run — may then revisit §2.1's "coil-at-Larmor is an extrapolation" sentence, and only if the bracket is consistent with step 4's overlapping 10 / 30 MHz brackets at ~−1 %; (b) non-monotone or no fit ⇒ printed as **no fit**, step 5's measured negative stands verbatim and §2.1 is unchanged; (c) OOM or the 7 200 s kill ⇒ the wall is above 512 GiB as well, the peak is recorded, and the axis closes for good on this hardware rather than being re-scoped a fourth time. **Cost:** mesh 288.2 s measured (step 5 attempt 1); the solve is the unmeasured quantity and is the whole point — every previous attempt died before returning. **Interval:** today's slot was already spent by `ANS-4` step 2b, so this run is authorised by the operator's dated, self-expiring `scripts/automation/xl-override.env`, which lifts the 7-day interval **only** — the ledger still gets its own row, the spent slot stays spent, and no scheduled session may write that file.)* **Step 5d EXECUTED 2026-09-09 — the rung solved, and the 64 MHz bracket exists for the first time.** `20260909T153910Z_TH-11-step5d.log`, **17 passed / 1 skipped, Status 0, elapsed 4838 s** at `-n 8`, complex build; **2 808 204 cells** (mesh 182.5 s), loaded solve **2331.7 s** + free **2257.6 s**, peak **263.4 GiB** (container lifetime, spanning a killed first attempt — ledger note). The August closure's premise is now measured false: the rung is not unaffordable, it needs 4× the old 64 GiB ceiling. **The ladder at 64 MHz**, `h` 0.005 → 0.0025 → 0.00125 at 1.26 / 2.52 / **5.03 cells per skin depth**: deviation from the quasi-static Dodd–Deeds form **+10.2698 % → +2.8063 % → +0.3824 %** (moves −7.4635 pp then −2.4239 pp), monotone. **Three-rung fit** `d(h) = d₀ + C·hᵖ`: **p = 1.623**, **d₀ = −0.7834 %** — inside the pre-registered (0.5, 3) window, so branch (a) fired. **The like-for-like reading** (step 4's own two-rung form on the finest pair): **[−2.0415 %, −0.4256 %]**, `p_eff` 2.876, which **overlaps both** of step 4's brackets — 10 MHz [−2.1492 %, −0.9050 %] and 30 MHz [−3.3675 %, −0.3812 %]. That is the pre-registered signature that **the eddy→displacement "trend" was the resolution term all the way to Larmor**, extending step 4's finding from 30 MHz to 64 MHz on a measured rung rather than an extrapolation. Supporting identities all green: complex-power residual **4.71e-14** loaded / **6.11e-14** free against 1e-9; the σ-blind control has the free solve dissipating **exactly 0.000 W** against the loaded **0.634 W**; ΔR by dissipation equals ΔR by reaction to all ten printed digits (1.3512462e+00 Ω); `I′` 0.968758 A. `ΔX` ratio **0.9478**, the box-truncation reading `MAT-6` step 9 measured, unchanged in kind. **Three caveats, stated:** (1) Dodd–Deeds is *quasi-static* and is the comparison, not the reference, at 64 MHz — what converges here is agreement with the quasi-static form, which bounds displacement-current effects on this fixture rather than validating full-wave physics against an independent 64 MHz reference; (2) the fit rests on three rungs at ratio 2 with no fourth to check it, and the two-rung `p_eff` of 2.876 disagrees with the three-rung `p` of 1.623, so the *bracket* is the honest object and the point estimate is not; (3) `d₀` = −0.7834 % sits just **outside** the 10 MHz bracket's upper end (−0.9050 %) while the bracket overlaps it — another reason to read the bracket. **Consequence, and it is a review's, not this run's:** §2.1's "coil-at-Larmor is an extrapolation" sentence is now **revisitable** — a 64 MHz h → 0 bracket exists and is consistent with 10 / 30 MHz — and the 2026-09-13 weekly owns whether it moves. Nothing in §2 was changed here. 
