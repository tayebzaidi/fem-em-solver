# OPS-40 — `evaluate_vector_field_parallel` refuses a point list that differs across ranks

 **`evaluate_vector_field_parallel` refuses a point list that differs across ranks** — opened 2026-09-07 03:00 review from `EX-52`'s 22:30 slot: the collective sizes its rank-0 buffer by the caller's `points` and scatters each rank's hits into it by index (`post/evaluation.py:68–77`), so a caller that hands each rank its own local points (the example's first draft: `point_sar` on local centroids) dies with `IndexError: index 36378 is out of bounds for axis 0 with size 36378` at `-n 2` (`20260907T033551Z_EX-52.log:400`) and passes silently at `-n 1`. Implementer; real build for the unit, complex for the gate re-runs. **Change:** at entry, `counts = comm.allgather(n_points)`; if `len(set(counts)) > 1` raise `ValueError` on **every** rank naming each rank's count and the rule ("collective: the point list must be identical on every rank — allgather your points first"), before any bounding-box work. One allgather of an int per call — no measurable cost. **Anchors (asserted):** (i) new `tests/post/test_evaluation_collective_guard.py` at `-n 2`: a rank-dependent list (`n = 3 + rank`) raises `ValueError` on every rank (an `allgather` of the caught-exception flag reads `[True, True]` — a one-rank raise ahead of the next collective is a hang) and the message carries both counts; (ii) the identical-list path is bit-unchanged — the guard precedes the first existing line; (iii) the `EX-52` gate `tests/validation/test_lossy_sphere_sar.py` (12 passed / 56 s at `-n 2`, `20260907T034300Z_EX-52-gate.log`) and `mat:2` (48 s) re-run green through the harness, their imported bounds unmoved. **Negative control (asserted):** at `-n 1` the same construction does not raise. **Tier / cost:** unit smoke `-n 2` `timeout -k 30 120` (seconds); the two re-runs standard, `-n 2`, `timeout -k 30 300` each. **Traps:** raise on every rank or not at all — never a rank-0-only raise ahead of a collective; pytest `-s` to see the message; `-k a or b`. **Scope:** a guard; no behaviour change on valid input, no band; the `_path_voltage` / `point_sar` / example consumers are not edited. **Negative result:** the guard tripping inside an *existing* green consumer at `-n 2` is a live bug found — known-issues with the module name, do not weaken the guard, stop. **Closed 2026-09-07 (implementer).** The guard landed as written — `counts = comm.allgather(n_points)` immediately after `n_points` is read and **before** any bounding-box work (`src/fem_em_solver/post/evaluation.py:38–56`), raising `ValueError` on every rank with the per-rank counts (`rank 0: 3, rank 1: 4`) and the rule string; no consumer, band or record touched. (i) New `tests/post/test_evaluation_collective_guard.py`, **4 passed / 1.58 s** at `-n 2` (`20260907T183121Z_OPS-40.log`, Status 0, elapsed 4 s, smoke, real build): the `n = 3 + rank` list gives an `allgather` of the caught-exception flag equal to `[True, True]` — every rank raises, no hang — and every rank's message contains both `rank 0: 3` and `rank 1: 4`, `"collective"` and `"allgather your points first"`. (ii) The valid path is unchanged and quantitatively correct: the same module evaluates an interpolated P1 field against its closed form `(x, 2y, 3z)` at 5 shared points to a max deviation **< 1e-12**, with `valid_mask` all true and the returned array bit-identical across ranks (`allgather` equality); the pre-existing `points must have shape (N, 3)` check still fires on both malformed inputs. **Negative control:** the identical rank-dependent construction at `-n 1` does **not** raise — **4 passed / 0.63 s** (`20260907T183132Z_OPS-40.log`, Status 0, 2 s). (iii) Both `EX-52` consumers re-run green with bounds unmoved: the gate `tests/environment tests/validation/test_lossy_sphere_sar.py` at `-n 2` complex, **12 passed / 55.56 s** (`20260907T183143Z_OPS-40.log`, Status 0, 57 s) against the 12 passed / 56 s record; `mat:2` at `-n 2` complex, Status 0, **47 s** (`20260907T183248Z_OPS-40.log`) against the 48 s record, reproducing SAR errors **3.422%** and **3.536%** against the 10% bound and both negative controls. No existing green consumer trips the guard — the pre-registered negative result did not occur. **Audited 2026-09-07 18:00 review (`auditor`, PASS; re-traced here).** All four logs footered `Status: 0` with elapsed 4 / 2 / 57 / 47 s (`20260907T183121Z_OPS-40.log:63–66`, `…183132Z:49–52`, `…183143Z:203–206`, `…183248Z:266–269`), headers on `c0433fa` = the closer's parent; 9 of 11 digits traced textually (`…183121Z:58, 65`; `…183132Z:47, 51`; `…183143Z:136, 201, 205`; `…183248Z:254, 256, 264, 268`; the origin `IndexError` at `20260907T033551Z_EX-52.log:400`) and the remaining two — the per-rank counts and rule string in the message — are enforced by `assert … in msg` inside the new module and recorded PASSED, not printed; both anchors are executed asserts (`< 1e-12` on the closed form, `flags == [True] * comm.size`); the diff is one new test module plus the 18-line guard, no pre-existing test, band or record touched. Two caveats, neither a demotion: the wrappers ran at `timeout -k 30 120` / `300` rather than the 30 s / 180 s tier ceilings (measured elapsed is inside the ceilings), and there is no red baseline for a new test — the provenance is `EX-52`'s `IndexError`, not a red-then-green. Re-traced by the review: `…183121Z:58` `4 passed in 1.58s`, `:65` `Elapsed (s): 4`; `…183248Z:254` `3.422% vs 10% bound`, `:256` `3.536% vs 10% bound`, `:268` `Elapsed (s): 47`. | ✅ 
