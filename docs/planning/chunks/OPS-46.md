# OPS-46 — Move the heavy §7 chunk histories out of `PROJECT_PLAN.md`

 **Move the heavy §7 chunk histories out of `PROJECT_PLAN.md`** — *opened 2026-09-12 by an interactive operator session (§9 operator directive 1); the next daily review rules on it before it is queued.* **The defect, measured 2026-09-12:** the plan is 1 299 833 B, of which §7 is 1 099 241 B (**85%**). A chunk's whole step history accumulates inside its table cell and a table cell cannot be partially read — `WF-6`'s cell is 41 963 B, `ANS-4`'s 33 943 B, the 15 largest rows carry 44% of all row text, and 104 of the 217 rows are under 400 B. The two largest cells belong to an *open* chunk and a benchmark chunk, so closure does not predict size and archiving only ✅ rows would leave the worst offender in place. **Change:** every row over 2 KB (74 rows, ≈ 552 KB) moves **byte-for-byte** to `docs/planning/chunks/<ID>.md`; the §7 cell keeps title, status, the current state in ≤ 2 sentences, and the pointer. Closed chunks' files go under the existing archive path; open chunks' files are where implementers append instead of growing the cell. Extends `scripts/maintenance/rotate_plan_archive.py`; no new machinery. **Prerequisite, before any row moves:** `scripts/testing/check_private_leak.py` selects its corpus by glob, so `docs/planning/chunks/**` must be added to that pattern set or the moved files escape the privacy check — and `ANS-4`, the Ansys benchmark chunk, is the second-largest cell being moved. **Also edits:** `plan-navigator`'s corpus is explicitly fixed and must gain the new path or it will return NOT FOUND for text that exists; `auditor` step 1 ("the chunk's §7 entry, in full") becomes the row plus its chunk file; one sentence each in `implementer-run.md`, `daily-review.md`, `weekly-review.md` on which half takes the narrative and which the status; one sentence in `AGENTS.md` rule 4, since chunk files are a new surface a non-Claude agent could write to. Citations of the form "§7 `ANS-4` row" stay valid — the row still exists. **Anchors (asserted):** (i) every moved cell's text is byte-identical to its new file — a diff of the extracted cell against the file is empty, so no recorded figure can be silently rewritten; (ii) `check_private_leak.py` exits 0 with the extended pattern set, and a planted synthetic value under `docs/planning/chunks/` is caught by it (positive control) before the real move; (iii) `plan-navigator` answers a question whose answer now lives only in a chunk file, with a citation; (iv) the plan is under 0.85 MB, re-measured with `scripts/probes/measure_plan_sections.py` and recorded before/after. **Commits:** (a) leak-check pattern set + the four protocol/agent edits; (b) the row moves, **one commit per §7 family** so a bad move is revertable; (c) the re-measurement. Docs/tooling only — no `src/`, no test, no band. **Ruled 2026-09-12 18:00 review — ACCEPTED and split into four implementer steps (§9 items 1, 3, 4, 5).** Re-measured by this review with the same 2 KB cut: **74 rows** over 2 048 B, grouped by family — OPS 20, EX 19, GEO 12, PORT 8, TH 4, ANS 3, MAG 2, MAT 2, POST 2, WF 1 — the three largest being `WF-6` (43 961 B), `ANS-4` (41 811 B) and `OPS-26` (22 040 B). Two corrections to the scope as written: (1) **the two agent-definition edits (`plan-navigator` corpus, `auditor` step 1) are operator-owned** — `Edit(.claude/**)` is on the *ask* list in `.claude/settings.json`, which a headless session cannot answer — so they are a Waiting-on-you item on the dashboard, anchor (iii) is asserted only once the operator lands them, and until then reviews Read a chunk file directly instead of asking the navigator; (2) the **narrative blocks below the tables** (`PORT-14`, `TH-15` and others carry their step histories as `>` blockquotes, not in the cell) are outside this chunk's 2 KB-row rule and are measured, not moved, by step 4 for a follow-up. **Step 1 (tooling, nothing moved):** `check_private_leak.py` gains `docs/planning/chunks/**` in its pattern set with a positive control; `rotate_plan_archive.py` gains a `chunks` subcommand that extracts a named row's status cell byte-for-byte to `docs/planning/chunks/<ID>.md`, rewrites the cell to title + status glyph + a state line the spec file supplies + the pointer, refuses to write unless the extracted text equals the file it wrote (anchor (i)) and supports `--dry-run`; the three protocol docs and `AGENTS.md` rule 4 gain their sentence. **Steps 2–4 (the moves, one commit per family):** WF + ANS + PORT; OPS + GEO + MAG; TH + MAT + POST + EX, then the re-measurement (anchor (iv), < 0.85 MB) and the closure claim. Status moves: step 1 ⬜ → 🟡; step 4 🟡 → ✅ if (i), (ii) and (iv) hold and (iii) is recorded as operator-pending. **Step 1 landed 2026-09-12 19:30 slot (tooling only, nothing moved):** `rotate_plan_archive.py chunks` (spec `=== <ID>` + one state line; `--dry-run`; `--census`) and the tracked anchor script `scripts/probes/ops46_step1_anchors.sh`, all green in `20260913T003904Z_OPS-46-step1.log` (smoke, 20 s, no `mpiexec`): (b) `OPS-36` moved on a copy under `logs/` — chunk body equals the independently re-extracted cell (`cmp` 0, 2212 B both), the copy shrinks by exactly cell − replacement (2212 − 229 B), `git diff --stat PROJECT_PLAN.md` empty; negative controls — one altered chunk-file byte refused (exit 1, copy unchanged) and a nonexistent row refused (exit 1, nothing written); (a) a synthetic plant under `docs/planning/chunks/` caught (exit 1, file named) and the tree clean afterwards in hook and `--audit` mode. **Three readings recorded:** (1) `check_private_leak.py` has no path glob — its corpus is the staged diff (hook) or every tracked file (`--audit`) — so no pattern-set edit was needed and the positive control is the proof; (2) §7 rows are `ID \| Title \| Status \| Tier`, and the history sits mostly in the *Title* cell, so the tool moves the span from the ID cell's end to the Tier cell's start (Title + Status) byte for byte and derives the title from the head before the first " — "; (3) the container's `git` refuses `/workspace` ("dubious ownership") and the leak check swallows git errors into "nothing staged", so a leak check run in the container without `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=safe.directory GIT_CONFIG_VALUE_0=/workspace` passes vacuously (printed in the log, exit 0 on the plant) — the host `pre-commit` hook is unaffected. **Census on the current plan (c):** 75 rows over 2 048 B parse (0 unparseable), per family OPS 20, EX 19, GEO 13, PORT 8, ANS 4, TH 4, MAG 2, MAT 2, POST 2, WF 1; against §9 items 3–5's lists (73 IDs) the census adds `GEO-17` and `ANS-5` and misses none — the review's "74"/GEO 12/ANS 3 is off by these two. **Step 2 landed 2026-09-12 19:30 slot (docs only): the WF, ANS and PORT families (12 rows) moved, one commit per family** (`2592c5b`, `efb05ed`, `8404422`), each through `scripts/probes/ops46_move_family.sh`: (i) every span re-extracted from `git show HEAD:PROJECT_PLAN.md` `cmp`-equals its chunk body, (ii) the leak check exits 0 on the move staged into a scratch index with git asserted to see it, (iii) one plan line per row and nothing else; the missing-row negative control refused before the first move (`20260913T005037Z_OPS-46-step2-WF.log`, `…005108Z_…-ANS.log`, `…005133Z_…-PORT.log`, Status 0, 3 s each). Plan 1342548 → 1165545 B. One earlier WF window (`…004941Z`) was red on the script's own no-other-changes check (harness outputs counted); the move was reverted and re-run. **Step 3 partly landed 2026-09-12 19:30 slot:** the MAG (2 rows, `c96873e`, `20260913T005401Z_OPS-46-step3-MAG.log`) and GEO (12 rows, `ac1b22c`, `20260913T005431Z_OPS-46-step3-GEO.log`) families moved with (i)/(ii)/(iii) green, Status 0; plan → 1064162 B. The OPS family (20 rows) is not yet moved (§9 item 4). | 🟡 

**Step 3 landed 2026-09-12 21:00 slot — the OPS family (20 rows) moved, so step 3 is complete** (`b27e690`, `20260913T020237Z_OPS-46-step3-OPS.log`, Status 0, 5 s, smoke, no `mpiexec`). This row moved with its own family, so this file now holds the history; the §7 row carries the state line "in progress, steps 1–3 landed". Anchors through `scripts/probes/ops46_move_family.sh`, 47 PASS and 0 FAIL: (i) all 20 spans re-extracted from `git show HEAD:PROJECT_PLAN.md` `cmp`-equal their chunk bodies; (iii) `git diff --numstat` 20/20 on the plan, 20 new chunk files, no other change; (ii) the leak check exits 0 on the move staged in a scratch index, with git seeing 21 paths. A `--dry-run` on the spec went first and confirmed every glyph (19 ✅, 1 🟡) and every state line before the real move. Plan 1 064 986 → 927 309 B (137 677 B moved out). The glyphs are unchanged by construction. Step 4 (TH, MAT, POST, EX, then anchor (iv) < 850 000 B and the closure claim) is §9 item 5.

**Step 4 landed 2026-09-12 21:00 slot (second item) ⇒ chunk ✅.** Four family moves, one commit each, all through `scripts/probes/ops46_move_family.sh` with every anchor green. Each spec was dry-run first and the tool's glyph read-back checked (TH-11 ✅, TH-12 ✅, TH-15 🟡, TH-19 ⬜, POST-6 🟡, the rest ✅). One dry-run refusal fixed before the move: `EX-24`'s first state line carried a digit run the row lacks.
- **TH** (4 rows): `7abb7f0`, `20260913T020527Z_OPS-46-step4-TH.log`, Status 0, 15 PASS / 0 FAIL; plan 927 147 → 898 428 B.
- **MAT** (2 rows): `294603d`, `…020555Z_OPS-46-step4-MAT.log`, Status 0, 11 PASS / 0 FAIL; → 889 116 B.
- **POST** (2 rows): `dfa6377`, `…020624Z_OPS-46-step4-POST.log`, Status 0, 11 PASS / 0 FAIL; → 882 390 B.
- **EX** (19 rows): `6c0c529`, `…020641Z_OPS-46-step4-EX.log`, Status 0, 45 PASS / 0 FAIL; → 806 962 B.

**Closure anchors.**
- **(i)** held on every moved row: 12 (step 2) + 34 (step 3) + 27 (step 4) = **73 rows**, each span re-extracted from git HEAD and `cmp`-equal to its chunk body. The 18:00 review's "74" is the census discrepancy recorded under step 1: §9 items 3–5 list 73 IDs, and `GEO-17` / `ANS-5` are over 2 048 B but in no list, so they were not moved.
- **(ii)** `check_private_leak.py --audit` on the final tree at `6c0c529` exits 0, with git asserted to see the 73 tracked chunk files so the pass is not vacuous (`20260913T020720Z_OPS-46-step4-leak-audit.log`, Status 0, 17 s).
- **(iv)** plan **806 962 B < 850 000 B**. Before and after, each with `measure_plan_sections.py`'s top-45: `20260913T020422Z_OPS-46-step4-measure-before.log` (927 147 B before step 4; 1 342 548 B before step 2) and `20260913T020712Z_OPS-46-step4-measure-after.log` (806 962 B, 73 chunk files).
- **(iii)** is **operator-pending**: the `plan-navigator` corpus and `auditor` step 1 edits sit under `Edit(.claude/**)`, which a headless session cannot make. Until they land, read a chunk file directly.

**For the narrative-block follow-up** (`--blocks`, same after-log): `TH-15` row 1 line plus a `nar` block of 1 242 lines (lines 1912–3153); `PORT-14` row 1 line plus a `nar` block of 735 lines (3945–4679). The top-45 after the moves is led by chunk spans that are now mostly narrative blocks: `WF-6` 158.4 kchars, `TH-15` 85.7, `PORT-14` 49.9, `MAT-4` 21.6, `POST-6` 10.9.
