# OPS-32 — Private-mode comparison writers for `ANS-3` and `ANS-4`

 **Private-mode comparison writers for `ANS-3` and `ANS-4`** — both `03_two_torus_gap_ports_10MHz.py` and `04_birdcage_four_port_10_64_128MHz.py` regenerate their tracked `COMPARISON.md` with `write_text`, exactly the shape that would have published `ANS-1`'s AED numbers; port `ANS-1`'s `aed_results/` reader + `private=True` writer pattern (`14305c5`) to both **before** either gains AED numbers, and re-register their in-script *reproduction controls* at **1e-6** against the measured ≤ 5e-8 run-to-run Z/S scatter (`EX-37`; the 2026-09-02 weekly's ruling, (1*) record licence — no physics band moves). Done-when: each example re-run exit 0 with the tracked table's AED cells blank by construction and, with a stub `aed_results/*.json` present locally (deleted after), an untracked `COMPARISON_private.md` filled; census `exit != 1`. Standard (`ans:3` 128 s, `ans:4` 125 s measured). Scoped 2026-09-02 weekly review. **Clause added 2026-09-02 10:30 review:** `scripts/testing/check_example_doc_references.py:357` scans every `*.md` under `examples/` including gitignored ones, so `ANS-1`'s `COMPARISON_private.md:6` already reads as a dead reference and the census sits at `dead=1 … exit=1` on `main` (`20260902T094305Z_OPS-31.log:36,46`; known-issues entry) — the two private files this chunk adds would make it three. Skip `*_private.md` (or anything `git check-ignore` matches) in the guide scan; done-when for the clause: with the private file present, the census reads `dead=0` and exits 0 or 2, and the scanned-guide count printed on the same line drops by exactly the number of private files present. Until this lands, every item whose anchor says "census `exit != 1`" reads it as "no dead reference other than the known `COMPARISON_private.md` line" | ✅ *(2026-09-02 — **writer half landed, control half is a fixture finding**. Anchors: (i) both examples re-run **exit 0** with every gate green — `ans:3` `20260902T183603Z_OPS-32.log`, Status 0, **171 s**, `‖S − Sᵀ‖/‖S‖ = 4.7586e-05 < 1e-03`, `‖S‖₂ = 0.864809`, corrected mutual −6.02% inside the 10% band with the raw ratio still a MISS at −10.55%; `ans:4` `20260902T183909Z_OPS-32.log`, Status 0, **172 s**, three gates green on all three rungs of one 116085-cell mesh, 10 MHz anchors 1.157e-10 vs 1e-06 and 2.568e-10 vs 1e-09. (ii) With a **synthetic** `aed_results/*.json` stub (values 1.0 / 2.0, deleted after the run) each script wrote a filled `COMPARISON_private.md` while the tracked `COMPARISON.md` kept every AED cell blank — the negative control held: `git diff` of both tracked tables contains **zero** stub values, and neither private file nor either `aed_results/` appears in `git status --porcelain` (`.gitignore:129–130`). Both tracked tables now also state the blankness is by construction. (iii) Census `20260902T184211Z_OPS-32.log:34,55` — `Scanned 47 guide(s) …`, `RESULT: dead=0 guide=0 stale=16 stale_severity=report exit=2`, against the morning's `Scanned 48 … dead=1 … exit=1` (`20260902T094305Z_OPS-31.log:34,46`): `dead=0`, `exit != 1`, and the scanned-guide count is the 50 markdown files under `examples/` minus the **3** `*_private.md` now present — exactly the clause's rule. The 16 stale hits are age-only (`OPS-19` exit-2 class). **Control half not done:** `ans:4`'s in-script controls were *already* 1e-6 / 1e-9 and needed nothing; `ans:3`'s four would **fail** at 1e-6 — measured raw 2.98e-05, corrected 2.92e-05, symmetry 1.91e-06, spectral 3.07e-10 — because they are taken against the `PORT-1` step-4 **log record**, not against a repeat of the same run, so 1e-6 would re-base a record comparison rather than tighten a scatter control. Band left at `EX-20`'s 1% with the measurement in a code comment (`03_two_torus_gap_ports_10MHz.py:113`) and a known-issues entry filed; a re-record of the three `RECORDED_*` constants under the (1*) licence must precede any 1e-6 control. No physics band, gate or §2 claim moved.)* *Audited 2026-09-02 18:00 review — `auditor` PASS on all eight checks, and the review re-traced `20260902T184211Z_OPS-32.log:34,55` itself (`Scanned 47 guide(s)`, `dead=0 … exit=2`). **Ruling on the slot's flag:** anchor (ii)'s letter ("tracked `COMPARISON.md` unchanged") was unmeetable as written — the table is a generated artifact that re-stamps its timestamp and its own FEM digits on every run, and anchor (i) requires the run — while its purpose held without exception: the auditor's own `git show 67281bf` grep finds every AED cell blank, zero stub values, and `"aed": null` in both `metrics.json`. The slot's decision to let the commit stand is **ratified**, and the control is restated for every future item on a generated artifact: "no AED value and no non-blank AED cell in the tracked diff", never "unchanged". The control half's follow-up is `OPS-33`.* 
