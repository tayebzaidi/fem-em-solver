# EX-52 — Imposed-field SAR on the lossy sphere against its closed form in ParaView

 **Imposed-field SAR on the lossy sphere against its closed form in ParaView** — the `MAT-4` step 1 gate (lossy-sphere SAR to 3.5 % on an imposed uniform field at 64 / 128 MHz) has no example of its own: `th:6` shows the field, `mri:2` the mass-averaging operator on a coil, `ports:9` coil-driven quadrant powers; the SAR-on-an-imposed-field angle (Phase 3's ramp is one short: 2 examples + the `ans:1` benchmark against a bar of 3 — 2026-09-06 weekly audit) is uncovered. Same-stem guide; every record and band imported from the `MAT-4` gate module, never restated; the closed-form SAR profile and the FEM `σ\|E\|²/2ρ` written to one combined XDMF; negative control per rule (e), *asserted*: the vacuum sphere's SAR at 0 to 1e-12. Census both windows through the harness, pre-census before any file is written. **Queued 2026-09-06 18:00 review as §9 item 3** (`mat:2`, `examples/materials/02_lossy_sphere_sar.py`; the full rubric item — imports, the two-σ ratio control backed by `20260803T020448Z_MAT-4-step1-gate.log`, the 40 s cost — lives there). **✅ 2026-09-06 (22:30 slot).** `examples/materials/02_lossy_sphere_sar.py` + same-stem guide, `mat:2`, exit 0 in **48 s** at `-n 2`, complex (`20260907T034158Z_EX-52.log`); 74 020 cells, three solves in 40.1 s. Anchors all asserted on the gate module's *imported* bounds and all held: mean SAR **3.422 %** (σ = 0.05) and **3.536 %** (σ = 0.57) against the imported 10 % bound — reproducing the §7 record 3.42 % / 3.54 % to the printed digits, printed not asserted; interior `E_z` spread 0.067 % / 0.107 % against 2 %; `Im/Re E_z` 0.1752 vs 0.1755 and 1.9900 vs 2.0011 inside 10 %. Both negative controls asserted and held: the two-σ separation **4.850×** against the 3× floor (σ-blind 11.4000; the 4.855× ceiling printed, not asserted), and the σ = 0 vacuum sphere at `dissipated_power_w` **exactly 0.0** with mean SAR 0.0 ≤ 1e-12. One combined XDMF written (`materials_02_lossy_sphere_sar_combined.xdmf`: `SAR_pointwise`, `SAR_closed_form`, `CellTags`). Census unchanged across the pair — `dead=0 guide=0 stale=79 exit=2` both windows (`…033328Z_EX-52-precensus.log`, `…034407Z_EX-52-postcensus.log`). One **additive** change to the gate module under standing rule (a): `_solve_lossy_sphere` gains keyword-only `return_fields=False` (default = the pre-`EX-52` behaviour and return dict exactly), re-run green in the same slot — `12 passed` / 56 s (`…034300Z_EX-52-gate.log`). No band moved, no `src/` change. **Scope:** an example of the gated capability on an **imposed uniform field** at 64 MHz — no coil, no mass averaging, no Larmor-coil claim. | ✅ 
