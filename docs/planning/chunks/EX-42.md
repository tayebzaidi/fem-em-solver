# EX-42 — `mat:1` prints the finite-wire-corrected Dodd–Deeds beside the filament form and the FEM ΔR

 **`mat:1` prints the finite-wire-corrected Dodd–Deeds beside the filament form and the FEM ΔR** — `MAT-8` ✅ 2026-09-02 gated `utils/dodd_deeds.coil_impedance_change_finite_wire`, and no example shows it: `examples/materials/` `mat:1` (the `MAT-6` fixture, ΔR 1.5838% vs the filament form) is the one place the corrected column belongs, and the sign matters (the term *raises* the closed form; the refined FEM sits below it). Executor `example-runner`. Add one column and two guide sentences: the finite-wire closed form at the fixture's `r_wire = 0.0025`, the FEM ΔR's deviation from **both** forms, and the correction itself. **Anchors (asserted in-script):** the correction reproduces `MAT-8`'s record **+0.115237%** on ΔR / **+0.144814%** on ΔX at rtol 1e-6 (same function, same inputs — a reproduction, so a miss is a wiring defect); the existing 1.5838% filament deviation record unmoved; census `exit != 1` read past the known `COMPARISON_private.md` line until `OPS-32` lands. **Negative control:** the column at `r_wire = 0` equals the filament column to 1e-12 (`MAT-8` measured 1.785e-16). Cost: `mat:1` ran inside a 73 s three-example window (`20260901T140821Z_EX-36-leg-rootmrimat-d.log`) ⇒ **≈ 60 s**, `./run_examples.sh mat:1 -t 400`, `-n 2` complex, foreground. Scope: an output column and a record; no band, no gate, `MAT-6` stays at 1.58% until step 11. Negative result: a record not reproducing is a wiring defect — journal, stop. Opened 2026-09-02 10:30 review | ✅ *(2026-09-03 15:00 slot, `20260903T200603Z_EX-42.log`, Status 0, **64 s** harness / 63.0 s in-script at `-n 2` complex — standard by measurement. Every anchor met: the finite-wire correction at the fixture's `r_wire = 0.0025` m reads **+0.115237% ΔR / +0.144814% ΔX** (`:157`), both asserted against the `MAT-8` record (`20260902T123618Z_MAT-8.log:71–74`) at rtol 1e-6 — the record has no importable constant in `test_dodd_deeds_finite_wire.py`, so it is carried as two full-precision literals recomputed from the **same** closed-form function at the **same** imported fixture inputs, a reproduction not a restatement; ΔZ finite wire = +3.2296790e-01 + j(−6.1675935e-01) Ω; the filament ΔR deviation record **1.5838%** unmoved against its 2% ceiling, with 1.4669% vs the finite-wire form printed but **not gated** (`:158`); the sign is stated in the guide — the term raises the closed form. **Negative control:** `r_wire = 0` collapses onto the filament form to **1.785e-16** relative on ΔR and ΔX, asserted `< 1e-12` — four decades of separation, matching `MAT-8`'s own reading. Census `exit != 1` at both ends: pre-census `dead=0 guide=0 stale=64 exit=2` (`20260903T200105Z_EX-42-precensus.log:103`), post-census `dead=0 guide=0 stale=63 exit=2` (`20260903T200716Z_EX-42-postcensus.log:102`) — the one-item stale drop is `mat:1` refreshing its own combined XDMF, predicted before the read. One executor deviation, display-only and fixed before the recorded window: the first run (`20260903T200410Z_EX-42.log`, Status 0, 66 s) printed the corrections 100× too large from a `%` format spec applied to an already-percent value; the assertions used the raw floats and passed in that run too. No band, no gate, no §2 change; `MAT-6` stays at 1.58% until step 11)* 
