# OPS-39 — The `TH-15` step-0 probe's `_census` made rank-safe, gated on the count identity at `-n 2`

 **The `TH-15` step-0 probe's `_census` made rank-safe, gated on the count identity at `-n 2`** — opened 2026-09-06 10:30 review from the 00:00 slot's finding: `tests/mesh/probe_two_torus_conductor_hole.py::_census` sums `tags.values` across ranks and counts every ghost entity once per rank that holds it, so under the two-torus fixture's `GhostMode.shared_facet` the solid's tag census overshot the owned cell count by **2972** at `-n 2` (`20260906T050521Z_TH-15.log:1464, 1485`: 187 148 against 184 176; tag 2 read 9448 vs step 0's 9348, 1.0697% > the 1% band) — the first `TH-15` step-2a window failed on it, the gate module masked its own census on `size_local` (`tests/mesh/test_two_torus_conductor_hole.py:119–134`), and the probe still carries the defect (known-issues entry 2026-09-06). **Done-when (§4):** the probe masks on `index_map(dim).size_local` before counting; a new unit module `tests/mesh/test_probe_census_rank_safety.py` asserts `Σ_tag census[tag] == index_map(tdim).size_global` exactly on a `create_box` mesh with `shared_facet` ghosting at `-n 1` and `-n 2`, and asserts the *naive* sum exceeds it by exactly `Σ_ranks num_ghosts` at `-n 2` (0 at `-n 1`) — the overshoot computed, not predicted; the probe's solid variant re-run at `-n 2` through the harness prints step 0's 9471 / 9348 / 110 696; the gate module's import of `_census` stays green; elapsed recorded; the known-issues entry retired in the same commit. Smoke (unit module) + standard (probe re-run, ≈ 50 s at `-n 2`), real build. Scope: a probe helper and its test — no generator change, no band, no record. Full item: §9 item 3 of the 2026-09-06 10:30 review. **Closed 2026-09-06 (15:00 implementer slot).** `_census` gained two optional keyword arguments (`indices=`, `n_owned=`) masking on `index_map(dim).size_local`, plus a probe-local `_owned_census` wrapper at all three probe call sites; the positional `(values, comm)` signature and `{tag: count}` return type are unchanged, so the gate module's import is untouched and green (`20260906T200326Z_OPS-39.log`, `5 passed`, 57 s at `-n 2`). New gate `tests/mesh/test_probe_census_rank_safety.py` (3 tests): on a 3072-cell `create_box` with `shared_facet` ghosting the masked census sums to `size_global` **exactly** (3072, both widths) and the naive census overshoots by **exactly `Σ_ranks num_ghosts`** — 0 at `-n 1` (`20260906T200217Z_OPS-39.log:43–45`, 5 s) and **256** at `-n 2` (naive `{1: 774, 2: 770, 3: 1784}` = 3328, `20260906T200227Z_OPS-39.log:51–53`, 2 s); the per-tag counts are also gated against the closed form `6·N²` per grid column (768 / 768 / 1536), width-invariant. Probe solid variant re-run at `-n 2` reproduces step 0's `-n 1` digits to the integer: 9471 / 9348 / 110 696 / 13 661 / 13 648 / 13 658 / 13 694, 184 176 cells, 31 550 vertices (`20260906T200239Z_OPS-39.log:950–952`, 33 s) — no rank-count mesh difference. Known-issues entry retired in the same commit. | ✅ 
