# ANS-1 — Loop over a lossy slab at 10 MHz: runnable half of the first AED benchmark

 Loop over a lossy slab at 10 MHz: runnable half of the first AED benchmark | ✅ *(example path restored 2026-08-31 by `EX-37` and re-run green, 63 s; the 08-09 record stands.* **AED half replicated 2026-09-01** *by the operator — Maxwell 3D 2026.1.0 AC Magnetic, low-order pair, adaptive to well under the 0.5% energy-error target. **The numbers are held privately and are never committed** (operator directive 2026-09-02: Ansys licence terms restrict disclosure of benchmark results) — they live in the gitignored `aed_results/` + `COMPARISON_private.md`, which the example writes from the results JSON so the private table survives regeneration (`20260902T…_ANS-1-private.log`). Methodology finding, public: Maxwell 3D AC Magnetic does not support higher-order shape functions with a winding excitation and falls back silently, so the `ANS-5` order-sensitivity column does not exist for eddy-current cases — run the low-order pair only (`ANS-3` take note). **Adjudicated 2026-09-02 weekly review — AGREE.** Both FEM codes bracket the Dodd–Deeds ΔR closed form on opposite sides, each inside `MAT-6`'s band; on ΔX both sit below the infinite-half-space value by the amount `MAT-6` step 9's box-truncation reading predicts for the SPEC's cube, so the truncation reading is confirmed by an independent solver and ΔX stays reported-never-gated. No disagreement to diagnose; no absolute-accuracy claim outside the eddy-current regime follows. Consequences: `MAT-6` step 8's slab-refined fixture may be promoted (**`MAT-6` step 11**, below); the filament-vs-finite-wire term is the closed form's unquantified floor for any sub-0.5% claim (**`MAT-8`**); `ANS-3`/`ANS-4` run the low-order pair only. Numeric ruling in the gitignored `docs/private/ans1-adjudication-2026-09-01.md`)* **Record rule (2026-09-10 03:00 review, on `OPS-44`'s recurrence hypothesis):** any `ans:` case that commits its own `metrics.json` adds that path to `COMMITTED_EXAMPLE_ARTIFACTS` in `tests/unit/test_doc_reference_exit_codes.py` **in the same commit**, with a dated provenance line — the pin stays a pinned path set (18:00 ruling (3)), and the declaration belongs to the chunk that widens the exemption, not to whichever later slot trips over the red (it has gone stale silently twice: `EX-29` → `OPS-44`). 
