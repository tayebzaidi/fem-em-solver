# EX-51 — The F-human birdcage in ParaView

 **The F-human birdcage in ParaView — the 16-leg, 32-ring-port coil at `ring_radius` 0.15 m (the 30 cm coil of the operator's 2026-08-25 directive) meshed at fixed absolute sizing, its 504 642 cells beside the similar-mesh control that fails the conductor-mass gate** (`GEO-25` step 2's newly gated capability, 2026-09-05: the F-human rung as a gated fixture — the **geometry / scale** angle: `mesh:8` / `mesh:9` / `mesh:11` show the 16-leg coil at 0.07 m only; no example shows the human-scale coil or *why* the sizing must not scale with the radius; opened 2026-09-05 18:00 review, §5.4 ramp). Executor `example-runner`, spawned foreground, both census windows through the harness; **real build** (a mesh, no solve). `examples/meshing/12_birdcage_f_human_rung.py` / `.md` (`mesh:12`): both rungs from the imported `_params(F_HUMAN_RING_RADIUS, scale_sizing=…)` of `tests/validation/test_birdcage_f_human_rung.py` — the single source of the parameter set — through `MeshGenerator.birdcage_port_domain`. **Anchors (asserted, imported, never restated):** branch-B cells vs `F_HUMAN_BRANCH_B_CELL_RECORD` (504 642) at `CELL_COUNT_BAND`; volume partition at the `GEO-18` `EXACT` 1e-9; all 32 ring-port terminal ratios inside `TERMINAL_RATIO_BAND` [0.95, 1.0]; meshed/CAD conductor mass ≥ `CAD_MASS_GATE` (0.95; record 0.965414). **Negative control (asserted — backed by the gate module's measurement of the same comparison on the same rungs, `20260905T183654Z_GEO-25.log:20024–20027`):** branch A (`scale_sizing=True`) reads `cad_ratio < CAD_MASS_GATE` (record 0.893028) with its cells at `F_HUMAN_BRANCH_A_CELL_RECORD` (204 977) at the band; assert `control < CAD_MASS_GATE <= fixture` and print the separation (record 0.072387). **Writes** one combined XDMF of the **fixture** mesh — `CellTags`, the 32 sheet facet tags through `write_xdmf_with_tags(…, facet_tags=…)`, and a DG0 `cell_diameter` array so the fixed sizing is visible against the coil's size; the control's mesh is *not* written (its numbers go to the log and the guide — the picture is the fixture, the control is the reason). **Tier / ranks / cost:** the gate module's two builds took 109 + 63 s at `-n 2` (`20260905T183654Z_GEO-25.log:9728, 20019`) ⇒ ≈ 200 s plus a ~15 s write; standard by host-runner window, `./scripts/run_examples.sh -e mesh:12 -n 2 -t 600`, real build, foreground. **Traps already paid for:** counts, areas and CAD masses are rank-local — the module's helpers reduce, use them; both rungs in one process is what the gate module does (no gmsh contamination seen there); full-filename guide references; `EX-47`'s runner-socket fallback; tag and DG0 arrays need distinct `name`s. **Scope:** a mesh, not a solve — no field, no port gate, no physics claim, **no Phase 6 cost claim** (the 64 MHz solve on this rung is unpriced and is the weekly's to date); no band moved. **Negative result:** a cell count off the record at the band through the example path while the module holds it is an example/test divergence — known-issues entry, stop. **Executed 2026-09-05 22:30 slot (`example-runner`, foreground, one 196 s window at `-n 2`, real build; `20260906T033712Z_EX-51.log`, Status 0):** every anchor an executed `assert` on an imported band — fixture cells **504 642** vs the record at relative **0.000e+00** (`:20003`), volume partition **1.000000000000** at `EXACT` 1e-9 (`:20004`), all **32** ring-port terminal ratios in **[0.974454791, 0.974455230]** inside [0.95, 1.0] (`:20005`), meshed/CAD conductor **0.965414** ≥ 0.95 (`:20006`); control cells **204 977** at relative **0.000e+00** (`:20009`), its ratio **0.893028** (`:20012`), and the two-sided `control < CAD_MASS_GATE <= fixture` asserted with the separation **0.072387** printed — 0.056972 below / 0.015414 above the gate (`:20014–20017`) — reproducing `20260905T183654Z_GEO-25.log:20024–20027` to the digit. Mesh wall times 109.73 s (fixture) / 62.98 s (control) (`:9722, :20000`). Under standing rule (a) the gate module gained one additive `keep_mesh: bool = False` keyword on `_build_rung` (three extra dict keys, populated only when set; its own two tests unchanged) and was re-run green in the same slot — `2 passed in 194.37s`, Status 0, 196 s (`20260906T034048Z_EX-51-gate-rerun.log:54, 60–61`). Census `dead=0 guide=0 stale=77 exit=2`, 44 → **45** examples (`20260906T034041Z_EX-51-postcensus.log:114, 116`). No band moved, no `src/` change, no solve, no Phase 6 cost claim. | ✅ 
