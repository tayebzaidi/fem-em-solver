# TH-19 — Carry the matched source projection to the coil, and retest the production element order

 **Carry the matched source projection to the coil, and retest the production element order** — *opened 2026-09-10, operator direction.* **The decision this reopens.** §10's `TH-12` clause (2026-08-23) pins **degree 1** for every coil-fed Phase-5/6 solve. It was never an accuracy judgement — degree 2 wins outright on the sphere, 0.1405 % against 3.643 % at 3.01× fewer cells — it was an identity failure: on the coil the complex-power identity `Im Z = 4ω(W_m − W_e)/I′²` read **4.5931e-09 loaded / 3.0030e-09 free against a 1e-9 family bound**, while the degree-1 rows of the *same run, same mesh, same process* sat at 8.07e-15. The known-issues entry names the mechanism: `W_m` is unmoved (3.04e-08 → 3.13e-08 J) while `W_e` explodes **3.5e7×** (2.03e-13 → 7.16e-06 J), so `Im Z` goes from the physical **+9.02 Ω** to **−2 117 Ω**. The ungauged curl-curl null space is far richer at second order (882 296 vs 162 710 dofs) and the penalty-free formulation lets irrotational content sit in `E`. **The clause is explicitly reversible on evidence, and its condition is already satisfied.** `TH-13` closed the discriminator: the injector is the **degree-1-only** `H¹₀` source projection — a plumbing defect in the excitation, not a property of the element. `TH-13` step 3a then *built the fix* and measured it on a loop fixture: residue **8.1e-17 / 1.8e-16** against ≤ 1e-8, and the gradient share of `W_e` **4.6e-23 / 3.1e-21** against ≤ 1e-6 — six to fourteen orders of margin, with the default path bit-identical on its control. It landed as `project_source="matched"` (`core/time_harmonic.py:50`, which sets the projection degree to the solve's own and pins the exterior to the solve's own Dirichlet set) and **has zero callers in `tests/` or `examples/`**. So the blocker is unqueued work, not an open question. **What is NOT evidence, stated so a later reader does not mistake it:** `ANS-4` steps 2b and 2d ran fifteen degree-2 coil solves on 2026-09-09/10 and passed reciprocity, σ_max and the C4 spreads — but those are the weak gates (§2.2), and that module asserts **no** power identity. Nothing since August has tested the actual objection. One genuinely new datum does favour degree 2: on the *identical* mesh it gave **tighter** C4 spreads than degree 1 (0.0191 / 0.0334 / 0.0241 % against 0.1012 / 0.0916 / 0.0654 %), i.e. it is less sensitive to the sheet-triangulation asymmetry `GEO-31` identified. **Step 1 — reproduce the red first** (standard, `-n 2`, complex): the `TH-12` fixture at degree 2 with `project_source` at its default, asserting the identity is **still** missed and printing `W_e`, `W_m` and `W_e/W_m`. A fix measured against an unreproduced red is worth nothing, and this repo has paid for that lesson. Expected ≈ 4.6e-09 / 3.0e-09; a materially different reading is itself the finding and stops the chunk. **Step 2 — the same fixture, `project_source="matched"`** (standard): the identity asserted at the **imported, unmoved 1e-9**, with `W_e/W_m` printed beside step 1's. Three pre-registered outcomes, none of them decided in-slot: **(a) passes** ⇒ degree 2 is identity-clean on a coil-fed drive and the production-order clause goes back to a **weekly** with evidence — this chunk does **not** change the default; **(b) improves but still misses** ⇒ record the factor; some null-space content is not the source's, and a gauged degree-2 formulation is the next chunk (the branch `TH-12` step 3 called CLASS); **(c) essentially unchanged** ⇒ the source projection is not the mechanism *on a coil*, which contradicts the loop result and makes this a finding about the feed rather than about the element. **Step 3, only on (a)** (heavy): the same identity on the **birdcage** 4-leg fixture at 10 and 128 MHz — the loop is not the production drive, and the clause is about the birdcage. **Negative control, asserted throughout:** with `matched` off the identity must still fail, so the keyword is provably doing the work and a silently ignored flag cannot read as success. **Cost:** `TH-12` step 2 measured degree 2 at 96.8 % of `memory.max` on the 138 k-cell fixture *against the old 64 GiB ceiling*; today's measurement puts degree 2 at 743 k unknowns near 20–40 GiB, so this is ordinary-tier work now and needs no `xl` slot. **Not in scope:** changing the production default (a weekly's), iterative solvers, gauged formulations, and the `ANS-4` comparison. **Re-priced 2026-09-10 18:00 review (steps 1–2 queued as §9 item 3).** (i) The red lives in `tests/validation/test_coil_loading_degree2_pair.py::test_complex_power_identity_holds_at_degree2`, one σ-half per window via `TH12_DEGREE2_HALF`. The 2026-08-18 figures above are superseded by the last observation: **3.8990e-09 loaded / 3.7235e-09 free** on 138 490 cells (`20260901T033434Z_TH-13-step3a3-degree2-loaded.log`, 374 s; `20260901T034059Z_TH-13-step3a3-degree2-free.log`, 405 s; both `-n 8`, `timeout -k 30 600`, cost probe 52.35 GiB projected; `docs/testing/attempts.md:7494–7514`). Step 1's expectation reads against those. (ii) The tier is **heavy at `-n 8`**, not standard at `-n 2`. (iii) The coil drive is a projected volume current (`_solve_projected_at`, `tests/validation/test_coil_loading_larmor_probe.py:173–176`, default projection), so `"matched"` does reach it. **Step 3 as written is void**, though: the birdcage's lumped-sheet drive passes `current_density=None, project_source=False` (`ports/lumped.py:480–483`), so the matched projection never touches it (known-issues degree-2 entry, `TH-13` step-3a row). Outcome (a) therefore goes to the weekly with that gap named, and a birdcage version needs a sheet-drive formulation ruling first. **Steps 1–2 EXECUTED 2026-09-10, 22:30 implementer slot — OUTCOME (a) ON BOTH σ-HALVES: with `project_source="matched"` the degree-2 coil identity holds at the imported, unmoved 1e-9, by five orders.** Test-side only, `git diff -- src/` empty: additive `project_source=True` on `_solve_projected_at` (forwarded to `solver.solve`; every other caller unchanged), and env `TH19_PROJECT_SOURCE` in `test_coil_loading_degree2_pair.py` (unset ⇒ `True`, `matched` ⇒ `"matched"`, anything else raises; degree-2 half only — the degree-1 control row always runs the default), printing the choice and `W_e`, `W_m`, `W_e/W_m`. `IDENTITY_TOLERANCE` not touched. All three windows `-n 8`, `timeout -k 30 600`, complex, `FEM_EM_REQUIRE_COMPLEX=1`, `tests/environment` first, `-s`, §5.1 durable capture (`[capture] rc=` in the raw file); collect-only smoke `20260911T033155Z_TH-19-step1-collect.log` (20 collected, Status 0, 2 s). **Window 1 — step 1, loaded, env unset** `20260911T033210Z_TH-19-step1.log`: **1 failed / 17 passed / 2 skipped, Status 1, 475 s** (the 17 are 11 `tests/environment` + the 6 the record passed). The red is reproduced: `test_complex_power_identity_holds_at_degree2` at relative **2.3898e-09** vs 1e-9 (`:538`); `Im Z` −2.323123e+03 Ω both routes, `W_e` **7.859344e-06 J**, `W_m` **3.135703e-08 J**, `W_e/W_m` **2.506405e+02** (`:436–437`) — `Im Z`, `W_e`, `W_m` equal the 2026-09-01 record to every printed digit, while the residual itself moved 3.8990e-09 → 2.3898e-09, which is the cancellation round-off the item said not to assert (anomaly recorded, not a different reading of the physics). Degree-2 solve 396.2 s, summed peak RSS 59.20 GiB (`:324`). **Window 2 — matched, loaded** `20260911T034033Z_TH-19-step2-loaded.log`: **18 passed / 2 skipped, Status 0, 484 s**. Identity residual **2.0421e-14** (`:398`), `Im Z` **+9.307547 Ω** both routes (degree-1 default: +9.017628 Ω); `W_e` **2.043905e-13 J**, `W_m` **3.135703e-08 J** (unmoved to 7 digits), `W_e/W_m` **6.518171e-06** (`:399`) — `W_e` falls **3.845e7×**, back to the degree-1 default's 2.03e-13 J scale; residual improves 1.17e5× over window 1. `P_loss` +1.3543067e-01 W vs +1.3543068e-01 W default. Solve 399.9 s, 60.10 GiB (`:482`). **Window 3 — matched, free** (window 2's 484 s was under the 560 s skip rule) `20260911T034858Z_TH-19-step2-free.log`: **18 passed / 2 skipped, Status 0, 483 s**. Residual **5.9380e-15** (`:420`), `Im Z` **+9.871903 Ω** (degree-1: +9.584200 Ω); `W_e` **3.405566e-13 J**, `W_m` **3.325847e-08 J**, `W_e/W_m` **1.023970e-05** (`:421`) against the default-path free record `W_e` 7.8594e-06 J / `W_m` 3.3258e-08 J / residual 3.7235e-09 (`20260901T034059Z_TH-13-step3a3-degree2-free.log:401`; free default **not** re-run this slot) — `W_e` falls 2.308e7×. `P_loss` exactly +0.0000000e+00 W (`:446`). Solve 405.3 s, 59.51 GiB (`:324`). **Degree-1 control, all three windows:** 138 490 cells; ΔR deviation **+1.5838%** vs record +1.5834% → +0.00039 pp against the 0.01 pp floor (`:337` / `:327` / `:337`); degree-1 identity residuals loaded / free 8.0765e-15 / 7.4137e-16, 4.9247e-15 / 8.8964e-15, 1.2607e-14 / 3.5215e-15. **Negative control:** matched off (window 1) the identity fails at the same fixture, same code, same rank count, so the keyword is provably doing the work. **Printed, asserted nowhere, cross-window:** matched degree-2 `ΔX` = 9.307547 − 9.871903 = −0.5644 Ω (degree-1 −0.5666 Ω). **Disposition, not decided in-slot:** pre-registered (a) — refer to the weekly with no default change, and with step 3's named gap (the birdcage's lumped-sheet drive bypasses the projection, `ports/lumped.py:480–483`). | ⬜

**Step 3 EXECUTED 2026-09-13, 13:30 implementer slot (§9 item 3, re-scoped by the 09-13 weekly) — BOTH DEGREE-2 POWER IDENTITIES HOLD ON THE SHEET-DRIVEN 4-LEG BIRDCAGE AT 10 AND 128 MHz; no `W_e` excess on this drive.** Tests only, `git diff -- src/` empty; new module `tests/validation/test_birdcage_power_identity_degree2.py`: `build_four_port_sweep(frequency_hz, build_only=True)` (116 085 cells, asserted `== STEP2_CELL_COUNT`), P1 driven through `run_lumped_sheet_port_case(…, degree=1|2, return_fields=True)` — the sheet drive's projection bypass left untouched, nothing routed through the injector; one frequency per process via `TH19_STEP3_FREQ_MHZ` (no default). Bands imported, never restated: (a) `DISCRETE_IDENTITY_RTOL` 1e-6 and `STEP2D_10MHZ_RTOL` 1e-5 from `test_birdcage_power_identity.py`, terms via its `_exact_shares`; (b) `IDENTITY_TOLERANCE` 1e-9 from its defining module `test_coil_loading_larmor_probe.py:124` (the degree-2 pair module re-imports it, `:82`), `Im S_src = Re L(E)/(2ωμ₀)` against `2ω(W_m − W_e)` with the family's `stored_electric_energy` / `_stored_magnetic_energy` (derivation in the module docstring: μᵣ = 1, real 50 Ω sheets store no reactive energy, the solver adds no gauge term). Windows `-n 8`, complex, `FEM_EM_REQUIRE_COMPLEX=1`, `tests/environment` first, `-v -s`, `timeout -k 30 590`, §5.1 durable capture: collect `20260913T183419Z_TH-19-step3-collect.log` (4 collected, Status 0, 3 s); **10 MHz** `20260913T183446Z_TH-19-step3-10MHz.log` **15 passed, Status 0, 134 s** (`[capture] rc=0` `:2506`); **128 MHz** `20260913T183723Z_TH-19-step3-128MHz.log` **15 passed, Status 0, 118 s** (`:2500`). **Readings (`:1946–1955` in each):** degree 2 (a) **4.575e-15** / **8.928e-15** (10 / 128 MHz; degree 1 9.656e-16 / 3.527e-16); (b) **6.789e-11** / **1.816e-12** (degree 1 2.975e-12 / 4.734e-14). `W_e/W_m` degree 1 → 2: **2.343550e-02 → 2.228745e-02** at 10 MHz, **1.209660e-01 → 1.174350e-01** at 128 MHz; `W_e(2)/W_e(1)` 0.9213 / 0.9305, `W_m(2)/W_m(1)` 0.9688 / 0.9585 — against the coil default path's 3.8e7× `W_e` rise. 139 140 → 745 938 DOFs; P1 solve 3.4 → 68.0 s (10 MHz), 3.2 → 57.4 s (128 MHz); peak `ru_maxrss` 2.57 GiB/rank, 17.86 GiB summed (10 MHz degree 2, `:1954`). **Negative control (asserted, by record, 10 MHz):** degree 1 reproduces `PORT-16` step 1's P1 `P_src,exact` 3.143759587e-03 W, `P_vol` 4.482780406e-04 W, `P_sheet,exact` 2.695481546e-03 W (`20260907T051231Z_PORT-16.log:1882`) at rel ≤ 1e-10 against 1e-5 (`:1960–1964`), record width `-n 2` vs this `-n 8` (integrals, not `OPS-41`'s point-sampled `V`). At 128 MHz no record exists; the degree-1 control asserts both identities at their bands instead (an executor addition, disclosed). **Caveats for the review:** (b)'s 10 MHz degree-2 margin is 15× and is cancellation round-off of the class step 1 saw move between runs — a second observation before it is leaned on; the `TH-13` step-3a known-issues expectation that the coil's `W_e` excess would reappear on the sheets as their own discrete divergence is **not** borne out on this drive. **Disposition, not decided in-slot:** both identities green ⇒ the production-order decision goes to the 09-16 weekly with both fixtures identity-clean (coil under `matched`, birdcage sheet drive as-is); no default change; `TH-12` step 3 (gauged degree 2) not opened. 
