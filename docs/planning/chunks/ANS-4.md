# ANS-4 — Gapped four-leg birdcage, phantom-loaded, four lumped ports: 4×4 S-matrix at 10 / 64 / 128 MHz

 **Gapped four-leg birdcage, phantom-loaded, four lumped ports: 4×4 S-matrix at 10 / 64 / 128 MHz — runnable half** (commissioned 2026-08-30 02:15 weekly review; authoritative spec `examples/ansys_benchmarks/ans4_birdcage_four_port_10_64_128MHz/SPEC.md`; the first independent absolute check of the coil-fed port model at a Larmor frequency) | ✅ **runnable half 2026-08-30** (`20260830T213415Z_ANS-4-run1.log`, Status 0, 125 s at `-n 2`) — all three gates on all three rungs of one 116 085-cell mesh (ratio 1.000000, `reused_mesh`); 10 MHz reproduces leg (d)'s 4×4 to **1.158e-10** (band 1e-6) and leg (d0)'s column to **2.568e-10** (band 1e-9); 64 / 128 MHz reproduce `PORT-11` steps 2/3 to worst **1.075e-03** / **6.755e-04** (band 1e-2); control separation **1.585460** vs 2e-3. **AED half replicated 2026-09-04 by the operator** — HFSS 2026 R1 Driven Modal via PyAEDT (untracked `aed/` script), lumped ports on the gap sheets, one adaptive setup per frequency, both `ANS-5` orders. Qualitative record (numbers private, `aed_results/` + `COMPARISON_private.md` + `docs/private/ans4-operator-notes-2026-09-04.md`): (a) Zero Order **stopped at its pass cap above the 0.002 ΔS target on all three frequencies** — a lumped-port re-triangulation plateau, not divergence — while First Order converged in a few passes on a far smaller mesh; (b) HFSS's matrix sizes **confirm the 6 / 20 unknowns-per-tet correspondence** (`ANS-5`'s open question, closed — README updated); (c) the `Z₁₁` rows are **different definitions** — AED's `Z(P1,P1)` is the open-circuit Z-matrix entry, ours the terminated-drive quantity — so the review compares `Z_in` derived from `S` instead; (d) HFSS warned that the port touches no conductor because 800 S/m is below its conductor threshold (contact verified in the GUI; expected, and one more reason for `ANS-6`). Private-mode run `20260905T041254Z_ANS-4-private.log`, Status 0, 128 s at `-n 2`, tracked table blank, private table filled. **Adjudicated 2026-09-06 (weekly review, run interactively; numeric ruling in gitignored `docs/private/ans4-adjudication-2026-09-06.md`): AGREE at 10 MHz — couplings, self term and the S-derived terminated-drive `Z_in` all agree to the few-percent level AED's own converged order pair brackets, so the 4-leg port model is not wrong by a constant factor; INCONCLUSIVE at 64 / 128 MHz — a miss that grows with frequency and is several times larger than AED's Zero-vs-First-Order shift, so element order is excluded as the cause; our fixed, unconverged 116 085-cell mesh is the first suspect (the sign is consistent with AED resolving more conductor loss in the 800 S/m legs), a Larmor feed-model systematic the second; no disagreement banked, no promotion at Larmor (known-issues entry 2026-09-06).** **Step 2 — the discriminator, `xl` (commissioned 2026-09-06 weekly, the first XL slot, §5.1):** on `_four_port_rung` at 128 MHz only, one harness window against `fem-em-solver-xl` at `-n 16`: degree-1 rungs at `conductor_resolution` ×1 (the record), ×0.75, ×0.6, ×0.45 (cells ≈ 116 k / 162 k / 210 k / ≈ 330 k by `PORT-14` step 1b's measured 116 085 / 161 695 / 209 604), plus one degree-2 solve on the ×1 mesh (priced by `TH-12` step 2 at 61.94 GiB on a 138 k-cell fixture — inside 512 GiB; nothing else in the window exceeds 15 GiB). Public readout, pre-registered: the three C4 class entries `S₁₁`/`S₂₁`/`S₃₁` at 128 MHz on every rung, printed with their relative move from the ×1 record, the C4 spread and σ_max on every rung asserted at the imported `PORT-11` bands (unmoved), and a Richardson h→0 estimate of the three classes from the three finest degree-1 rungs printed. Private readout (the ruling, `docs/private/`): the h→0 estimate and the degree-2 value against the AED First Order column. Decision rule, written before the run: closes ≥ 50 % of the private gap → discretisation, the Larmor verdict becomes AGREE-pending-convergence and the F-small production mesh is re-priced (a `GEO` chunk); < 25 % → feed model, a birdcage-sheet Larmor systematics chunk opens (`PORT-1`'s ladder pattern); between → one more rung in the next XL slot. Cost estimate from priced rungs: `PORT-13` step 1 measured 27.96 s/solve on 270 k cells at `-n 8`, so four degree-1 rungs ≈ 4 × (mesh ≤ 112 s + 4 drives × ≤ 30 s) ≈ 15 min; the degree-2 solve is the unknown and is why this is an XL item — stop rule 7 200 s with `timeout -k 60`. Done-when: the window footered Status 0, the ledger row filled, all pre-registered prints present, no band moved, the public §7 cell carries the relative moves and the verdict word only. **Step 2 SPLIT 2026-09-09 (02:15 weekly review, §5.1 step 3b) — the readout and the decision rule above are unchanged and are not restated or loosened by the split.** Priced from our own smaller rungs, the four degree-1 rungs are **not XL work** (≈ 21 GiB at ×0.45 by `TH-12`'s own `p = 1.271` floor exponent; four drives ≈ 36 s at ×1 scaling superlinearly to ≈ 230 s at ×0.45) and become **step 2a** — two ordinary heavy windows at `-n 4`, headless, implementer-runnable, **queued as §9 item 1 by the 03:00 daily review**; only the single degree-2 solve on the ×1 mesh is genuinely XL on memory before time (≳ 49 GiB, ≈ 1 100–1 300 s for four drives) and becomes **step 2b**, this week's XL slot, held for an operator-interactive session because a 1 100 s window cannot fit a 660 s foreground slot (§9 item 6). 2b must pre-register `TH-12` step 2's degree-2 complex-power identity red as **expected, COIL-SPECIFIC and common-mode — the band is not to be loosened**. **The measurement module landed on `main` 2026-09-09 at `d6cd0fb`** (`tests/validation/test_ans4_resolution_ladder.py`, plus additive `conductor_resolution` / `degree` keywords on `_build` and `_four_port_rung`), written by an interactive session and **committed unexecuted** by the 03:00 daily review's step-2 tree disposition — no harness log exists for it and nothing it contains has closed anything. Known trap for 2a, named by that review: the module builds the degree-2 solve **unconditionally whenever the ×1 factor is in the ladder**, and `FEM_EM_ANS4_STEP2_RUNGS` does not suppress it — 2a's first edit is a second additive knob, `FEM_EM_ANS4_STEP2_DEGREE2`. Honest caveat the weekly recorded: splitting lands the Richardson estimate *before* the degree-2 control, so if 2a alone closes ≥ 50% of the private gap the Larmor verdict is decidable without 2b and the 2026-09-13 weekly may re-commission the slot. **Step 2a EXECUTED 2026-09-09, 04:30 implementer slot — NEGATIVE RESULT, the ladder does not exist as specified.** Standing-rule-(c) re-run first and green: `20260909T093128Z_ANS-4-step2a.log`, `-n 2`, complex build, `tests/environment` + `tests/mesh/test_birdcage_leg_offset.py` + `tests/validation/test_port_birdcage_leg_offset_sweep.py`, **22 passed in 202.74 s**, `Status: 0`, elapsed **205 s** — `d6cd0fb`'s additive `conductor_resolution` / `degree` keywords are behaviour-preserving at their defaults **by measurement**, not only by inspection. The one edit landed: `FEM_EM_ANS4_STEP2_DEGREE2` (default on, `0` for the degree-1 windows), test module only. Collect-only smoke `20260909T093524Z_ANS-4-step2a.log`, 4 tests, `Status: 0`, 3 s. **Window 1** (`FEM_EM_ANS4_STEP2_RUNGS="1.0 0.75"`, `-n 4`, `timeout -k 30 560`): `20260909T093534Z_ANS-4-step2a.log`, **1 failed / 14 passed in 130.88 s**, `Status: 1`, elapsed **132 s**. Anchor (i) green — ×1 meshed **116 085** cells, ratio **1.000000** inside 2e-2. The refinement negative control green — ×0.75 meshed **161 695** cells, **+39.3%**, reproducing `PORT-14` step 1b to the digit, so the keyword refines. (ii) green on both rungs: reciprocity **9.194053e-16** / **2.271834e-15** against 1e-3. (iii) green on both: σ_max **0.998974779044** / **0.998823907170**. **(iv) FAILS on ×0.75**: `Z` class spreads move from **0.1012 / 0.0916 / 0.0654%** at ×1 to **0.5390 / 0.4591 / 1.6886%**, against the imported, unmoved `ADJACENT_SPREAD_BAND` = 0.5% — refining `conductor_resolution` **breaks the fixture's C4 symmetry**, which is the `GEO-26` step 2 class of defect (a non-`C_N`-covariant refinement), not a broken solve (reciprocity and passivity are untouched at 1e-15 / 0.9988). Per the item's own negative-result clause the slot **stopped**: the second window (`"0.6 0.45"`) was not run, nothing was fixed in-slot, no band moved. Printed readout for the record (asserted nowhere), 128 MHz: ×1 `S₁₁ = +4.753451182e-01 +5.808054918e-01j`, `S₂₁ = +2.276311790e-01 −2.671367065e-01j`, `S₃₁ = +5.270667676e-02 −2.216416825e-01j`; ×0.75 `S₁₁ = +4.794845596e-01 +5.733351073e-01j` (move from ×1 **1.1379%**), `S₂₁ = +2.257202657e-01 −2.647272728e-01j` (**0.8762%**), `S₃₁ = +5.318210974e-02 −2.178860589e-01j` (**1.6616%**); S-class spreads at ×0.75 0.4433 / 0.0549 / **2.0731%**. Richardson not attempted (needs three rungs finer than ×1; one exists). Costs measured: mesh 21.6 / 29.9 s, four drives 18.5 / 28.6 s at `-n 4`. **Consequence: `ANS-4`'s Larmor verdict stays INCONCLUSIVE and is not decidable from a one-point ladder; the single ×0.75 move of 0.9–1.7% is a reading, not a convergence estimate.** Known-issues entry 2026-09-09; the discriminator the next review must commission is per-leg congruence (cell counts / gap-sheet areas per leg at ×1 and ×0.75, **no solve**) before any further rung is priced, and 2b's degree-2 control now rides on a fixture whose C4 symmetry is only demonstrated at ×1. **Step 2b EXECUTED 2026-09-09 by an operator-interactive session — the degree-2 solve landed, and it should not have been an XL item.** One window against `fem-em-solver-xl`, `-n 16`, `timeout -k 60 7200`, complex build: `20260909T143756Z_ANS-4-step2b.log`, **14 passed / 1 skipped**, `Status: 0`, elapsed **249 s** (ladder 215.5 s wall; ×1 degree-1 mesh 22.5 s + four drives 11.9 s; the degree-2 four drives on the *reused* ×1 mesh 178.4 s). The skip is the refinement control, correctly inapplicable to a one-rung ladder. All imported bands green on **both** rungs and none moved: reciprocity 1.532231803e-14 (degree 1) / 1.067096809e-13 (degree 2) vs 1e-3; σ_max 0.998974779044 / 0.998625576225; `Z` class spreads 0.1012 / 0.0916 / 0.0654 % vs **0.0191 / 0.0334 / 0.0241 %** against the unmoved 0.5 % — **degree 2 is the more C4-symmetric solve on the identical mesh**, the opposite of what `conductor_resolution` refinement did to the same fixture in step 2a. Readout, asserted nowhere: degree 2 moves the three C4 classes off the degree-1 record by **S₁₁ 6.0900 %, S₂₁ 5.3841 %, S₃₁ 6.6959 %** (`S₁₁ = +5.154873290e-01 +5.589486684e-01j`, `S₂₁ = +2.122248726e-01 −2.561950360e-01j`, `S₃₁ = +4.558023712e-02 −2.081539784e-01j`). Richardson not attempted and printed as such — it needs three rungs finer than ×1 and step 2a's ladder does not exist. The pre-registered `TH-12` degree-2 complex-power red did not surface: this module asserts no power identity. No band was loosened anywhere. **Tier finding, for the 2026-09-13 weekly and against §5.1: this run was mis-tiered.** The item priced it at ≳ 49 GiB and 1 100–1 300 s and called it "well outside the ordinary service's 128 G"; it measured **249 s** and a `docker stats` reading of **~16.6 GiB** mid-solve (no in-run peak instrumentation — see the ledger note). 49 GiB was never outside 128 G, and the constraint that actually forced an interactive session was the scheduled slot's 660 s **tool** cap, which the XL *service* does not address. §5.1 reserves the tier for "the convergence rung the heavy tier cannot hold" and reserved the first slot for the **refinement** rung — the high-tet-count case — so the week's slot went to the smallest fixture in the repo. The ledger row stands: §5.1's own rule is that a started run has spent the week. What the weekly owes is a genuine XL commissioning (`TH-11` step 5's case, measured OOM at 2.81 M cells, is the standing candidate) and a rubric line requiring a **measured** memory price, not an extrapolated one, before an item may be marked `xl`. The Larmor verdict is unchanged and stays a review's — the comparison against the AED column lives only in gitignored `docs/private/`. **Step 2c COMMISSIONED 2026-09-09 (operator directive, interactive) — the refinement rung the XL tier was built for, on the knob step 2a did not turn.** Step 2a refined `conductor_resolution` and **broke the fixture's C4 symmetry** (0.1012 / 0.0916 / 0.0654 % → 0.5390 / 0.4591 / **1.6886 %** at ×0.75 against an imported 0.5 % band that is not to be widened), so that ladder does not exist and no tet count can be bought along it. 2c turns the **global** `resolution` instead — the one knob this 116 085-cell fixture has never been refined in (1.5 cm air cells inside a 7 cm coil) — and it is a different knob for a measured reason: `GEO-28`'s quadrant census reads the mesh symmetric to ≤ 0.1 % in mass, and `GEO-29` already priced this ladder's **mesh** side on this very fixture (116 085 / 149 049 / 197 393 / 281 728 cells at 0.015 / 0.012 / 0.0095 / 0.0075, no solve, cells ∝ h^−1.28). The **solve** side is unmeasured, so under §5.1's own rule the first window is a **cost probe** and says so. **Pre-registered:** one window against `fem-em-solver-xl`, `-n 16`, `timeout -k 60 7200`, complex, `FEM_EM_ANS4_STEP2_RUNGSPEC="0.015:1 0.015:2 0.0075:2 0.005:2"` — explicit `h:degree` rungs, coarse to fine, **approximating the AED design rather than only out-meshing our own** (operator direction 2026-09-09). The correction that sets the target: by `ANS-5` our degree 2 **is** HFSS First Order, and AED's First Order run is its *converged* one — it needed only **0.49–0.60 M tets** (3.1–3.8 M unknowns) where its Zero Order run took 1.28–1.53 M, because order buys accuracy per tet. So the rung that matches AED is **degree 2 at ≈ 0.5 M cells** (≈ 3.0 M unknowns), and the earlier plan to push degree 1 to AED's *Zero Order* 1.5 M tets was matching the wrong column. Degree 2 on 1.5 M cells is ≈ 9.8 M unknowns and ≈ 1.1 TB by the `TH-11` step 5d measurement — past this tier and past anything Ansys ran — so it is explicitly **not** attempted. the first rung the **control** that must reproduce **116 085** cells inside the imported `STEP2_CELL_COUNT_BAND`. Predicted from `GEO-29`'s exponent and to be verified, not assumed: 116 085 cells at degree 1 (the control, an exact record) and 116 085 / ≈ 282 k / **≈ 474 k** at degree 2 → ≈ 0.14 M / 0.74 M / 1.80 M / **3.03 M** unknowns. The last rung sits inside AED First Order's own **3.1–3.8 M** unknown count on a comparable tet count — the closest like-for-like this fixture can be run at. Memory, extrapolated from `TH-11` step 5d's *measured* 260.9 GiB at 3.37 M unknowns: ≈ 40 / 112 / **226 GiB** — **far over the ordinary service's 128 G and inside 512 G, which is exactly what the tier is for**. The h=0.015 degree-2 rung is a free cross-check: it must reproduce step 2b's already-recorded degree-2 entries, proving the ladder is the same construction before any conclusion is drawn from the finer ones. Stop rule *after each rung*, so a wall costs the next rung and not the ones already measured: > 4 500 000 **unknowns** (≈ 400 GiB, so the factorisation may overshoot the extrapolation and still fit) or > 5 400 s cumulative ends the ladder with the rungs above standing. **Asserted, every band imported and unmoved:** the control's cell record, each finer rung strictly refining (`PORT-14` step 1b's mechanical control — a keyword that silently did nothing would read as a converged ladder), and on **every** rung reciprocity, σ_max and the C4 class spreads at `PORT-11`'s bands. **Printed, gated by nothing:** the three C4 classes per rung with their move from the control, and the Richardson h → 0 estimate from the three degree-2 rungs — an h-ladder at the **matched order**, which is what an order-matched comparison with AED actually needs. **A C4 break does not cost the readout.** The class-spread assertion and the printed comparison live in *separate* test functions, so a failing gate on some rung still leaves every rung's `S₁₁`/`S₂₁`/`S₃₁` and the Richardson estimate in the log — the run answers the tet-count question either way, and the gate failure is recorded beside it rather than instead of it. **Pre-registered negative branch:** if global refinement *also* breaks the C4 spreads, that is a **finding about the fixture, not about the band** — it lands as 2a did, the band is not widened, and the consequence is that no `h`-ladder on this fixture can feed the Larmor verdict, which would make the feed model the surviving suspect by elimination. **Code:** the module and both additive keywords are on `main`; 2c added a third, `resolution`, on the same precedent, plus the per-rung stop rule (`FEM_EM_ANS4_STEP2_RESOLUTION`). **Who runs it:** an interactive operator session, not a slot — today established that no scheduled slot can run an XL window to a footer (§5.1, `OPS-43`), and it must use the durable-capture shape §5.1 now requires. **Deadline: within 24 h of 2026-09-09 15:50Z, i.e. by 2026-09-10 15:50Z**, which is inside `scripts/automation/xl-override.env`'s window; after that the interval rule returns and this waits for the 2026-09-13 weekly. The service must be **restarted immediately before the window** so `memory.peak` is attributable to this run (§5.1). It runs after `TH-11` step 5d frees the service — two XL solves cannot share 512 GiB. **Step 2d EXECUTED 2026-09-10 (operator-interactive, `xl`) — the matched-order comparison lands, and it moves the Larmor verdict.** `20260910T152049Z_ANS-4-step2d.log`, its wrapper killed early so it carries **no `## Exit` footer and is uncountable on its own**; the run wrote to a gitignored container-side file first (§5.1 durable capture) and the full output is recovered, footered, in `20260910T180911Z_ANS-4-step2d-capture.log` — **cite that one**. **14 passed / 1 failed in 7225.01 s (2:00:25)** at `-n 16`, peak **290.2 GiB** (clean: service restarted immediately before the window). Ladder, coarse → fine: h=0.015 degree 1 (116 085 cells, 0.14 M unknowns, four drives 12.0 s), then degree 2 at h=0.015 / 0.0075 / 0.005 (116 085 / 281 728 / **592 744** cells, 0.74 / 1.80 / **3.79 M** unknowns, four drives 169.0 / 1119.2 / **5653.5 s**). **The public readings** (asserted nowhere, per the pre-registration): the three C4 classes of `S` at 128 MHz move from the degree-1 control by **6.09 / 5.38 / 6.70 %** at degree 2 on the *same* mesh, and then barely move again under `h` — **5.83 / 4.82 / 6.71 %** and **5.72 / 4.50 / 6.72 %**. So the order is the lever and the mesh is nearly spent: the degree-2 sequence converges, successive change **1.19 % → 0.62 %** on `S₁₁` (ratio 1.90), 1.21 → 0.62 (1.94) on `S₂₁`, 0.75 → 0.50 (1.51) on `S₃₁`. Degree 2 also gave **tighter** C4 spreads than degree 1 on the identical mesh at the two coarser rungs (0.0150–0.0422 % against 0.0305–0.0836 %), consistent with `GEO-31`'s finding that the sheet triangulation, not the physics, sets that floor; the finest rung's spreads rise again (0.0819–0.1423 %), which is worth a look and is **not** explained here. Every imported gate passed on every rung — reciprocity, σ_max, class spreads — bands unmoved. **The one failure is a defect in the measurement module, not in the physics:** the refinement control compared two rungs that share one mesh (h=0.015 at degree 1 and degree 2) and asserted `116085 > 116085`. Fixed the same day — refinement is a property of a mesh, so the control now compares distinct meshes — and the fix is verified against the exact rung list that failed. **The verdict this changes, and the numeric ruling is a review's, in gitignored `docs/private/ans4-step2d-2026-09-10.md`:** the 2026-09-06 adjudication reasoned that element order was *excluded* as the cause because AED's own Zero-to-First-Order shift was small. That inference does not hold — AED's answer was order-insensitive because its First Order run was already converged, while ours was not, and the asymmetry was the signal rather than an exclusion. At **matched order and comparable unknown count** our finest rung now agrees with the AED column far more closely than the degree-1 comparison did *(the percentages were redacted 2026-09-10 by the 18:00 daily review: an ours-vs-AED gap is a private benchmark number, CLAUDE.md hard rules)*. **AGREE at 64 / 128 MHz is now the reading the evidence supports**, but the 2026-09-13 weekly owns whether it is banked, and §2.2 is unchanged here. Consequence beyond `ANS-4`: this is direct evidence for `TH-19` (the production element order), whose objection to degree 2 on a coil remains untested — this run asserts no power identity. **Step 2a′ EXECUTED 2026-09-10, 19:30 implementer slot — the congruent cut carries step 2a's ×0.75 `Z` class-spread break too; with `GEO-32`'s flag on, both rungs pass every imported gate.** Test-side only, no `src/`: additive `c4_congruent_sheets=False` on `_build` and `_four_port_rung` (ignored under `reuse`), env `FEM_EM_ANS4_STEP2_C4_CONGRUENT` (unset/`0` = off) threaded to every rung and echoed in each rung label, and `OPS-43` (c)'s `report_peak_rss` after each rung on all ranks. Both windows `-n 4`, `timeout -k 30 560`, `-s`, complex, `FEM_EM_ANS4_STEP2_RUNGS="1.0 0.75"`, `FEM_EM_ANS4_STEP2_DEGREE2=0`. **Window 1, flag on** `20260911T003204Z_ANS-4-step2a-prime.log`: **15 passed, Status 0, 147 s**. `size_global` **116 118 / 161 645** (`:2031, :3898`, both as `GEO-32` predicted; control ratio 1.000284 inside the imported band, `:3901`). `Z` class spreads self / adjacent / opposite ×1 **0.0739 / 0.0822 / 0.0506 %** (`:3911`), ×0.75 **0.0481 / 0.0649 / 0.0555 %** (`:3913`) against the unmoved 0.5 %. Reciprocity 9.73e-16 / 2.37e-15, σ_max 0.998977574510 / 0.998688470520. S-class spreads at ×0.75 0.0395 / 0.0573 / 0.0267 % (were 0.4433 / 0.0549 / 2.0731 % flag off). Printed, asserted nowhere: ×0.75 moves `S₁₁/S₂₁/S₃₁` off the flag-on ×1 rung by 1.3786 / 1.3308 / 0.8225 % (`:3924–3926`). **Window 2, flag off (negative control by reproduction)** `20260911T003442Z_ANS-4-step2a-prime.log`: **1 failed / 14 passed, Status 1, 137 s**. It fails the same assertion as step 2a (`:3718`, self 0.5390 %), with 116 085 / 161 695 cells and spreads 0.1012 / 0.0916 / 0.0654 % and 0.5390 / 0.4591 / 1.6886 % (`:3693–3694`). The six `S` entries and both σ_max match `20260909T093534Z` to every printed digit; only reciprocity moved at round-off (9.19e-16 → 1.01e-15). `[mem]` lines printed on all four rungs (on: 2.4269 / 2.9917 GiB summed over 4 ranks, `:2030, :3897`; off: 2.4599 / 3.0097 GiB, `:1916, :3678`). **Reading:** the ×0.75 break was the port-sheet cut, not the conductor refinement, so the `conductor_resolution` ladder exists **with the flag on**. Per the item's green branch the ladder was **not** extended in-slot; ×0.6 / ×0.45 flag-on is the review's to queue. A flag-on mesh is not the `GEO-19` record mesh, so its moves are measured against the flag-on ×1 rung. No band moved and the default stays off. The Larmor verdict is untouched (step 2d's, the 2026-09-13 weekly's). **Step 2a″ EXECUTED 2026-09-11, 04:30 implementer slot — guard landed; the flag-on `conductor_resolution` ladder is green to ×0.45 and the Richardson fit lands inside its bracket.** Test-side only, no `src/`. **Edit 1 (the guard):** `_require_explicit_c4_flag(factors)` runs at the top of the `ladder` fixture, before any mesh. It raises `ValueError` naming the known-issues 2026-09-09 `ANS-4` entry and both remedies (`=1` measure, `=0` flag-off control) when `FEM_EM_ANS4_STEP2_C4_CONGRUENT` is unset and any factor ≠ 1.0. It is pure env logic, so every rank raises. `RUNGSPEC`/`RESOLUTION` paths are untouched. **Window 0, guard control (asserted; flag unset, `RUNGS="1.0 0.75"`, `-n 2`, `timeout -k 30 120`, smoke)** `20260911T093156Z_ANS-4-step2a-dprime-w0.log`: all four ladder tests ERROR at setup with the message (`:81–104`), no rung line, `11 passed, 4 errors` (environment only, `:172`), **Status 1, 31 s**. Known-issues entry retired. **Windows 1–2:** flag `=1`, `DEGREE2=0`, `-n 8`, `-s`, complex, durable capture to `logs/ans4-step2a-dprime-w{1,2}-raw.log` (`[capture] rc=0` both). The container timeout was `-k 30 590` rather than the item's 600, to fit the 660 s tool window. **Window 1 (`RUNGS="1.0 0.6"`)** `20260911T093247Z_ANS-4-step2a-dprime-w1.log`: **15 passed, Status 0, 141 s**. `size_global` ×1 **116 118** (control ratio 1.000284, `:4029`) and ×0.6 **209 544** (*predicted* ≈ 210 k from flag-off 209 604; `:2107, :4026`). ×0.6 took mesh 38.5 s + four drives 29.7 s = **68.2 s**, under the 150 s gate for window 2. `Z` spreads ×1 0.0739 / 0.0822 / 0.0506 %, ×0.6 **0.0522 / 0.1007 / 0.0263 %** (`:4048–4049`). Reciprocity 1.84e-14 / 2.64e-14; σ_max 0.998977574510 / 0.998986563062. `[mem]` 3.8586 / 5.0113 GiB summed over 8 ranks (`:2106, :4025`). The ×1 `S` entries equal 2a′'s `-n 4` values to every printed digit (`:4061–4063` vs `20260911T003204Z…:3919–3921`). **Window 2 (`RUNGS="0.75 0.6 0.45"`)** `20260911T093524Z_ANS-4-step2a-dprime-w2.log`: **14 passed, 1 skipped (the record-rung test, no ×1 by design), Status 0, 250 s**; ladder built in 220.0 s (`:5946`). `size_global` **161 645 / 209 544 / 293 534** (`:2060, :3979, :5945`; ×0.45 *predicted* ≈ 330 k). Mesh 29.3 / 38.9 / 54.5 s, four drives 22.9 / 24.7 / 40.1 s. `[mem]` 4.4642 / 5.1066 / **6.0662 GiB** summed over 8 ranks (`:2059, :3978, :5944`). **Negative control (asserted by reproduction):** ×0.75 reproduces 161 645 exactly. Its spreads **0.0481 / 0.0649 / 0.0555 %** and σ_max 0.998688470520 equal 2a′'s to the printed digit (`:5966`, *predicted* equality held). Its `S` entries equal 2a′'s `:3924–3926`, and ×0.6's equal window 1's `:4065–4067`. ×0.45 spreads read **0.0931 / 0.1072 / 0.0861 %**, reciprocity 2.08e-14 and σ_max 0.998876951943 (`:5968`). **Every finer rung passes every imported gate, bands unmoved.** S-class spreads at ×0.45: 0.0757 / 0.1052 / 0.0523 %. **Richardson (printed, asserted nowhere; `:5993–5996`)** on ×0.75 / ×0.6 / ×0.45: fitted `p` = **2.1282 (S₁₁) / 2.6525 (S₂₁) / 2.8791 (S₃₁)**, all inside `RICHARDSON_P_BRACKET` (0.2, 6.0), so the module printed an estimate rather than "not asymptotic". Relative moves, computed from the printed entries: S_inf is **1.03 / 0.63 / 0.58 %** beyond ×0.45 and **3.06 / 2.20 / 2.65 %** from the flag-on ×1 rung. ×0.45 itself sits 2.03 / 1.58 / 2.08 % from ×1, and ×0.75 sits 1.38 / 1.33 / 0.82 %. *Caveat for the review:* this is a three-point fit with no fourth point to test asymptoticity. S₁₁'s move from ×1 is not monotone in magnitude (1.38 % at ×0.75, 1.18 % at ×0.6, 2.03 % at ×0.45; `w1:4065`). The fitted exponents sit above the rate a degree-1 N1curl port quantity is usually expected to converge at. Read the extrapolant as a reading, not a converged value. No AED number is compared here; any gap goes to `docs/private/` only. The Larmor verdict stays with the 2026-09-13 weekly. **Step 2a‴ EXECUTED 2026-09-11, 19:30 implementer slot — negative result: the fourth rung shows step 2a″'s Richardson fit is not asymptotic.** Env only, no code change. **Window (`RUNGS="0.6 0.45 0.35"`, flag `=1`, `DEGREE2=0`, `-n 8`, `-s`, complex, `timeout -k 30 590`, durable capture to `logs/ans4-step2a-tprime-raw.log`, heavy)** `20260912T003101Z_ANS-4-step2a-tprime.log`: **14 passed, 1 skipped (record-rung test, no ×1 by design), Status 0, 247 s**, `[capture] rc=0` (`:6643, :6646–6647`). `size_global` **209 544 / 293 534 / 394 481** (`:2112, :4078, :6071`; ×0.35 *predicted* 380–420 k). Mesh 38.5 / 54.5 / 74.8 s; four drives 7.2 / 10.7 / 18.1 s (faster than 2a″'s 24.7 / 40.1 s — `PORT-19`'s reuse-on default landed in between). `[mem]` 4.6957 / 5.8522 / **7.1558 GiB** summed over 8 ranks (`:2111, :4077, :6070`). **Anchors (asserted):** every finer rung refines (`:6080–6082`); every rung passes the imported `PORT-11` gates, bands unmoved (`:6091–6093`) — ×0.35 reciprocity 9.16e-16, σ_max 0.999071644712, `Z` spreads **0.0510 / 0.0619 / 0.0476 %**. **Negative control (asserted by reproduction):** ×0.6 and ×0.45 reproduce 209 544 and 293 534 exactly. Their `Z` spreads and σ_max equal 2a″'s (`w2:5967–5968`), and their `S` entries equal `w2:5984–5990`, to every printed digit (*predicted* equality held). Reciprocity reads 8.6e-16 / 3.6e-16 against 2a″'s 3.4e-14 / 2.1e-14, both round-off. **Richardson on ×0.6 / ×0.45 / ×0.35 (`:6117–6121`): "no estimate" for S₁₁, S₂₁ and S₃₁.** Slot arithmetic on the printed entries: the step-move ratio `|S(0.35) − S(0.45)| / |S(0.45) − S(0.6)|` = **1.0223 / 1.1494 / 1.1660**. The model ratio on these factors spans only 0.1686 (p = 6) to 0.8277 (p = 0.2), so no exponent in the bracket fits; only a p < 0 would. Relative step moves per class are 1.17 / 1.10 / 1.19 % (×0.75→×0.6), then 0.87 / 0.72 / 0.75 % (→×0.45), then **0.89 / 0.83 / 0.88 %** (→×0.35). The decay stalls after ×0.6. ×0.35 sits **1.38 / 1.17 / 0.68 %** from 2a″'s extrapolant, against ×0.45's 1.03 / 0.64 / 0.59 %, so the ladder moves *away* from it. "Relative move of S_inf between the two fits" is therefore not computable (no second fit). **Reading:** 2a″'s p = 2.13 / 2.65 / 2.88 fit is not asymptotic, and its extrapolant is not a converged value. The degree-1 `conductor_resolution` ladder is outside its asymptotic range to ×0.35 (≈ 394 k cells). Every rung keeps `h_global` = 15 mm and `shell` = 12 mm (`:196, :2115, :4081`), so a floor set by the unrefined bulk is the candidate cause. That is not measured here. No spread broke, no band moved, no AED number is compared, and the Larmor verdict stays with the weekly. **Ruled 2026-09-12 03:00 review:** 2a‴ accepted as a negative result; 2a″'s extrapolant is not an h → 0 reference. **Step 2e scoped (§9 item 1):** an additive `FEM_EM_ANS4_STEP2_CONDUCTOR_FACTOR` env fixes the conductor grading at ×0.45 while the step-2c global ladder runs 0.015 / 0.012 / 0.0095 (predicted ≈ 293.5 k / 326 k / 375 k cells, ≤ 8 GiB at `-n 8`, ≈ 4–5 min), so the bulk-floor hypothesis gets one window before the 2026-09-13 weekly. **Step 2e EXECUTED 2026-09-12, 04:30 implementer slot — green; refining the bulk at fixed ×0.45 conductor grading moves every class by more than 2a‴'s conductor rungs did.** Test-side additive env only, no `src/`: `FEM_EM_ANS4_STEP2_CONDUCTOR_FACTOR` (unset = off, no `conductor_resolution` passed, default labels unchanged) is read inside the 2c loop, and the factor is printed in every rung line. `_require_explicit_c4_flag` also refuses a factor ≠ 1 with the flag unset. `test_the_record_rung_reproduces_the_fixture` skips when the control rung carries a factor ≠ 1, printing its count. Guard smoke `20260912T093219Z_ANS-4-step2e-smoke.log` (Status 0, 4 s): the refusal fires at ×0.45 with the flag unset and passes with flag `=1` or factor 1. **Window (`C4_CONGRUENT=1`, `DEGREE2=0`, `RESOLUTION="0.015 0.012 0.0095"`, `CONDUCTOR_FACTOR=0.45`, `-n 8`, `-s`, complex, `timeout -k 30 590`, durable capture to `logs/ans4-step2e-raw.log`, heavy)** `20260912T093235Z_ANS-4-step2e.log`: **14 passed, 1 skipped (the record test, by design, `:6075`), Status 0, 328 s harness / 325.6 s pytest**, `[capture] rc=0` (`:6653–6654, :6657`). Cells **293 534 / 392 442 / 513 061** (`:2159, :4108, :6073`). The *predicted* 293.5 k / 326 k / 375 k assumed the global increments over ×1 simply add on top of the ×0.45 mesh; they do not, and the two refinements compound. Mesh 54.8 / 71.1 / 91.9 s (under the 150 s trap), four drives 10.8 / 22.1 / 31.6 s, ladder 296 s. `[mem]` 5.7972 / 7.9648 / **10.9388 GiB** summed over 8 ranks (`:2158, :4107, :6072`), above the *predicted* ≤ 8 GiB, which rested on the same wrong cell prediction. **Anchors (asserted, bands unmoved):** every finer rung refines; every rung passes the imported `PORT-11` gates (`:6103, :6104, :6108`). Reciprocity reads 1.23e-15 / 5.84e-16 / 9.02e-16, σ_max 0.998876951943 / 0.999113337649 / 0.999171282230, and the `Z` class spreads (self / adjacent / opposite) are 0.0931/0.1072/0.0861 %, **0.0219/0.0317/0.0509 %** and **0.0177/0.0362/0.0413 %**. Global refinement does not break C4 with the congruent cut. **Negative control (asserted by reproduction): held.** h = 0.015 at ×0.45 meshed 293 534 cells, and its S₁₁ / S₂₁ / S₃₁ (`:6117–6119`) equal `…tprime.log:6109–6111` to every printed digit. Its σ_max and `Z` spreads also equal `…tprime.log:6092`. **Readout (`:6116–6127`, S-class spreads 0.0757/0.1052/0.0523 %, 0.0177/0.0308/0.0402 %, 0.0142/0.0335/0.0323 %).** Relative class moves per global rung, from the printed entries: 0.015→0.012 **1.25 / 0.84 / 1.53 %** (S₁₁/S₂₁/S₃₁), then 0.012→0.0095 **0.62 / 0.48 / 0.81 %**, a step-move ratio of 0.502 / 0.575 / 0.521 (decaying). The total move 0.015→0.0095 is **1.84 / 1.21 / 2.31 %**. For comparison, 2a‴'s conductor-rung moves were 0.87 / 0.72 / 0.75 % (→×0.45) and 0.89 / 0.83 / 0.88 % (→×0.35), whose ratios grew. The < 0.2 % negative-result clause is far from triggered. **Richardson on h = 0.015 / 0.012 / 0.0095 (`:6129–6133`): an estimate for all three classes.** Fitted p = **3.2264 / 2.6300 / 3.0608**, inside (0.2, 6.0), with S_inf **2.38 / 1.71 / 3.06 %** from the h = 0.015 ×0.45 control. **Reading:** the unrefined bulk is a real contributor. Turning `h_global` at fixed conductor grading moves the classes more per rung than conductor grading beyond ×0.6 did, and the moves decay. The ×0.45 flag-on ladder is therefore not converged at 513 k cells. *Caveats:* this is a three-point fit with no fourth point to test asymptoticity, and 2a″'s fit (p ≈ 2–3, also inside the bracket) was disproved by one more rung. The exponents again sit above the rate usually expected for a degree-1 N1curl port quantity. Conductor grading stays at ×0.45 on every rung, so this extrapolant is not an h → 0 value in both knobs. No AED number is compared (any gap goes to `docs/private/` only), no band moved, and the Larmor verdict stays with the 2026-09-13 weekly. **Step 2f EXECUTED 2026-09-12, 12:00 implementer slot — green, and step 2e's p ≈ 3 did not hold: a fourth global rung pulls the exponent into the degree-1 range and moves the extrapolant by up to 1.2 %.** Runs only, no code. **Window (`C4_CONGRUENT=1`, `DEGREE2=0`, `RESOLUTION="0.012 0.0095 0.0075"`, `CONDUCTOR_FACTOR=0.45`, `-n 8`, `-s`, complex + `FEM_EM_REQUIRE_COMPLEX=1`, `tests/environment` first, `timeout -k 30 900`, durable capture to `logs/ans4-step2f-raw.log`, heavy)** `20260912T170105Z_ANS-4-step2f.log`: **14 passed, 1 skipped (the record test, factor ≠ 1, by design), Status 0, 440 s harness / 437.07 s pytest**, `[capture] rc=0` (`:6666–6667, :6670–6671`). Cells **392 442 / 513 061 / 684 301** (`:2142, :4107, :6095`; 0.0075 *predicted* ≈ 650–700 k, held). Mesh 70.7 / 94.9 / **123.6 s**, four drives 17.2 / 28.1 / **54.0 s** (0.0075 *predicted* ≈ 120 + 45 s), ladder 406.1 s (`:6096`). `[mem]` 7.6883 / 10.7309 / **17.0052 GiB** summed over 8 ranks (`:2141, :4106, :6094`; *predicted* 14–15 GiB, exceeded, printed only). **Anchors (asserted, bands unmoved):** every finer rung refines (`:6104–6106`); every rung passes the imported `PORT-11` gates (`:6115–6117`). The 0.0075 rung reads reciprocity 1.12e-15, σ_max 0.999211612914 and `Z` class spreads **0.0087 / 0.0126 / 0.0162 %**, the tightest on this fixture so far. **Negative control (asserted by reproduction): held.** 0.012 and 0.0095 meshed 392 442 and 513 061 cells. Their S₁₁ / S₂₁ / S₃₁ (`:6129–6135`), σ_max and `Z` spreads equal step 2e's (`…step2e.log:6104, :6108, :6121–6127`) to every printed digit; reciprocity moved at round-off (5.84e-16 → 6.50e-16, 9.02e-16 → 9.16e-16). **Readout (`:6136–6139`):** 0.0075 `S₁₁ = +5.057563945e-01 +5.813856095e-01j`, `S₂₁ = +2.173363506e-01 −2.671924378e-01j`, `S₃₁ = +4.480654767e-02 −2.137883514e-01j`, with S-class spreads 0.0070 / 0.0126 / 0.0148 %. Relative class moves per rung, computed in the slot from the printed entries: 0.015 → 0.012 1.2524 / 0.8394 / 1.5295 %, 0.012 → 0.0095 0.6214 / 0.4826 / 0.8064 %, and **0.0095 → 0.0075 0.4460 / 0.3132 / 0.5832 %** (*predicted* ≈ 0.3 / 0.25 / 0.4 % if p ≈ 3). The step-move ratio on the new pair is **0.7209 / 0.6466 / 0.7180**, against 0.5021 / 0.5750 / 0.5214 on step 2e's pair; the model ratio on these sizes is 0.800 at p = 1, 0.633 at p = 2 and 0.500 at p = 3. **Richardson on (0.012, 0.0095, 0.0075) (`:6141–6146`):** fitted p = **1.4430 / 1.9062 / 1.4602**, inside (0.2, 6.0), against step 2e's 3.2264 / 2.6300 / 3.0608. S_inf reads `+5.141479713e-01 +5.821501838e-01j` / `+2.154606953e-01 −2.668923589e-01j` / `+4.297102176e-02 −2.112820244e-01j`. The two extrapolants differ by **|ΔS_inf|/|S_inf| = 0.9895 / 0.2941 / 1.2315 %**. The 0.0075 rung already sits 0.11 / 0.26 / 0.20 % from step 2e's S_inf, and the new S_inf lies 1.09 / 0.55 / 1.42 % beyond it. **Reading:** no pre-registered negative-result bar fires as written: an estimate printed, p stayed in the bracket, and the move shrank. But the step-2e exponent was pre-asymptotic. The decay per rung slowed from ≈ ×0.5 to ≈ ×0.7, the refit exponent fell to the 1.4–1.9 range usually expected of a degree-1 N1curl port quantity, and the 0.0095-based extrapolant undershot by up to 1.2 %. *Caveats:* this is still a three-point fit, and two earlier three-point fits on this fixture (2a″, 2e) were each revised by one more rung. S₂₁'s p (1.91) differs from S₁₁/S₃₁'s (≈ 1.45). Conductor grading is held at ×0.45, so S_inf is a limit in `h_global` only. A fifth rung (h ≈ 0.006) is *predicted* at ≈ 0.9 M cells and ≈ 25 GiB by this ladder's ≈ ×1.33 cells per rung. A (0.0095, 0.0075, 0.006) window is *predicted* at ≈ 560–600 s of ladder at `-n 8`, at the edge of a 660 s foreground slot; pricing it is the review's. No AED number is compared (any gap goes to `docs/private/` only), no band moved, and the Larmor verdict stays with the 2026-09-13 weekly. **Step 2g EXECUTED 2026-09-12, 15:00 implementer slot — green, and the item's interaction clause fires: the global ladder at ×0.35 does not move like the one at ×0.45, most sharply in S₃₁.** Runs only, no code. **Window (`C4_CONGRUENT=1`, `DEGREE2=0`, `RESOLUTION="0.015 0.012 0.0095"`, `CONDUCTOR_FACTOR=0.35`, `-n 8`, `-s`, complex + `FEM_EM_REQUIRE_COMPLEX=1`, `tests/environment` first, `timeout -k 30 600`, durable capture to `logs/ans4-step2g-raw.log`, heavy)** `20260912T200107Z_ANS-4-step2g.log`: **14 passed, 1 skipped (the record test, factor ≠ 1, by design), Status 0, 432 s harness / 430.69 s pytest**, `[capture] rc=0` the last output line (`:6731–6732, :6735–6736`). *Deviation, disclosed:* the item said `timeout -k 30 900`, and 900 + 30 s cannot return a footer inside the 660 s foreground window implementer-run.md requires, so the slot used 600 (step 2f's same-size window measured 437 s). Cells **394 481 / 524 983 / 693 462** (`:6170–6172`; *predicted* ≈ 394 k / 530 k / 690 k, held). Mesh 75.2 / 98.9 / **128.4 s**, four drives 14.7 / 25.5 / **40.0 s** (`:2186, :4159, :6151`). `[mem]` 7.02 / 9.48 / **13.40 GiB** summed (`:2185, :4158, :6150`; *predicted* ≤ 18 GiB, held). **Count and S control (asserted by reproduction): held** — the 0.015 rung meshed 394 481 cells and printed S₁₁ / S₂₁ / S₃₁ equal to `20260912T003101Z_ANS-4-step2a-tprime.log:6113–6115` to every printed digit (`:6195–6197`); σ_max 0.999071644712 and the three `Z` spreads equal `…tprime.log:6093`; reciprocity moved at round-off only (6.62e-16 against 9.16e-16). **Anchors (asserted, bands unmoved):** every finer rung refines (`:6170–6173`); every rung passes the imported `PORT-11` gates (`:6181–6183`) — worst reciprocity 2.29e-15, σ_max ≤ 0.999071644712, worst `Z` spread 0.0619 %. **Readings** (moves computed in the slot as |S_b − S_a|/|S_a| from the printed digits, a formula that reproduces the fixture's "move from control" column to four decimals): cumulative moves from h = 0.015 at ×0.35 **1.1529 / 1.4174 / 0.8893 %** at 0.012 and **1.4722 / 1.6609 / 1.6316 %** at 0.0095 (`:6199–6205`), beside ×0.45's 1.2524 / 0.8394 / 1.5295 % and 1.8423 / 1.2053 / 2.3073 % — ratio ×0.35/×0.45 0.921 / 1.689 / 0.581 and 0.799 / 1.378 / 0.707 (*predicted* within 20 % if additive; only S₁₁ at 0.012 holds). Step 0.012 → 0.0095 at ×0.35 is 0.7137 / 0.5187 / **0.8896 %**, step ratio 0.619 / 0.366 / **1.000** (×0.45: 0.496 / 0.575 / 0.527) — S₃₁'s move does not shrink. Richardson at ×0.35 (`:6207–6211`): **p = 2.3270 / 4.6812 / 0.2363** against step 2e's 3.2264 / 2.6300 / 3.0608; S₃₁'s S_inf lies 15.80 % beyond its own finest rung and is not a reading. |S_inf(×0.35) − S_inf(×0.45)|/|S_inf(×0.45)| = **0.7974 / 0.8657 / 14.94 %** (*predicted* ≈ 0.89 / 0.83 / 0.88 % if additive). Knob move ×0.45 → ×0.35 at fixed h = 0.015 / 0.012 / 0.0095: S₁₁ 0.893 / 0.950 / 0.865 %, S₂₁ 0.834 / 1.048 / 0.950 %, S₃₁ **0.878 / 0.505 / 0.487 %**. **Negative-result clause fires** ("×0.35 moves differing from ×0.45's by more than half of themselves ⇒ the knobs interact: record in §7 and stop"): at h = 0.012, S₂₁'s cumulative moves differ by 0.408 of the ×0.35 move (0.689 of the ×0.45 move) and S₃₁'s by 0.720 (0.419); S₁₁ stays ≤ 0.251 under either reading. Whichever denominator is meant, one class crosses one-half, so the two knobs are recorded as **interacting**, most clearly in the opposite-port class S₃₁. Stopped as instructed: no fifth rung, no two-knob fit. *Caveats:* S₁₁'s and S₂₁'s fixed-h knob move is nearly h-independent (≈ 0.9 %, additive-looking) even where the cumulative-move ratio misses 20 %. S₃₁ is the smallest entry (|S₃₁| ≈ 0.22), so small absolute shifts read as large relative moves. Each knob has three points, and 2a″/2e show three-point pictures on this fixture revised by one more point. A two-knob extrapolation is the weekly's to price against the `xl` tier. No AED number is compared, no band moved, and the Larmor verdict stays with the 2026-09-13 weekly. **Ruled 2026-09-12 18:00 review — 2f and 2g accepted as measured (re-read `20260912T170105Z_ANS-4-step2f.log:6141–6146` and `20260912T200107Z_ANS-4-step2g.log:6199–6211`); the step-2 family is FROZEN under the four-attempt rule.** Since 2a″ (the last sub-step that landed a gate — the explicit-flag guard, asserted in its own smoke window) the family has run 2a‴, 2e, 2f and 2g, four attempts whose asserted anchors were all imported gates and whose questions (asymptoticity, knob separability) are gated by nothing; the operator directive of 2026-09-12, enacted by this review, caps a family at four, so no 2h is queued and the ≈ 0.9 M-cell fifth rung 2f prices is not taken. The banked negative, for the weekly's Larmor verdict: **no degree-1 ladder on this fixture is in a proven asymptotic range on either knob** (2a‴ refuted the conductor fit, 2f refuted the global fit's p ≈ 3, 2g shows the two knobs interact in S₃₁), so a degree-1 extrapolant is not an h → 0 reference and the only order-matched, converging sequence on record is step 2d's degree-2 `xl` ladder (successive change 1.19 → 0.62 % on S₁₁). What would move the row's Larmor verdict is the weekly banking or declining AGREE on step 2d's evidence, and the only further measurement that could bear on it is a two-knob or degree-2 rung the weekly prices against the `xl` tier as a numbered step 3. **Step 3a EXECUTED 2026-09-14, 04:30 implementer slot — green; `FEM_EM_ANS4_FREQUENCY_HZ` lands, the flag-off control reproduces 2a″ and the knob reaches the solve.** Tests only, no `src/`: one module-level `LADDER_FREQUENCY_HZ` (env `FEM_EM_ANS4_FREQUENCY_HZ`, unset or empty = `FREQUENCY_128_HZ`, read once at import) threaded through the four `_four_port_rung` sites (2d, 2c, 2a ladders and the degree-2 solve) and the ladder-built print, which now echoes the env. The §9 item counted five sites; the fifth is that print. New `test_the_frequency_knob_reaches_the_solve` picks the ×1 degree-1 control rung (either ladder path, conductor factor unset or 1). At 128 MHz with `c4_congruent_sheets` on it asserts the driven column against step 2a″ w1's record (`STEP3A_RECORD_128MHZ_C4`, restated from `20260911T093247Z_ANS-4-step2a-dprime-w1.log:4061–4063`) at rtol 1e-6, only at the record width `-n 8` (`OPS-41`; the pending file's "`-n 2`" is superseded, as the item said). Off 128 MHz it asserts every class moves more than 1e-2 from that record, and at 64 MHz it prints the per-class max\|ΔS\| against `PORT-11` step 2's 4×4 (`20260826T110434Z_PORT-11-step2.log.gz:9264–9267`) as a *predicted* negative control. **Window 1 (flag-off control: `DEGREE2=0`, `C4_CONGRUENT=1`, `RUNGS="1.0"`, `-n 8`, `-s`, complex + `FEM_EM_REQUIRE_COMPLEX=1`, `timeout -k 30 300`, durable capture, standard)** `20260914T093325Z_ANS-4-step3a-w1.log`: **15 passed, 1 skipped (the refinement control, one rung), Status 0, 62 s**. 116 118 cells (ratio 1.000284, `:2110`), f = 1.280000e+08 Hz with the env unset (`:2109`). S₁₁ / S₂₁ / S₃₁ equal 2a″'s to every printed digit (`:2138–2140`), \|ΔS\|/\|S\| = **7.067e-11 / 8.171e-11 / 2.083e-13** against 1e-6 (`:2154–2156`). The imported `PORT-11` gates pass (`:2126`, reciprocity 1.08e-15, σ_max 0.998977574510, spreads 0.0739 / 0.0822 / 0.0506 %). Four drives took 4.1 s against 2a″'s 15.2 s, the `PORT-19` reuse landing in between, and the digits did not move. **Window 2 (the knob: `DEGREE2=0`, `RUNGS="1.0"`, `C4_CONGRUENT` unset — the `GEO-19` record mesh, `FEM_EM_ANS4_FREQUENCY_HZ=64e6`, `-n 2`, `timeout -k 30 300`, durable capture)** `20260914T093444Z_ANS-4-step3a-w2.log`: **15 passed, 1 skipped, Status 0, 63 s**. 116 085 cells (ratio 1.000000, `:1882`), f = 6.400000e+07 Hz echoed (`:1881`). Every imported gate passes at 64 MHz (`:1887`: reciprocity 1.25e-15, σ_max 0.999721388281, spreads 0.0573 / 0.0599 / 0.0370 %). Class moves from the 128 MHz record **0.4305 / 0.1523 / 0.1689** against the 1e-2 floor (`:1902–1904`). **Negative control (*predicted* ≤ 1e-2, printed):** per-class max\|ΔS\| against `PORT-11` step 2's 64 MHz 4×4 reads **4.564e-11 / 6.076e-11 / 2.442e-11** (`:1905`). The package route reproduces the 2026-08-26 record to round-off at a different width and 19 days of commits later, far inside the prediction. `pgrep -c python3` was 0 before and after both windows. **Status moved:** none on the row. `xl-pending.md` entry 2's prerequisite is on `main`; marking it `READY` and queueing it is the next review's. No band moved, no AED number, no 64 MHz accuracy claim. **Step 3 — the 64 MHz order-matched rung, XL window 2026-09-16 02:00** (queued by the operator 09-15 into the new FIFO `docs/testing/xl-queue.d/`, run by `xl-run.sh` with no session, `20260916T070008Z_ANS-4-step3.log`, committed `1fcb2a8`): the entry-2 command verbatim — `FEM_EM_ANS4_FREQUENCY_HZ=64e6`, `RUNGSPEC="0.015:1 0.005:2"`, `-n 16`, `timeout -k 60 14400`, durable capture, `memory.peak` appended. **16 passed, Status 0, 1658 s** (the 2000–3000 s prediction beaten: `PORT-19`'s reuse is in the module). ×1 degree-1 rung 116 085 cells (ratio 1.000000), four drives 3.6 s, gates green (spreads 0.0573 / 0.0599 / 0.0370 %, `:2434`). Degree-2 rung 592 744 cells / ~3 793 562 unknowns, mesh 105.7 s, four drives 1476.9 s (`:4605`), summed `ru_maxrss` 282.3 GiB over 16 ranks (`:4604`), cgroup `memory.peak` 301 547 610 112 B = 280.8 GiB (`:5735`; the launcher recreates the service before the window, so the peak is this run's); reciprocity 1.640e-14, σ(S) 0.999758 / 0.8657 / 0.8590 / 0.8580, column power sums 0.805–0.807, class spreads 0.1985 / 0.1715 / 0.1946 % (band 0.5 %, `:4600–4603`). **Public readout, the degree 1 → 2 move on the driven column at 64 MHz** (`S_1k` at `:2427` vs `:4596`): self `S11` 4.489e-02+5.803e-01j → 8.063e-02+5.929e-01j, \|ΔS\|/\|S\| = **6.51 %**; adjacent `S21` 3.665e-01−2.047e-01j → 3.560e-01−2.075e-01j, **2.60 %**; opposite `S31` 2.179e-01−2.566e-01j → 2.041e-01−2.567e-01j, **4.11 %** (step 2d at 128 MHz: 6.09 / 5.38 / 6.70 %). Recorded by the 2026-09-16 03:00 review as a record; the private miss against the AED First Order column at 64 MHz and the pre-registered decision rule (§10; `xl-pending.md` entry 2, now `RUN`) are the 2026-09-19 weekly's. The same review queued three priced-family variants of this window under the daily licence (entries 3–5: step 3b 128 MHz degree 2 on the `c4_congruent_sheets` cut, 3c 10 MHz degree 2, 3d 64 MHz degree 2 on the congruent cut). 

**Steps 3b and 3c — the two priced-family variants that ran 2026-09-17 / 09-18 (recorded by the 2026-09-18 03:00 review from `log-pathologist` readings; both CONFIRMED countable; adjudication the 2026-09-19 weekly's).** Both at `-n 16`, `RUNGSPEC="0.015:1 0.005:2"`, service recreated before the window, FFCx cache warm (the named cache volume survives the recreate — these are not cold prices). **3b, 128 MHz, `c4_congruent_sheets=on`** (`20260917T070008Z_ANS-4-step3b.log`): 15 passed / 1 skipped, rc 0, Status 0, 1539 s (`:4977`, `:5955–5959`); `memory.peak` 293 192 646 656 B = 273.1 GiB (`:5953–5954`), summed `ru_maxrss` 274.6 GiB (`:4824`); ×1 rung 116 118 cells / 139 177 dofs, degree-2 rung 592 550 cells / **3 827 268 dofs** (`:4485`; the readout's `~3,792,320` is a 6.4 × cells estimate), mesh 103.4 s, four drives 1361.4 s (`:4825`; one factorisation 1321.9 s, held solves ≈ 5 s). Degree-2 gates (`:4849`): reciprocity 6.292e-15, σ_max 0.999035987, class spreads **0.0056 / 0.0088 / 0.0070 %** (×1 rung 0.0739 / 0.0822 / 0.0506 %). Driven column at degree 2 (`:4816–4819`): `S11` +5.174448157e-01+5.727174207e-01j, `S21` +2.125222010e-01−2.623893707e-01j, `S31` +4.341302766e-02−2.094238693e-01j; two-rung move (h 0.015 → 0.005 and degree 1 → 2 together) **5.71 / 4.52 / 6.73 %** (`:4874–4877`). The ×1 rung reproduces the 2a″ record to rel 7.1e-11 / 8.2e-11 / 2.4e-13 (`:4896–4899`) — printed, not asserted: `test_the_frequency_knob_reaches_the_solve` skips at `-n 16` by design (`OPS-41`, record width `-n 8`). **3c, 10 MHz, cut off** (`20260918T070008Z_ANS-4-step3c.log`): 16 passed, 0 skipped, rc 0, Status 0, 1603 s (`:4757`, `:5735–5739`); `memory.peak` 305 814 069 248 B = 284.8 GiB (`:5733–5734`), summed `ru_maxrss` 286.3 GiB (`:4604`); ×1 rung 116 085 cells / 139 140 dofs, degree-2 rung 592 744 cells / **3 828 494 dofs** (`:4265`), mesh 107.0 s, four drives 1419.4 s (`:4605`; factorisation 1263.0 s, held solves 5.4–7.8 s). Degree-2 gates (`:4624`): reciprocity 8.056e-14, σ_max 0.999993961, class spreads **0.4451 / 0.2199 / 0.2186 %** — the self class at 89 % of the 0.5 % band (the identical mesh at 64 MHz, step 3: 0.1985 %). Driven column at degree 2 (`:4596–4599`): `S11` −3.540954870e-01+1.453842103e-01j, `S21` +4.608063178e-01−4.231620120e-02j, `S31` +4.323582467e-01−7.297403596e-02j; two-rung move **4.41 / 1.35 / 1.08 %** against the window's own ×1 rung (`:4652–4654`). Step 3's "degree 1 → 2 move" above is this same two-rung move; step 2b's 6.09 / 5.38 / 6.70 % was a degree-only move on one mesh and is a different comparison. Label traps in both logs (a literal "at -n 2", and in 3c a literal "at 128 MHz" over 10 MHz data): known-issues 2026-09-18, `OPS-51`. Queued by the same review under the daily licence: step 3e (64 MHz on step 2d's four-rung ladder — the *h*-convergence statement under the 64 MHz order-matched rung) and step 3f (10 MHz degree 2 on the congruent cut — the discriminator for 3c's near-band self spread); `xl-pending.md` entries 7 and 8. No band moved; no AED number here.
