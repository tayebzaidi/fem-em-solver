# OPS-33 — Re-base `ans:3`'s four `RECORDED_*` constants to the 0.11 image under the (1*) licence, then register the 1e-6 run-to-run control `OPS-32` could not

 **Re-base `ans:3`'s four `RECORDED_*` constants to the 0.11 image under the (1*) licence, then register the 1e-6 run-to-run control `OPS-32` could not** — `OPS-32` measured that `examples/ports/02_package_sparameter_sweep.py:125–149` (`EX-20`'s records, which `03_two_torus_gap_ports_10MHz.py:109–112` re-exports) carries the `PORT-1` step-4 figures from the v0.7.2 image (`RECORDED_RAW_RATIO` 0.894543, `RECORDED_CORRECTED_RATIO` 0.939849, `RECORDED_S_SYMMETRY_RESIDUAL` 4.758625e-05, `RECORDED_S_SPECTRAL_NORM` 0.864809457) while the 0.11 image reads 0.894516 / 0.939822 / 4.7586e-05 / 0.864809 (`20260902T183603Z_OPS-32.log:1398–1405`): a 2.9e-05 record-vs-image gap, tenth of a percent of the 1% band, an `OPS-27`-class stale record and not scatter. Re-record all four with the 0.11 tag and the v0.7.2 values kept in a comment (the `OPS-27` pattern), then tighten the *reproduction* control on `raw` / `corrected` / `spectral` to **1e-6 relative** against `EX-37`'s measured ≤ 5e-8 Z/S scatter (≥ 20× headroom), and on the symmetry residual to **1e-6 absolute** — the ceiling rubric: that residual is ~4.8e-05 absolute, so a 5e-8 S-entry scatter is ~1e-3 of it *relative* and a relative 1e-6 there is arithmetically unreachable (this is why `OPS-32`'s symmetry entry "moved" 1.71e-06 → 1.91e-06 between two runs of the same code). Done-when: `ports:2` and `ans:3` both exit 0 with every physics gate unmoved (`‖S − Sᵀ‖/‖S‖ < 1e-3`, `‖S‖₂ ≤ 1`, corrected mutual inside 10%, raw a MISS), the four reproduction misses printed ≤ 1e-6 (rel / abs as above), and a **negative control** that the old v0.7.2 values would *fail* the new band (2.9e-05 > 1e-6 — printed from the comment values, asserted, so the band is shown to bite). Census `exit != 1`. Standard, host runner, `-n 2` complex; `ans:3` measured 171 s, `ports:2` unmeasured on this image — run them as separate windows, `-t 400`. Scope: two records and a control band under the (1*) licence, no physics band, no gate, no §2 claim; `EX-20` / `ANS-3` statuses unchanged. Negative result: a miss > 1e-6 on a freshly re-based record is run-to-run scatter above `EX-37`'s figure on this fixture — known-issues entry with the measured scatter, band left at 1%, stop. Opened 2026-09-02 18:00 review | ✅ *(2026-09-03 — **the re-record landed and the 1e-6 control is registered; the negative result did not occur**. All four `RECORDED_*` in `examples/ports/02_package_sparameter_sweep.py` re-based onto the 0.11 image at full metrics.json precision — `RECORDED_RAW_RATIO` 0.894543 → **0.8945163788281**, `RECORDED_CORRECTED_RATIO` 0.939849 → **0.9398215452105**, `RECORDED_S_SYMMETRY_RESIDUAL` 4.758625e-05 → **4.7586341120262e-05**, `RECORDED_S_SPECTRAL_NORM` 0.864809457 → **0.8648094567341** — with the superseded v0.7.2 digits kept verbatim as `SUPERSEDED_V072_RECORD` (the `OPS-27` pattern, as data rather than prose so the control can assert on them). Band: `REPRODUCTION_BAND_RELATIVE` 0.01 → **1e-6** on raw / corrected / ‖S‖₂ and a new `REPRODUCTION_BAND_SYMMETRY_ABSOLUTE` = **1e-6 absolute** on the symmetry residual, per the row's reachability rubric. **Anchor (i)** — both examples re-run exit 0 with every physics gate unmoved: `ans:3` `20260903T050551Z_OPS-33.log`, Status 0, **165 s**, `‖S − Sᵀ‖/‖S‖ = 4.7586e-05 < 1e-03`, `‖S‖₂ = 0.864809 ≤ 1`, corrected mutual 0.939822 at −6.02% inside the 10% band with the raw ratio still a **MISS at −10.55%** (`:1408,1414`); `ports:2` `20260903T050845Z_OPS-33.log`, Status 0, **206 s**, same four gates plus the deprecated-heuristic separation 3.030e-01 (`:1424,1433`). **Anchor (ii)** — the four reproduction misses printed and asserted: `ans:3` raw **0.00e+00**, corrected **4.63e-14**, symmetry **5.99e-14 abs**, ‖S‖₂ **7.57e-15** (`:1409`); `ports:2`, an independent solve, raw **6.51e-10**, corrected **6.39e-10**, symmetry **1.87e-10 abs**, ‖S‖₂ **6.64e-11** (`:1425`) — ~3 decades inside 1e-6, which is what makes the figure a scatter control rather than a tautology on a freshly re-based record. **Anchor (iii)** — census `20260903T051230Z_OPS-33.log:34,69`: `Scanned 47 guide(s)`, `RESULT: dead=0 guide=0 stale=30 stale_severity=report exit=2`, `exit != 1`; the 30 stale hits are age-only (`OPS-19` exit-2 class, inflated by this slot's two fresh runs leaving the other cases' artifacts aged). **Negative control, asserted in both scripts** — the superseded v0.7.2 raw/corrected miss the *same* run by **2.98e-05 / 2.92e-05**, both > 1e-6, so the band is shown to bite; the assertion is on the live measurement, not on the comment. (The symmetry record is *not* part of that control: v0.7.2's 4.758625e-05 differs from the new record by only ~9e-11 absolute and would have passed — the row anticipated this, which is why it named raw and corrected only.) Generated-artifact control per the `OPS-32` ruling: the tracked `COMPARISON.md` and `metrics.json` re-stamped their timestamp and FEM digits as designed, and the diff contains **no AED value and no non-blank AED cell** (`"aed": null` unchanged); nothing under `aed_results/` or any `*_private.md` was staged. Guides/`SPEC.md` narrative digits re-recorded with them. No physics band, no gate, no §2 claim moved; `EX-20` / `ANS-3` statuses unchanged. Retires the `OPS-32` known-issues entry in the same commit.)* *Audited 2026-09-03 03:00 review — `auditor` PASS on seven checks, **DEMOTE on tier honesty**: `ports:2` measured **206 s** (`20260903T050845Z_OPS-33.log:1438`, re-traced by the review) against the 180 s standard ceiling the row declared; no wrapped ceiling was exceeded (`-t 400`, `:12`), so on the `OPS-27` / `OPS-30` / `OPS-31` precedent the row is **re-tiered to heavy and stays ✅**. Digits re-traced: `:1425` the four `ports:2` misses, `:1426` the superseded-record control 2.98e-05 / 2.92e-05. The full-precision re-based constants trace to the tracked `metrics.json` at `80ec7ae`, not to a log line — the auditor's caveat, noted, not disqualifying.* 
