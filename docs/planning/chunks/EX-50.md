# EX-50 — The conductor as a hole in ParaView

 **The conductor as a hole in ParaView — the PEC sphere cut from the box beside `th:3`'s dielectric sphere solved inside, the exterior dipole coefficient asserted against β = 1** (`TH-15` step 1's newly gated capability, 2026-09-05: the first interior-conductor model — the **geometry / boundary-condition** angle: `th:3` shows a *solved-inside* sphere whose ε sets the field; no example shows a body absent from the mesh with `n × E = 0` on its cavity facets; opened 2026-09-05 18:00 review, §5.4 ramp). Executor `example-runner`, spawned foreground, both census windows through the harness. `examples/time_harmonic/09_pec_sphere_as_hole.py` / `.md` (`th:9`): mesh from `MeshGenerator.sphere_in_box_domain(as_hole=True)` at the gate module's imported `RESOLUTION_SPHERE` / `RESOLUTION_FAR` (the 13 239-cell gate rung — call the generator directly, the module's `_MESH_CACHE` is process-local), the imported `_pec_exterior_numpy` Dirichlet callable on the wall, `pec_facet_tags=(OUTER_BOUNDARY_TAG, CAVITY_TAG)`, the dipole fit on the imported `_probe_shells` sample set through `evaluate_vector_field_parallel`. **Anchors (asserted, imported from `tests/validation/test_pec_sphere_hole.py`, never restated):** `\|β − 1\| ≤ BAND` (= 2 × `TH8_RECORD_INTERIOR_MISS` = 4.886%; the gate read 1.9746% on this mesh — reproduce it to 1e-3 relative, the same mesh and code); reduced cavity dof count > 0 and `max\|E\|` on them ≤ 1e-12; `\|Im E\|/\|Re E\| ≤ 1e-6`. **Negative control (asserted — backed by the gate module's measurement of the same comparison on the same mesh, `20260905T170225Z_TH-15.log:303`, 29.0× with ceiling 30×):** the natural cavity `pec_facet_tags=(OUTER_BOUNDARY_TAG,)`, `\|β_control − 1\| ≥ 5 × BAND`; the factor printed, none larger claimed. **Printed only:** cells beside the gate's 13 239; the max / rms pointwise miss (records 46.07% / 18.64%) with the guide saying why they are large while β is 2% and that the module asserts the convergence *rate* (+1.19 ≥ 0.8), not these numbers. **Writes** one combined XDMF: `E_real` / `E_magnitude` (CG1 interpolation of the N1curl field on the air cells) plus `CellTags` and the cavity facet tag through `write_xdmf_with_tags(…, facet_tags=…)`, so ParaView shows a box with a spherical void whose surface field is tangentially zero — `Clip` through y = 0, `Glyph` on `E_real`; the guide puts it beside `th:3`'s picture. **Tier / ranks / cost:** the module's three solves on 13 239 cells took 34 s at `-n 2` (`20260905T170225Z_TH-15.log`); two solves + one write ≈ 25 s ⇒ standard, `./scripts/run_examples.sh -e th:9 -n 2 -t 300`, complex build (the `th:` group sources it), foreground. **Traps already paid for:** `<=`/`max` on complex operands — `.real`/`abs` first; the cavity dof set and the probe cloud cross ranks — reduce; two arrays need two `name`s; `EX-47`'s runner-socket fallback; full-filename guide references. **Scope:** an example of the gated capability at 10 MHz on the sphere — no loss, no Q, no copper, no two-torus, no coil; no band moved; the additive licence is not expected to be needed. **Negative result:** β outside the band through the example path while the module holds it is an example/test divergence — known-issues entry, stop. *(closed below)* **2026-09-05 21:00 implementer slot** (`example-runner`, foreground; one host-runner window, Status 0, **4 s harness-wall / 1.9 s in-example**, `20260906T020307Z_EX-50.log`). `examples/time_harmonic/09_pec_sphere_as_hole.py` / `.md` (`th:9`) written; every anchor imported from `tests/validation/test_pec_sphere_hole.py`, never restated, and reproduced to the digit: **13 239 cells** (the gate's own middle rung, mesh built by calling the generator directly rather than through the gate's process-local `_MESH_CACHE`); fitted **β = 1.019746**, `\|β−1\|` = **1.9746%** against `BAND` = 4.8860% — the gate module's own reading on this identical mesh, to 1e-3 relative; cavity dofs (tag 2, reduced) = **1702** > 0, max `\|E\|` on them **0.000e+00** < 1e-12; `\|Im E\|/\|Re E\|` = **0.000e+00** < 1e-6. **Negative control, asserted:** the natural-cavity route (`pec_facet_tags=(OUTER_BOUNDARY_TAG,)`) fitted β = −0.419038 (void closed form −0.5), `\|β_control−1\|` = 141.9038% = **29.0× the band**, against the `≥ 5×` floor and reproducing the gate module's own 29.0×/30× measurement of this identical comparison on this identical mesh (`20260905T170225Z_TH-15.log:303`) to the digit. **Printed, not asserted:** max/rms pointwise miss on the probe shells **46.0725% / 18.6437%**, matching the gate's own 46.07%/18.64% records. Wrote one combined XDMF (`time_harmonic_09_pec_sphere_hole_combined.xdmf`) with `E_real`/`E_magnitude` plus `CellTags` and the cavity facet tag via `write_xdmf_with_tags(..., facet_tags=facet_tags)`. Census through the harness both sides, predicted before read: pre `dead=0 guide=0 stale=77 exit=2`, 43 guides/runnable (`20260906T020153Z_EX-50-precensus.log:114`) → predicted 44/44, unchanged dead/guide/stale → measured post `dead=0 guide=0 stale=77 exit=2`, **44** guides/runnable (`20260906T020354Z_EX-50-postcensus.log:114`) — exact match. No `src/` change, no `tests/` change — the additive licence was not needed (every record and callable the gate module already exposed). No band moved, no record re-recorded; an example of the gated capability only. | ✅ 
