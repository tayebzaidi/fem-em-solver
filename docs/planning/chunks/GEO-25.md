# GEO-25 — F-human cost probe

 **F-human cost probe** — `birdcage_port_domain` at `ring_radius` 0.07 → 0.15 m (30 cm coil, the operator's 2026-08-25 directive), a parameter set on the existing constructor, not a new one; cell count and mesh time per rung, conductor sizing **not** coarser than the 4.8 mm floor without re-measuring it (known-issues `GEO-21` ruling) | ✅ **step 2 closed 2026-09-05 13:30 implementer slot — the §3 route back was walked, the row is ✅ again.** New `tests/validation/test_birdcage_f_human_rung.py`, `2 passed in 194.05s`, Elapsed 196 s, heavy by ceiling (`timeout -k 30 560`), `-n 2`, real build, `docs/testing/logs/20260905T183654Z_GEO-25.log:20030`. **Every step-1 reading now reproduces at `-n 2` as an executed assert, to the digit.** Branch B (the fixture, `scale_sizing=False`): cells **504 642 vs record 504 642, relative 0.000e+00** at the imported `CELL_COUNT_BAND` = 0.01; volume partition **1.000000000000** at the imported `GEO-18` `EXACT` = 1e-9; terminal-area ratio **0.974454791 … 0.974455230 on all 32 ring ports**, inside the `GEO-19` closed-form [0.95, 1.0]; meshed/CAD conductor **0.965414**, clearing the imported `CAD_MASS_GATE` = 0.95 by **+0.015414** (`:9730–9735`, mesh 109.35 s). Branch A (the negative control, `scale_sizing=True`): cells **204 977 vs record 204 977, relative 0.000e+00**; the same partition and terminal identities hold (0.974445583 … 0.974446474); meshed/CAD **0.893028**, i.e. **0.056972 BELOW** the same gate (`:20017–20022`, mesh 63.50 s). The separation assert `control < CAD_MASS_GATE <= fixture` reads **0.893028 < 0.95 ≤ 0.965414, separation 0.072387** (`:20024–20027`) — **fixed absolute mesh sizing is now a gated conclusion, not an ungated reading**: a similar-mesh F-human coil fails the existing `GEO-15`/`GEO-21` conductor-mass gate and the fixed-sizing one clears it. Two version-tagged cell records registered under the (1\*) licence at `tests/validation/test_birdcage_f_human_rung.py` (`F_HUMAN_BRANCH_B_CELL_RECORD`, `F_HUMAN_BRANCH_A_CELL_RECORD`), both asserted at the imported band and never at equality (the `OPS-27` stale-exact class). **The step-1 determinism worry is answered for this rung**: the repeat was on the 0.10 m branch-A rung, and both 0.15 m rungs now reproduce at `-n 2` in a different process at 0.000e+00 — no known-issues entry needed. `_params` moved verbatim into the gate module, which is now the single source of the F-human parameter set, and `scripts/probes/geo25_ring_radius_cost_probe.py` imports it (plus `BASE_RING_RADIUS`, `LEG_COUNT`) back; all 16 printed parameters verified unmoved against step 1's own log (`20260905T184025Z_GEO-25.log` vs `20260904T214031Z_GEO-25.log:36–50`). Two builds per process, cached — the module never rebuilds a rung for a second assert. Nothing in `src/` changed; no solve, no port gate, no physics claim. The step-1 measurement block below is untouched. ~~🧪 **demoted ✅ → 🧪 by the 2026-09-04 18:00 review on the §3 rule**~~ — a measurement-only step is 🧪, never ✅: the probe script says "asserts nothing" in its own docstring (`scripts/probes/geo25_ring_radius_cost_probe.py:3`) and contains no `assert`, so no executed quantitative assertion exists (audit DEMOTE, evidence re-read by the review). The commission text below said "anchors, asserted per rung" while naming `mesh-probe` as executor, whose definition says "never an assertion" — the two were in conflict and the executor followed its definition; the review's fault, not the slot's. **Every measurement below stands untouched**; the route back is **step 2 (the gate)** scoped at the end of this row. **Rungs 1, 2 and 3 all measured, 2026-09-04 16:30 implementer slot** (`mesh-probe`, six foreground windows, all Status 0, 554 s total). **Negative control passed exactly:** the 0.07 m rung reproduces `mesh:9`'s **265 621 cells at relative 0.000e+00** (`20260904T213333Z_GEO-25.log:10182,10190`), terminal-ratio range 0.974454791–0.974455668 matching `EX-35`'s record digit for digit. Volume partition **1.000000000000** and terminal ratio in [0.95, 1.0] on all 32 ports at every rung and both branches — the `GEO-18`/`GEO-19` identities are genuinely scale-free. The row's phrase "phantom/air **sizing** scaled with the radius" is ambiguous (mesh size field vs geometric size), so the probe measured **both branches** rather than guess; the choice inverts the sign of the answer. **Branch A** (global `resolution` scales too — geometrically similar meshes): 0.07 m **265 621** cells / 72.35 s / 0.548 GiB (`…213333Z:10182–10189`); 0.10 m **244 056** / 67.82 s / 0.538 GiB (`20260904T213508Z_GEO-25.log:10126–10133`); 0.15 m **204 977** / 69.98 s / 0.505 GiB (`20260904T213646Z_GEO-25.log:10331–10338`). **Branch B** (`resolution` held at `mesh:9`'s 0.015 m — fixed absolute element size, the only reading under which the `r³` prediction is testable): 0.10 m **352 984** / 90.21 s / 0.627 GiB (`20260904T213833Z_GEO-25.log:10248–10255`); 0.15 m **504 642** / 112.27 s / 0.752 GiB (`20260904T214031Z_GEO-25.log:9733–9740`). **The pre-registered `r³` prediction is wrong on both readings** — branch A 0.07→0.15 measures 204 977 against a predicted 2 613 618 (0.0784×, fitted exponent **−0.34**), branch B 0.0784→0.1931× (fitted exponent **+0.84**, time exponent +0.58). The mesh is dominated by the pinned 1.6 mm conductor refinement region, whose count grows roughly *linearly* with conductor length, not with domain volume; **≈ 0.84 is the honest cost exponent** for the same coil made bigger at fixed absolute fidelity, and §10 Phase 6 should date itself from that, not from `r³`. **The stop rule was never approached** — worst rung 504 642 cells (17% of the 3 M ceiling), 112.27 s (19% of 600 s), peak RSS 0.752 GiB; conductor sizing never coarsened; rung 3 was licensed by the row's own arithmetic on both branches (`3.375 × 67.816 = 228.9 s` and `3.375 × 90.205 = 304.4 s`, both < 600 s) and run. **Determinism:** the 0.10 m branch-A rung re-run in a fresh process reproduces every reported digit (`20260904T214248Z_GEO-25.log:10126–10133`); only wall time (67.82 → 68.55 s) and `ru_maxrss` move. **No `GEO-21`-class negative result** — every rung meshes, no mesher fallback and no invalid-boundary line in any log. **One ungated reading for the weekly review:** under branch A the conductor's meshed/CAD mass recovery falls monotonically **0.976465 → 0.959483 → 0.893028**, so at 0.15 m it is *below* `CAD_MASS_GATE = 0.95` — an F-human coil built by scaling the mesh sizing with the radius would **fail the existing `GEO-15`/`GEO-21` conductor-mass gate**, while branch B stays 0.976465 → 0.972185 → 0.965414. That is an independent argument that **fixed absolute sizing is the right scaling for this generator**; nothing was asserted or moved on it. Probe script `scripts/probes/geo25_ring_radius_cost_probe.py` (measurement only, asserts nothing, imported by nothing). Two deviations, both disclosed: (a) `conductor_resolution` pinned at `mesh:9`'s **1.6e-3 m**, which is *finer* than the 4.8 mm `GEO-21` figure — the rule as written is "not **coarser** than the floor", 4.8e-3 is `GEO-21`'s coarse *control* (0.846 mass recovery), and 1.6 mm is the only value that reproduces the mandated 265 621-cell control; (b) `leg_spacing` scaled with `coil_length` (the row names only `coil_length`) — an unscaled 11 cm ring separation inside a 30 cm coil is not a similar geometry. **Also unscaled and worth the weekly's attention: the conductor *cross-section* (`leg_width`, `ring_minor_radius`) is held at the 7 cm coil's values at every rung**, so these are cost figures for a human-sized cage built from small-coil stock, not a re-proportioned conductor. A price, not a fixture: no record moved, no default changed, no solve, no assertion written. Original commission text follows. *(commissioned 2026-08-30 02:15 weekly review; **the ruling text was lost with that session** — what survived is the dashboard summary and the `GEO-21` known-issues ruling's two sentences. **Re-scoped 2026-09-02 weekly review — queueable.** Executor `mesh-probe`, measurement only, no solve. Ladder on the existing constructor: `ring_radius` 0.07 → 0.10 → 0.15 m with `coil_length` and the phantom radius scaled by the same factor (a 6 cm phantom in a 30 cm coil is not a load), 16 legs, ring-gap (high-pass) layout as `mesh:9`, conductor sizing pinned at the 4.8 mm `GEO-21` floor, phantom/air sizing scaled with the radius. **Anchors, asserted per rung:** the scale-free CAD identities `GEO-18`/`GEO-19` already gate — volume partition 1.000000000000 and terminal-area ratio in [0.95, 1.0] on every port; the cell count per rung against the r³ prediction from the 0.07 m rung (report the fitted exponent; `GEO-19` measured 2.6472× cells / 3.2357× mesh time for 4 → 16 legs at fixed radius, the only cost rung above F-small on record). **Stop rule (pre-registered):** a rung whose mesh exceeds **3 M cells** or **600 s** is the finding — record it and stop, do not shrink the conductor sizing to make it fit. **Negative control:** the 0.07 m rung reproduces `mesh:9`'s 265 621 cells at 0.000e+00 relative. Tier heavy by ceiling (`timeout -k 30 1200`, gmsh is serial so `-n 1`), three windows ≈ 5 + 12 + 30 min predicted from the r³ scaling. **Scope:** a price, not a fixture — no record moves, no default changes, no solve; the exponent and the 0.15 m cell count are what §10 Phase 6 dates itself from. **Negative result:** a rung that does not mesh is a `GEO-21`-class known-issues entry; §10 subgoal 4 is explicitly **not** blocked on it. **Queued 2026-09-03 18:00 review as §9 item 5 (spare), sized to the 20-minute rule:** rungs 1 and 2 only, rung 3 attempted only if `3.375 × t_2 < 600 s` from rung 2's mesh time; otherwise the prediction is the finding and the 0.15 m rung is the weekly's to re-rung)* **Step 2 — the gate (scoped 2026-09-04 18:00 review, the §3 route back to ✅; §9 item 6).** New `tests/validation/test_birdcage_f_human_rung.py`, `-n 2`, heavy by ceiling (`timeout -k 30 600`; the two builds measured 112.27 + 69.98 s at `-n 1`, so ≈ 200–250 s expected). Move the probe's `_params(radius, scale_sizing=…)` verbatim into the module as the single source of the F-human parameter set and have the probe import it (additive; the probe's printed digits must not move). Build the **branch-B 0.15 m rung** (`scale_sizing=False`) and assert, every band imported: (i) cells = **504 642** at `CELL_COUNT_BAND` — a new version-tagged record `F_HUMAN_BRANCH_B_CELL_RECORD` registered by this step under the (1\*) licence; (ii) volume partition 1.000000000000 at the `GEO-18` band; (iii) terminal-area ratio in [0.95, 1.0] on all 32 ports (the `GEO-19` band); (iv) meshed/CAD conductor mass recovery ≥ `CAD_MASS_GATE` (0.95, imported from `tests/mesh/test_birdcage_conductor_sizing.py:63`) — measured 0.965414. **Negative control:** the **branch-A** 0.15 m rung (`scale_sizing=True`, second build in the same window) must read *below* `CAD_MASS_GATE` — measured 0.893028, 5.7 pp under against the fixture's 1.5 pp over; assert `control < CAD_MASS_GATE <= fixture`, margins printed, and its cells 204 977 reproduced at `CELL_COUNT_BAND` (a second record, control only). **Traps:** `cell_tags.values` and facet areas are rank-local — reduce before asserting (the probe already does); gmsh builds on rank 0; a build that fails at `-n 2` deadlocks — both rungs are known to mesh, so `-n 2` is licensed. **Scope:** registers the F-human branch-B rung's CAD identities and its cell record — *not* a solve, not a port gate, not a physics fixture; F-human physics stays Phase 6's and the weekly's. **Negative result:** cells not reproducing 504 642 at the band (the probe's determinism repeat was on the 0.10 m rung, not this one) is a known-issues entry with the measured spread and the record registered at the observed value with its band stated — never widened to swallow it; mass recovery below 0.95 on branch B is the finding that the F-human coil needs its own conductor sizing, known-issues entry, row stays 🧪. 
