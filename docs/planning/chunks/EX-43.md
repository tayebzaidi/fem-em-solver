# EX-43 — The first coil-driven SAR quantity in ParaView

 **The first coil-driven SAR quantity in ParaView — quadrant powers of the primal `σ\|E\|²` on the loaded birdcage** (`WF-6` step 3h's newly gated capability, 2026-09-02: twelve C4 integral pairs ≤ 1.52% at the unmoved 5% band on one fixture at 10 MHz at fixed `h` — the **output-quantity** angle: `EX-38`/`39`/`40` show `\|B₁⁺\|`, `mri:2` shows mass-averaged SAR on an *imposed* field, and no example shows SAR on a solved coil). Executor `example-runner`. `examples/ports/09_birdcage_sar_quadrant_powers.py` / `.md` (`ports:9`): `build_four_port_sweep` at 10 MHz on the default 116 085-cell mesh, four single drives + the quadrature superposition; **writes** the phantom's local SAR map `σ\|E\|²/(2ρ)` (DG0, W/kg) for the P1 and quadrature drives and the four quadrant weights `w_j` as DG0 fields in one combined XDMF with tags, with the guide stating the *map* is a viewing quantity (the pointwise construction is the retired one) and the *integrals* are the gate; **prints** the `P_j^{(k)}` table and the twelve C4 pairs. **Anchors (asserted in-script, records imported from `test_birdcage_sar_integral.py` as it stands at `642bfc5`):** the twelve pairs at their `STEP3G_*` records (rtol 1e-3) and ≤ the imported 5% band; partition identity rtol 1e-10 on all five drives; P1 total = 5.637745667e-08 W at rtol 1e-3; 116 085 cells at ratio 1.000000; census `exit != 1`. **Negative control:** the mis-paired 180° quadrant control `>` the C4 pairing at every `k` (measured 89–159×). Cost: 3g's four solves 106 s + the write ⇒ ≈ 130 s, `./run_examples.sh ports:9 -t 400`, `-n 2` complex, foreground. Scope: an example; no band, no gate, no §2 change. Negative result: a record not reproducing at 1e-3 is a wiring defect — journal in this row, stop. Opened 2026-09-03 03:00 review, §9 item 3 | ✅ *(2026-09-03 07:30 slot, `20260903T123502Z_EX-43.log`, Status 0, **77 s** harness / 73.1 s in-script at `-n 2` complex — every anchor met on the first run: partition identity residuals 1.0e-14–1.6e-14 (band 1e-10), all five drives; P1 phantom total 5.637745667e-08 W vs the record, relative 4.114e-11; the twelve C4 pairs 0.2132–1.5200% (band 5%), all reproducing `STEP3G_INTEGRAL_PAIR_RECORDS` to the printed digit; the four mirror pairs 0.3438–1.7527%, all reproducing `STEP3I_MIRROR_RECORDS` to the printed digit; mis-paired 180° control 89.088×/130.890×/159.272× the C4 pairing; flank-vs-opposite mirror control 21.8–110.1×; cell ratio 1.000000 (116085/116085); docrefs census dead 1→0, stale 34→34 unchanged, exit 1→2 (staleness-only, not a failure). **Demoted ✅ → 🧪 2026-09-03 10:30 review (audit):** the census figures appear in **no harness log** — the one committed log is the example run alone (`grep 'dead='` on `20260903T123502Z_EX-43.log` returns nothing) and no `*-precensus` / `*-postcensus` window was logged, where `EX-41` and `EX-36` each logged both (`20260902T200212Z_EX-41-precensus.log:55`); every physics anchor traces to `:4618–4671` and stands, so this is an evidence gap on one anchor, not a defect. **Re-closes** on one logged census reading `dead=0` and `exit != 1` on a tree carrying `ports:9`'s artifacts — §9 item 2's mandatory pre-census, or any later census log, cited in this cell). **Re-promoted 🧪 → ✅ 2026-09-03 15:00 slot** on exactly that evidence: `EX-42`'s mandatory pre-census, run through the harness on the tree as it stood at `2730af3` with `ports:9`'s artifacts in place and **before any `EX-42` edit**, reads `RESULT: dead=0 guide=0 stale=64 stale_severity=report exit=2` (`20260903T200105Z_EX-42-precensus.log:103`, Status 2 at `:106`) — `dead=0` and `exit != 1`, the restoration condition the 10:30 review pre-registered. The physics anchors were never in question)* 
