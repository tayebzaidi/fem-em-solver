# OPS-34 — Measure the `ports:1` terminated-`Z` records against the 0.11 image; re-base the example's four `RECORDED_*` only if stale

 **Measure the `ports:1` terminated-`Z` records against the 0.11 image; re-base the example's four `RECORDED_*` only if stale** — `OPS-33`'s slot flagged that `examples/ports/01_two_torus_port_pair.py:143–160` still carries `RECORDED_RAW_RATIO` 0.894283 / `RECORDED_CORRECTED_RATIO` 0.939581 from the v0.7.2 `20260813T020352Z_PORT-1-step3bxviii-pairgate-n2.log` under a 2e-3 `RAW_REPRODUCTION_BAND` (symmetry 3.11213e-05 / spectral 0.861356895 were already re-recorded on 0.11 by `EX-30`, printed not asserted), and that the gate module `tests/validation/test_port_package_sparameters.py:94–95` carries the same two ratios. Whether they are stale is **unmeasured** — the 2e-3 band has passed on every 0.11 run, which bounds the gap below 2e-3 and says nothing finer. Executor `record-reconciler`. Step A: run `ports:1`, read the four misses. Step B, only if raw or corrected misses by > 1e-6: re-base the example's four constants at `metrics.json` precision with the v0.7.2 digits kept as `SUPERSEDED_V072_RECORD`, the reproduction control at 1e-6 relative (raw / corrected / ‖S‖₂) and 1e-6 absolute (symmetry), the superseded raw/corrected asserted to fail it (the `OPS-33` pattern). The gate module is untouched in either step — a test band and a test record are a review's call from step A's number. Done-when: `ports:1` exit 0 with the physics gates unmoved; the misses printed; after step B ≤ 1e-6 with the negative control biting; census `exit != 1`. If step A reads < 1e-6 the records are current and the item closes on the measurement. (1\*) licence, no physics band, no gate, no §2 claim. Negative result: a miss > 1e-6 that persists after re-basing is scatter above `EX-37`'s 5e-8 on this route — known-issues entry, band left, stop. Opened 2026-09-03 03:00 review, §9 item 5 (spare). **✅ done 2026-09-04 (19:30 slot), `record-reconciler`, three standard windows at `-n 2` complex, 143 / 142 / 143 s, Status 0 each.** Step A (`20260904T003215Z_OPS-34.log:651`): the records **are** stale — raw 0.894516 vs 0.894283 (miss 2.61e-4 relative), corrected 0.939822 vs 0.939581 (2.56e-4), both three decades over the 1e-6 trigger, so step B ran; symmetry 3.1121e-05 / spectral 0.861357 were already 0.11-era. Step B: a `repr()`-precision print added (this route has no `metrics.json`, unlike the package/d3 route `OPS-33` re-based from `ANS-3`'s), digits read off `20260904T003530Z_OPS-34.log:648` — raw **0.8945163788281**, corrected **0.9398215452105435**, symmetry **3.11213171925739e-05**, spectral **0.8613568943949574** (177 998 cells); all four `RECORDED_*` re-based to them, the v0.7.2 digits kept verbatim as `SUPERSEDED_V072_RECORD`, `RAW_REPRODUCTION_BAND` 2e-3 replaced by `REPRODUCTION_BAND_RELATIVE = 1e-6` (raw / corrected / ‖S‖₂) and `REPRODUCTION_BAND_SYMMETRY_ABSOLUTE = 1e-6`, and the symmetry/spectral prints promoted to assertions. Anchor re-run `20260904T004019Z_OPS-34.log:649–650,654`: the four misses **6.51e-10 / 6.38e-10 / 3.60e-11 abs / 1.85e-10** — i.e. measured run-to-run scatter on this route is ~1e-9, a decade under `EX-37`'s 5e-8, so the 1e-6 band has three decades of headroom and no negative-result clause fires; the negative control bites, the superseded raw/corrected missing the same run by 2.61e-04 / 2.56e-04; physics gates unmoved and green (corrected −6.02% inside the unmoved 10%, ‖S−Sᵀ‖/‖S‖ = 3.1121e-05 < 1e-3, ‖S‖₂ = 0.861357 ≤ 1). Census `dead=0 guide=0 stale=62 exit=2` (`20260904T004409Z_OPS-34-census.log:101`), `exit != 1`. `tests/validation/test_port_package_sparameters.py` untouched — its 2e-3 band and its two v0.7.2 ratios are now measurably 2.6e-4 stale and are **a review's call**; likewise the same digits in `src/fem_em_solver/ports/gap_voltage.py:31–32`'s docstring prose, out of the (1\*) licence. | ✅ 
