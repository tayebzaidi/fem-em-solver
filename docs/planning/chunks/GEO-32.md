# GEO-32 — Force a C4-congruent port-sheet cut by construction

 **Force a C4-congruent port-sheet cut by construction — do `GEO-31`'s cut asymmetry and `PORT-18`'s read-back lead vanish with it?** — opened 2026-09-10 10:30 review, ruling (1), on `PORT-18`'s exclusion of the `'+'`-side mechanism. `GEO-31` measured the four sheets as the same patch cut four ways (facet-centroid Hausdorff 0.44–0.99 ℓ, ×1 included). `PORT-18` then printed that the N1curl interpolant read-back spread of a smooth C4-invariant field tracks the rungs that broke — ×1 7.223213e-06 → ×0.75 3.520702e-04; `resolution` 0.012 3.139712e-06 → 0.0095 7.307614e-04 — with `GEO-31`'s sheet pairings (`20260910T093526Z_PORT-18.log`). Neither reading is causal until the cut is made congruent and the symptom is re-read. This chunk builds that lever and reads the geometry. **No solve.** Implementer (a `src/` keyword), real build. **Change:** an opt-in keyword `c4_congruent_sheets: bool = False` on `MeshGenerator.birdcage_port_domain`, threaded to `_build_birdcage_port_model`. It is valid only with `emit_port_sheets=True`, `leg_gap_length` set and `leg_count == 4`; otherwise raise `ValueError` naming the reason. When True, after the last `occ.synchronize()` that follows the fragment (`io/mesh.py:4139–4160`) and before `gmsh.model.mesh.generate(3)` (`:4563`), constrain sheets 2–4 to sheet 1 with `gmsh.model.mesh.setPeriodic(2, [s_k], [s_1], list(_z_rotation_affine(θ_k − θ_1)))`. Use the sheets' **post-fragment** surface tags (from `fragment_map`) and the snapped affine, so the copy is exact at 90°. With `False` no new line executes. New probe `scripts/probes/geo32_c4_congruent_sheet_cut.py` builds each mesh with `geo30`'s parameter set and `RUNGS`, and reads it with `geo31`'s `_measure_rung` and `port18`'s `_measure_rung` — import them, do not retype. **Anchors (asserted):** (A) *reproduction, flag off* — at ×1 and ×0.75 the flag-off build reproduces `GEO-31` to its printed digits (`20260910T003835Z_GEO-31.log:7270–7279`): `size_global` 116 085 at ×1 (the `GEO-19` record, imported unmoved 1% `CELL_COUNT_BAND`); facet counts 58/58/58/58 and 80/74/80/74; centroid Hausdorff d(P3) = 1.114339e-03 m at ×1 and 0.9897 ℓ on P2/P4 at ×0.75. (B) *congruence, flag on* — at both rungs the four sheets' facet counts are equal and every sheet's facet-centroid Hausdorff against the rotated sheet 1 is ≤ **1e-12 m** (≈ 5e-10 ℓ). For scale, `GEO-31`'s boundary-vertex Hausdorff, congruent by construction, reads ≤ 2.9e-14 m. **Negative control (asserted — separation from a same-fixture measurement):** the flag-off `max_i d_i` is ≥ **0.4 ℓ** at both rungs (`GEO-31`: 0.53 ℓ at ×1, 0.9897 ℓ at ×0.75), i.e. ≥ 8e8× the (B) bar. A keyword that silently does nothing fails (B); a metric that reads 0 regardless fails (A). **Printed, asserted nowhere (rule (e)):** per rung and flag — `size_global`, meshing time, sheet facet counts, sorted facet-area multisets, and `PORT-18`'s smooth-field read-back spread (`R^+`, q = 2). At ×0.75 that spread is **predicted** to fall from 3.52e-04 toward the ×1 class (≈ 1e-5) with the flag on. **Tier / ranks / cost:** window 1 builds ×1 and ×0.75, flag off and on — four meshes at `GEO-31`'s measured 25–37 s each plus facet passes (`GEO-31` 144 s for four meshes; `PORT-18` 154 s with interpolations) ⇒ `-n 2`, `timeout -k 30 500`, `-s`. Optional window 2: `resolution` 0.0095, both flags, `-n 2`, `timeout -k 30 300`. Regression: `tests/mesh/test_birdcage_port_sheets.py` at `-n 2`, `timeout -k 30 300`, green unchanged with the flag off. Standard by expectation, heavy by ceiling. **Traps already paid for:** `setPeriodic` takes **post-fragment** tags after the last `occ.synchronize()` — a pre-fragment tag names an entity the fragment deleted. gmsh matches the boundary curves itself under the affine and fails if they do not coincide; that failure is a finding, never something to loosen with a tolerance. Only the snapped `_z_rotation_affine` will do: last-bit coordinates move the cell count at 1e-3 (`GEO-19` step B). Facet tags, `size_local` and centroids are rank-local — gather to rank 0 before any Hausdorff (`GEO-31`, `OPS-40`). Every `SpatialCoordinate` facet form pins `quadrature_degree` (`POST-5`). No UFL comparison, and no `sqrt` inside one (`OPS-22`, `WF-6` 4c). DG0 indicators need ghosts via `scatter_forward()` (`PORT-18`). `timeout -k 30`; foreground; no `-k a or b`; sweep 0-byte cache stubs only. **Scope:** a mesher lever and a geometric reading. It does **not** solve, re-run any port gate with the flag, or change the default; it moves no record, band or rung status, and `ANS-4` step 2a / `WF-6` step 4f stay blocked. Whether a congruent cut restores the ×0.75 `Z` class spreads is the follow-on solve, which the review queues from (B) and the printed spread. **Negative result:** if gmsh refuses the constraint, the mesh fails, or (B) misses ⇒ print gmsh's message and the `d_i`, do not land the keyword, park on `attempt/*`, mark BLOCKED, stop; the review then weighs a mesh-one-sector-and-copy construction. If (A) misses ⇒ the generator is not reproducible run-to-run, which undermines `GEO-30`/`GEO-31`'s comparisons: report both readings, open a known-issues row, stop. If (B) is green but the printed spread is unchanged ⇒ the cut is excluded as the lead's carrier; record that, it is a finding. **MEASURED 2026-09-10 15:00 slot — the lever works, all anchors green, and the cut is the lead's whole carrier** (implementer; keyword `c4_congruent_sheets` in `src/fem_em_solver/io/mesh.py`, `setPeriodic` block immediately before `generate(3)`, so after the `as_hole` drop's `synchronize()` too; post-fragment sheet surfaces read off the positional out-map, one surface per sheet required or `RuntimeError`; new `scripts/probes/geo32_c4_congruent_sheet_cut.py`; **disclosed additive change:** `geo30._build_rung` gained a `c4_congruent_sheets=False` pass-through, the generator's own default, so `GEO-30`/`GEO-31`/`PORT-18` build the identical mesh). gmsh accepted the constraint on all three rungs built (`[birdcage-mesh] c4_congruent_sheets: post-fragment sheet surfaces [36, 43, 50, 57] bound to master 36`). **Window 1** (`-n 2`, ×1 + ×0.75, flag off/on, `timeout -k 30 500`) `20260910T200420Z_GEO-32.log` Status 0 / 127 s. **(A)** flag off reproduces `GEO-31` to the printed digit: ×1 `size_global` 116 085 (ratio 1.000000, 1% band), counts (58, 58, 58, 58), d(P3) = 1.114339e-03 m; ×0.75 counts (80, 74, 80, 74), d(P2)/ℓ = d(P4)/ℓ = 0.9897 (`:7442–7443`). **(B)** flag on: counts (58, 58, 58, 58) and **(80, 80, 80, 80)**, max d_i = **9.603429e-15 m** at both rungs against the 1e-12 m bar (`:7444, 7446`). **Negative control:** flag-off max d_i = 0.527683 ℓ / 0.989700 ℓ against ≥ 0.4 ℓ (`:7445, 7447`). **Optional window 2** (0.0095 rung, `timeout -k 30 300`) `20260910T200744Z_GEO-32.log` Status 0 / 81 s: (B) counts (70, 70, 70, 70), max d_i 9.603429e-15 m; control 0.506854 ℓ (`:3824–3825`); (A) has no record there and is not evaluated. **Regression** `tests/mesh/test_birdcage_port_sheets.py` `-n 2`, `timeout -k 30 300`, `-s`: `20260910T200641Z_GEO-32.log` 2 passed, Status 0 / 54 s. **Printed, asserted nowhere (rule (e)):** `PORT-18`'s R^+ q = 2 read-back spread, flag off → on — ×1 7.223213e-06 → **1.465268e-15**, ×0.75 3.520702e-04 → **1.344563e-15** (`20260910T200420Z_GEO-32.log:7438–7439`), 0.0095 7.307614e-04 → **9.775987e-16** (`…200744Z…:1801, 3690`). The prediction was ≈ 1e-5 at ×0.75; the reading is round-off, on the ×1 rung too, so **the interpolant read-back spread is carried entirely by the cut**. Sorted facet-area multisets agree to ≤ 1.02e-11 relative flag on (`:7251, 7373`). **One consequence for the follow-on solve, printed not ruled:** the constraint moves the *volume* mesh — `size_global` flag off → on 116 085 → 116 118, 161 695 → 161 645, 197 393 → 197 284 (`:7438–7439`; `…200744Z…:1801, 3690`) — so a flag-on build is **not** the `GEO-19` 116 085 record mesh, and any flag-on port gate carries its own cell count. Meshing time unchanged (22.86 / 22.48 s, 31.06 / 31.72 s, 35.52 / 34.91 s). No solve, no default change, no record/band/rung moved; `ANS-4` step 2a and `WF-6` step 4f stay blocked; whether a congruent cut restores the ×0.75 `Z` class spreads is the review's follow-on. | 🧪 **MEASURED 2026-09-10 15:00 slot** — lever landed opt-in, (A)/(B)/control green; measurement, no solve. **Accepted as measured by the 2026-09-10 18:00 review:** the cut carries the whole read-back spread; the solve follow-on is `ANS-4` step 2a′ (§9 item 1). **Follow-on solve measured 2026-09-10 19:30 slot (`ANS-4` step 2a′, §7 `ANS-4`):** with the flag on, ×0.75's `Z` class spreads fall from 0.5390 / 0.4591 / 1.6886 % to **0.0481 / 0.0649 / 0.0555 %** (`20260911T003204Z_ANS-4-step2a-prime.log:3913`), and the flag-off control reproduces step 2a's red to the digit (`20260911T003442Z…:3718`). The cut carried the `Z` break as well as the read-back spread 
