# ANS-2 — Coil-driven SAR in the loaded four-leg birdcage at 10 MHz: pointwise SAR, whole-phantom power, mass-averaged SAR

 **Coil-driven SAR in the loaded four-leg birdcage at 10 MHz: pointwise SAR, whole-phantom power, mass-averaged SAR** — the first benchmark that can attack the *absolute* SAR claim (every coil-driven SAR number we hold is a C4 symmetry identity, and a SAR wrong by a constant factor passes one exactly). **Commissioned 2026-09-09 02:15 weekly review** on §10's reserved trigger ("subgoal 4", fired by `MAT-4` steps 4 and 5b); authoritative spec `examples/ansys_benchmarks/birdcage_coil_driven_sar_10MHz/SPEC.md`. Geometry, materials, ports and boundary condition are byte-for-byte `ANS-4`'s — one AED session replicates it. Three scoping judgements are the weekly's, in the spec: the **shape-free** rows (pointwise SAR, whole-phantom power) adjudicate; the mass-averaged rows carry a pre-registered **sphere-vs-cube** systematic (ours averages over a sphere of equal mass, IEC 62704-1 and hence HFSS over a cube) so a rows-4–6 miss cannot read as a false disagreement; normalisation is **carried explicitly, never matched** (ours 5.0e-03 W incident, HFSS's default 1 W — the one error mode that passes every self-check on both sides). **Step 1 — the runnable half at SPEC rows 1–3, 7 and 8, scoped and queued by the 2026-09-09 03:00 daily review (§9 item 4)**: example script + same-stem guide through `./run_examples.sh`, combined-XDMF, `metrics.json`, and a `COMPARISON.md` regenerated by the script with the AED columns blank (`ANS-1`'s rule — a benchmark cannot drift from the gate), asserting only bands and records imported from `MAT-4`'s gate modules (the 1 g and 10 g C4 pairs at the unmoved 5% `C4_COVARIANCE_BAND`, the whole-phantom coverage identity, the rung's 199 920 / 58 866 cell records at 1%), with the mis-paired 1 g control at 87.01–87.06% as the asserted negative control (≈ 17× separation, `20260908T200629Z_MAT-4-step5b.log:1990–1993`). **Step 2** — SPEC rows 4–6 (mass-averaged and peak-over-phantom), held until rows 1–3 have adjudicated, per the spec. **Step 3** — the operator's AED replication; the case reaches the dashboard's Waiting-on-you when step 1 closes and **not before** (sending a spec whose runnable half does not exist wastes an AED session). Numbers private, verdict public, as every other case. **Step 1 EXECUTED GREEN 2026-09-09 09:00 slot** (`20260909T140558Z_ANS-2-step1.log`, `Status: 0` `:1929`, `Elapsed (s): 233` `:1930`, `-n 4`, heavy by ceiling): `examples/ansys_benchmarks/birdcage_coil_driven_sar_10MHz/02_birdcage_coil_driven_sar_10MHz.py` + same-stem guide, emitted by `./scripts/run_examples.sh -e ans:2 -n 4 -t 560 --dry-run` and run through the harness; `metrics.json`, `COMPARISON.md` (AED columns verbatim blank) and combined XDMF regenerated by the script. Readings — row 1 pointwise SAR 4×4 at `:1902–1905` (diagonal 5.878191741e-07 / 5.079232898e-07 / 5.287007744e-07 / 5.763100136e-07 W/kg); rows 2–3 whole-phantom power 5.503204729e-08 … 5.496862019e-08 W and average SAR 2.435069030e-07 … 2.432262495e-07 W/kg at `:1907–1910`; row 7 excitation at `:1911–1915` — **incident 5.0000000e-03 W printed explicitly against HFSS's 1 W, carried never matched**, accepted Re P ≈ 2.1052e-03 W, `|Im P|/Re P` ≈ 0.337 printed and never gated; row 8 metadata at `:1916` (199 920 cells / 58 866 tag-3, four drives 18.15 + 17.59 + 17.06 + 17.41 s). Anchors at `:1918–1923`, every band and record **imported** from `MAT-4`'s gate modules and none restated or moved: the four 10 g C4 pairs 0.0309 / 0.0060 / 0.0394 / 0.0644% and the four 1 g pairs **0.0957 / 0.1199 / 0.1305 / 0.1065%** — reproducing `MAT-4` step 5b (`20260908T200629Z_MAT-4-step5b.log:1990–1993`) to the digit — asserted ≤ the unmoved 5% `C4_COVARIANCE_BAND`; the coverage identity power **7.771561e-14** / mass 5.284662e-14 asserted ≤ 1e-10; the two mesh records 199 920 / 58 866 at ratio 1.000000 asserted inside 1% `CELL_COUNT_BAND`; negative control, the mis-paired 1 g reading **87.0143 / 87.0546 / 87.0506 / 87.0592%**, asserted ≥ 10× the band (≈ 17× measured). No imported gate went red, so the negative-result clause did not fire. Docrefs census `dead=0 guide=0 stale=87 exit=2` (`20260909T141114Z_ANS-2-step1-census.log:126`), the informational stale-only code — `stale=87` is `OPS-42`'s business. One executor judgement, disclosed: the control is asserted as a **floor** (10 × the imported band, i.e. ≥ 50%) rather than against step 5b's measured 87.0x%, because an example must not re-record a gate digit (`ANS-1`'s rule); the measured value is printed, in `metrics.json` and in `COMPARISON.md` with its provenance. **Nothing absolute is licensed:** §2's absolute-SAR clause does not move, the row stays 🟡 pending step 2 (SPEC rows 4–6) and step 3 (the operator's AED replication), and the case now qualifies for the dashboard's Waiting-on-you. | 🟡 *(spec written 2026-09-09; **step 1 runnable half executed green 2026-09-09**; steps 2–3 open)* 
