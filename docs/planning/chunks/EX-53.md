# EX-53 — Mass-averaged 10 g SAR on the coil-driven field in ParaView

 **Mass-averaged 10 g SAR on the coil-driven field in ParaView — four C95.3 averaging balls on the loaded F-small birdcage, the 4×4 of 10 g averages beside the pointwise map, the C4 covariance identity asserted at the imported band** (`MAT-4` step 4's newly gated capability, 2026-09-06 — the **output-quantity** angle: `mri:2` shows the averaging operator on an *imposed* uniform field, `ports:9` the coil-driven quadrant *powers*; no example shows a mass-averaged SAR on a *solved coil field*, nor the ball placement that makes the C4 identity a statement; opened 2026-09-07 03:00 review, §5.4 ramp — the example `MAT-4` step 4 owed the day it landed). Executor `example-runner`, spawned foreground (the spawn prompt states verbatim that the runner ends no turn while a window is running — the 22:30 slot's runner did and was killed), both census windows through the harness; complex build. `examples/mri/03_birdcage_mass_averaged_sar.py` / `.md` (`mri:3`): the four single-drive solves on the finer-phantom rung exactly as the gate builds them — every constant imported from `tests/validation/test_birdcage_sar_mass_averaged.py` (`CENTRE_RADIUS_M`, `WHOLE_PHANTOM_BALL_RADIUS_M`, `EXACT_IDENTITY_RTOL`, the ball masses, the quadrature degree, the C4 band through its import chain); if the module's solve helper does not expose the solved fields and ball geometry to a caller, one additive keyword under rule (a), gate re-run green in the same slot (132 s at `-n 4`). **Anchors (asserted, imported, never restated):** the four cyclic 10 g C4 pairs ≤ the imported 5% band (records 0.3303 / 0.0756 / 0.0574 / 0.3132% printed beside, not asserted); the whole-phantom ball reproduces the tagged `½∫σ\|E\|²` at `EXACT_IDENTITY_RTOL`; the four balls lie inside the phantom as the gate asserts it. **Negative control (asserted — backed by the gate's own measurement of the same comparison on the same fixture, `20260907T003548Z_MAT-4-step4.log:1943–1946`):** the mis-paired 180° centre reads above the band (sign asserted; its ~86% size printed as predicted, ceiling 100%). **Printed, not gated:** the 1 g column (verdict (b), the gate's own), the per-drive 10 g peaks labelled "not a C95.3 compliance figure" in the log and the guide. **Writes** one combined XDMF `mri_03_birdcage_mass_averaged_sar_combined.xdmf`: the P1-drive pointwise `SAR` (DG0), a DG0 integer `AveragingBall` field (0 outside, `k+1` inside 10 g ball `k`; the whole-phantom ball is the phantom and is not written), and `CellTags` — the guide's ParaView recipe thresholds on `AveragingBall` to show the four balls sitting in the SAR map. **Tier / ranks / cost:** the gate's four solves + 21 operator calls measured **132 s at `-n 4`** (`20260907T003548Z_MAT-4-step4.log:2260`) ⇒ `./scripts/run_examples.sh -e mri:3 -n 4 -t 600`, standard by host-runner window, predicted ≈ 150 s. **Traps already paid for:** `evaluate_vector_field_parallel` / `point_sar` are collectives over an identical point list on every rank (`EX-52`, `20260907T033551Z_EX-52.log:400`) — build any point list globally; `build_density_field` accepts a mapped `0.0` since `ff50ce0`; `mass_averaged_sar` at the imported degree 16, not the library default 12 (`mri:2`'s measured insufficiency); the census before any file is written. **Scope:** demonstrates the gated 10 g identity from the output-quantity angle; no absolute SAR, no compliance, no 1 g gate, no Larmor, no convergence; §2 unchanged. **Negative result:** any imported anchor missing through the example path while the module holds it is an example/test divergence — known-issues entry, stop. **✅ 2026-09-07 (09:00 implementer slot, `example-runner` foreground, first run).** `examples/mri/03_birdcage_mass_averaged_sar.py` / `.md` (`mri:3`) ran green at `-n 4` in **91 s** harness-wall / 87.9 s in-script (`20260907T140658Z_EX-53.log`, Exit 0, predicted ≈ 150 s). The gate reproduced exactly through the example path — 120 499 cells / 2 746 tag-3, four solves + 21 operator calls in 86.4 s (`:log tail`). Anchors, all **asserted on the module's imported constants, never restated**, all held: anchor (i) the whole-phantom ball **5.587038273e-08 W** vs the tagged `½∫σ|E|²` at **1.688e-14** relative (budget `EXACT_IDENTITY_RTOL` 1e-10) and vs step 3f's own record at 5.403e-11 (budget 1e-03); containment `r₀ + a₁₀g = 28.3650 mm < 30.0 mm`; anchor (ii) the four cyclic 10 g C4 pairs **0.3303 / 0.0756 / 0.0574 / 0.3132 %** against the imported unmoved 5.0 % band, reproducing the `MAT-4` step 4 records to the printed digits. Negative control asserted per rule (e): the mis-paired far-side centre reads **86.0132 / 85.9249 / 85.9671 / 85.9582 %** on the four drives — sign asserted `>` band and `≤` the 100 % ceiling, size printed as predicted (~90 %). Printed, not gated: the 1 g column (6.8383 / 2.9297 / 0.4116 / 4.7159 %, verdict (b) unchanged), the kernel-mass errors, and the four per-drive 10 g peaks (~6.33e-07 W/kg) labelled "not a C95.3 compliance figure". One combined XDMF written (`mri_03_birdcage_mass_averaged_sar_combined.xdmf`: pointwise `SAR`, the DG0 integer `AveragingBall` indicator, `CellTags`). One **additive** change to the gate module under standing rule (a): the module-scoped fixture body lifted verbatim to `_build_mass_averaged()` with the fixture reduced to a one-line wrapper, plus four additive return keys (`mesh`, `cell_tags`, `rho_field`, `solves`) — nothing removed or renamed, re-run green in the same slot at **23 passed / 113.87 s** (`20260907T140233Z_MAT-4-step4-rerun.log`). No band moved, no `src/` change. **Disclosed deviation:** the pointwise `SAR` field written to the XDMF weights by the phantom's physical `PHANTOM_RHO_KG_PER_M3`, not the gate's zero-elsewhere `rho_field` (which would divide by zero outside the phantom and exists only as a ball-mass denominator) — the same choice the gate's own `tagged` anchor makes, documented in the script docstring and guide §3 step 6. Census through the harness both sides: `dead=0 guide=0 stale=81 exit=2`, **46 → 47** runnable, `guide=0` on the new pair (`20260907T140219Z_EX-53-census-pre.log`, `…140926Z_EX-53-census-post.log`) — the `stale` drift from the 03:00 review's 79 is age-based on `time_harmonic` artifacts, present *before* any file of this chunk was written and not attributable to it (the weekly's `EX-30`-class refresh). *Audited PASS, 2026-09-07 10:30 review, at `1b8d8e6`:* all four logs footered on header `f700f5e` = the parent; 25 of 25 digits traced (`20260907T140658Z_EX-53.log:1858, 1865–1868, 1875–1879, 1882–1887, 1897–1902`; `…140233Z_MAT-4-step4-rerun.log:2126`; both census logs `:118–120`); the anchors are executed asserts in the script (`03_birdcage_mass_averaged_sar.py:220, 249, 254, 280, 284, 289`) on constants imported from the gate module, which the example imports via `_build_mass_averaged` rather than re-implementing; the gate diff is the verbatim fixture lift plus four additive keys, nothing renamed; no `src/`; 91 s inside its declared 600 s host-runner window. Re-traced by this review: `:1865` `k=0->1  10 g    0.3303%   1 g    6.8383% (PRINTED NOT GATED)`, `:1878` `5.587038273e-08 W vs tagged integral 5.587038273e-08 W [1.688e-14 relative]`. | ✅ 
