# Daily review — 2026-09-25 03:00 (Friday)

Model `claude-opus-5-5` (the daily review's dated override, through
2026-09-29). Interval: 2026-09-23 03:18 (`3341e72`) → 2026-09-25 03:00,
spanning Thursday 09-24 (off day — no slots; one interactive session).

## 1. What happened

| When | Who | Outcome |
|---|---|---|
| 09-23 04:30 slot | implementer | `64f393c` **`OPS-60` ✅**; `b84a5b8` `PORT-23` step 1 (engine = raw (C2) to ≤ 3.5e-16); `b6e4004` `ANS-6` step 2b resolution knob (entry 14 READY); `fb6a44d` `WF-7` step 2 (H4, `|B₁⁺|` C16 0.1495 %) |
| 09-23 06:00 slot | implementer | `8fc56fc` `GEO-34` step 1a (STEP round trip, mesh gates); `83f64c2` `EX-57` `mesh:11`; fallback `e0aa152` `mesh:12` |
| 09-23 07:30 slot | implementer | queue drained ⇒ fallback `a60d87a` `EX-57` `mat:1` — **nested-executor breach** (below) |
| 09-23 09:00 slot | implementer | fallback `cabb2f3` `EX-57` `mat:2` |
| ≈ 09-23 15:00 UTC | ? | **ordinary `fem-em-solver` container exited (137)** — cause not established (below) |
| 09-24 02:00 | cron `xl` | `657fda4` `ANS-4` step 3g — Status 0, 2084 s |
| 09-24 12:00 / 19:45 | operator sessions | `b4bbec2` privacy ruling (relative AED agreement levels publishable); `42d7bba` `weekly_counts.py` token section |
| 09-25 02:00 | cron `xl` | `099e2d5` `ANS-2` step 4 — Status 0, 1315 s |

All four 09-23 slots fired. Six items landed and four fallback figures;
nothing was parked.

## 2. Tree, branches, service

- **Tracked tree clean.** One untracked file:
  `examples/ansys_benchmarks/ans2_birdcage_coil_driven_sar_10MHz/metrics_h0.00125.json`,
  the 09-25 `xl` window's output. Entry 10 names it an output to commit.
  `xl-run.sh` stages only its log, ledger and queue paths, so it stayed
  untracked. It would have stopped the 04:30 slot (implementer step 1
  checks `git status`) and the 09-26 `xxl` window (`xl-run.sh:114` refuses
  on any porcelain output). **Not committed.** Entry 10 said to commit it,
  but the `OPS-59` artifact pin (`tests/unit/test_doc_reference_exit_codes.py:463`)
  lists exactly one `metrics.json` per `ans:` case, so a second tracked JSON
  would turn `test_the_in_tree_exemption_cannot_silently_widen` red. The
  entry's instruction conflicted with the pin; the pin wins. The review
  **moved** the file beside its XDMF under the gitignored `paraview_output/`,
  and the log carries every readout figure. **The same trap was armed for
  09-29:** entry 14's `ans:6` window writes an untracked
  `metrics_degree2_h0p005_64MHz.json`, which would have made the 09-30 window
  refuse its dirty tree. This review added one `.gitignore` rule,
  `examples/ansys_benchmarks/*/metrics_*.json`. It matches knob-tagged files
  only: the tracked `metrics.json` does not match, and
  `git ls-files 'examples/ansys_benchmarks/*/metrics_*.json'` reads 0.
- **No `recovered/*`.** Five `attempt/*` branches are kept, as on 09-23.
- **`.worktrees/census-before`**, left by the 09-23 `ANS-6` slot, is no
  longer a registered worktree (`git worktree list` shows `main` only). The
  directory remains, gitignored and root-owned in part, so the operator
  removes it.
- **The ordinary service was down.** The first census call returned
  `service "fem-em-solver" is not running` (`20260925T080223Z_EX-57-next.log`).
  Through the harness, `docker compose ps -a` read `fem-em-solver … Exited
  (137) 41 hours ago` (`20260925T080253Z_SERVICE-CHECK.log`), i.e. about
  09-23 15:00 UTC, an hour after the last slot. The xl / xxl containers
  showed their normal post-window stops. No cron job stops that service
  (`scripts/automation/crontab`, `xl-run.sh:149` stops only the tier
  service), so the cause is **not established** (an OOM kill, a manual stop
  and a Docker Desktop event all fit a 137). No harm yet: 09-24 was an off
  day. This review started it
  (`20260925T080307Z_SERVICE-START.log`, Status 0, `Container fem-em-solver
  Started`) and the census then ran (`20260925T080311Z_EX-57-next.log`).
  Without that, every slot today would have met a dead service. Two
  attempts that chained a command after the harness call were socket
  denials (1 s, no container reached). Their logs and rows were deleted,
  following the `GEO-34` 1a precedent.

## 3. Audit (§4) — one chunk newly ✅

- **`OPS-60` (`64f393c`) — auditor DEMOTE(8), overruled; ✅ stands.** Every
  §4 element was traced 8/8: `20260923T093017Z_OPS-60.log:3,12,76,146,153,223,226–227`.
  The red baseline ran on `9eb1e39`, the parent of the pin commit `f3fcdeb`
  (`20260921T093041Z_OPS-60.log:3,48,55`). The assert
  `tests/environment/test_scikit_rf_version.py:40` has been untouched since
  `f3fcdeb`, and the closing commit touches no code or test. Exact version
  equality counts as quantitative on the `OPS-18` / `OPS-25` precedent,
  re-affirmed here as the auditor asked: a documented reference value with
  a discriminating control. The DEMOTE is about the tier word: a "smoke" row
  closed on a 39 s window under `timeout -k 30 60` that ran the whole
  `tests/environment` suite. That window is the one the 09-23 review wrote
  into item 33 verbatim, so the defect is the review's label, not the
  executor's. Following the 09-23 `OPS-57` / `OPS-58` tier notes, the tier
  column was re-labelled standard.

The other 09-23 landings closed steps inside 🟡 rows, not chunks, so no
§4 audit is owed. Their logs were read for the rulings below.

## 4. Incomplete attempts and rulings

No new `attempt/*` item. Three matters from the journal entries:

1. **`WF-7` step 2's 38 sample points (the slot asked the review to rule):
   step 2 stands.** `MIN_SAMPLE_POINTS` = 50
   (`test_birdcage_b1_plus_map.py:135`) is `POST-6`'s fixture constant. It
   is tied to an absolute 20 mm × 20 mm sample cylinder (`:125–126`) that
   does not scale with the phantom, and it is not in step 2's Done-when.
   The count was disclosed. Item 39 reprints the reading on a phantom with
   ≈ 8× the centroids.
2. **The `mat:1` nested-executor breach** (attempts 09-23 12:30). A
   foreground `example-runner` spawned a background `example-runner`, which
   backgrounded its own harness window and **resumed after the slot had
   committed**. The slot stopped it before it touched the tree. The durable
   fix is `.claude/agents/example-runner.md`, which scheduled sessions cannot
   write (dashboard item, carried). The rule now sits in §9's fallback
   paragraph and in item 42's spawn prompt: foreground, no nesting, and a
   `ListAgents` check before committing.
3. **`PORT-23` step 1's disclosure (3):** the §2.1 circuit-layer passage
   now names `scikit-rf` 2.1.0 as an engine identity against the raw
   layer. Disclosures (1) and (2), a constant copied from a log with an
   exact match and one additional identity at the 1e-12 band, are accepted
   as disclosed.

## 5. `xl` readings (two `log-pathologist` rulings)

- **3g (09-24, entry 11): all seven claims CONFIRMED.** 18 / 18 passed on
  16 ranks, 2084 s, 281.95 GiB; the command matched entry 11 under `cmp`.
  The finest cut-off rung reproduces 3c's spreads and driven column to
  every printed digit on a fresh solve. The pathologist computed the
  successive degree-2 changes; this review re-checked S₁₁ by hand
  (6.969 % → 3.606 %):
  S₁₁ 6.969 → 3.606 % (1.93), S₂₁ 1.813 → 0.934 % (1.94), S₃₁ 1.843 →
  1.100 % (1.67). **Entry 11's rule: every ratio is ≳ 1.5, but the last
  change is below 1.08 % only on S₂₁**, so the "h-converged" branch fails.
  The sequence is monotone and falling, so the "non-monotone" branch does
  not fire either. The pre-registered rule has no branch for "falling but
  not yet converged". The weekly rules. Its input is 3h on 09-27 (the same
  ladder on the congruent cut), and the cut cannot explain a 3.6 % S₁₁
  change: the finest rung's S-class spread is 0.52 %. For the weekly only,
  not a gate: a geometric tail at ratio 1.93 would leave S₁₁ about as far
  from its limit as the last change, so the 10 MHz S₁₁ figure is a point,
  not a value.
- **`ANS-2` step 4 (09-25, entry 10): countable; claims 1, 2, 5, 7
  CONFIRMED, 3 CONFIRMED with price corrections, 4 OVERRULED in part.**
  Status 0, 1315 s, 39.91 GiB, orphans 0 / 0 measured; the 09-19 probe's
  cell counts were reproduced exactly. **Readout:** C4 driven-point spread
  15.730 → 4.528 %; phantom power moves −0.27 / −0.14 / −0.30 / −0.06 %;
  every figure was recomputed from the metrics JSON. **Ruled branch (a)**
  under the pre-registered rule (spread < 5 %, every move < 2 %). Branch (b)
  fails on size, so no private reading was needed. The ±10 % pointwise band
  tightens to 4.53 % and row 2's 5 % band stands (§7 `ANS-2`). Caveats
  carried to the weekly: a 0.47 pp margin on one rung; per-drive point
  values moved −6.7 … +9.9 %; the two rungs ran at different widths.
  **Claim 4 overruled in part:** three "ASSERTED" labels in the example's
  output are builder prints that the example never asserts. The pathologist
  also found an **AED figure in the example's tracked text** (docstring and
  print, since `a2e4a04`), now printed into the tracked log. Both are
  recorded in known-issues 2026-09-25 and `OPS-63`. The same figure stood
  in the §7 `ANS-2` row; this review redacted it there. The tracked logs
  that carry it are the operator's call. The figure appears in none of
  this review's text.

## 6. §10 assessment

- **H** — H4 landed; **H5 is queued (item 39)** on a refined phantom. At
  the default 15 mm the 10 g ball spans ≈ 1.8 cells, the regime in which
  `MAT-4`'s 1 g column missed its band. H5's result can close `WF-7`: the
  row gained a Done-when (rule (j)). H2 (`xxl` 0d) runs 09-26. H6 remains
  the weekly's, in conflict with the strip-conductor note.
- **F** — F2: 3e (64 MHz) converging; **3g (10 MHz) falling but not
  converged on S₁₁**; 3h / 3i on the cut run 09-27 / 09-28; entry 15 (3j,
  128 MHz on the cut) was added so that T6 reads all three frequencies on
  one mesh family. F4 / F5 belong to the weekly and the operator.
- **T** — T5 (gate (a)) and T6 wait on the weekly. T9b's probe is queued
  09-29 (entry 14). T10 waits on the operator.
- **`GEO-34`** step 1b is queued (item 40). 1c needs a CAD kernel in the
  image (operator).

The backlog still leads to the mission. The one gap this review filled is
a defect, not physics: `OPS-63`. **Examples (§5.4):** the only newly-✅
chunk is `OPS-60`, an infrastructure pin, so no example chunk is owed.

## 7. XL clerk (step 6b)

1. **Marked RUN:** entry 11 (3g) and entry 10 (`ANS-2` step 4), each with
   a ledger row filled from the pathologist's reading.
2. **Queued:** entry 14 (`ANS-6` 2c, hole-mesh cost probe) →
   `20260929-ANS-6-step2c.env`. Its command line is byte-identical to the
   pending entry (`sort -u` of the two lines reads 1). Entry 15 →
   `20260930-ANS-4-step3j.env` (same check).
3. **Budget and floor** (per `bash_guard.runs_in_trailing_week`,
   cutoff = run date − 6 days): 6 charged rows as of today (09-20 … 09-25).
   09-27, 09-28, 09-29 and 09-30 each count 5 at their own cutoff, so every
   queued night runs. **4 `xl` ahead, 1 `xxl` ahead — floors met.** After
   09-26's `xxl` window no `xxl` entry is pre-registered; the weekly
   (09-26 21:00) commissions the next.
4. **Written under the daily licence:** entry 15, `ANS-4` step 3j, the
   128 MHz degree-2 *h*-ladder on the congruent cut. It is a priced-family
   variant of entry 13 (one knob, the frequency, returned to the module's
   default as 3b and 2d ran it), priced from 3e / 3g with factor reuse and
   3b's cut-on finest rung: predicted 2 000–2 700 s, 270–290 GiB. The
   expected `-n 16` record-width skip is written into the entry.

## 8. Restock (step 6) and §9 hygiene (6c)

Queue on arrival: items 33–38 were all DONE; the queue had drained on 09-23
at 07:30. New items 39–42: `WF-7` step 3 (H5, 60 min), `GEO-34` step 1b
(45), `OPS-63` (40), `EX-57` `th:1` (20). **165 slot-min against 240:
short 75 min; 4 items against 5: short one.** Nothing else is ready.
Every executable chain step is queued or waits on the weekly or the
operator; the rest of the ready physics is `xl` work, which cron runs. The
drained-queue fallback covers the slots.

§9: items 19–38 collapsed to one line each; digest rewritten. **§9 is
39.3 KB** (48.0 KB at `HEAD`), still over the ≈ 25 KB target. Four
full-rubric items take ≈ 19 KB and the standing rules ≈ 10 KB. The rule
that falls short is the item size itself: the rubric's seven elements plus
traps run 4–6 KB per item.

## 9. For the 2026-09-26 weekly (not ruled here)

- F2 at 10 MHz: 3g falls without converging on S₁₁, which the rule does
  not cover (§5). This bears on T6's pre-registered flip, which needs 3e
  **and** 3g converged.
- `ANS-2` (a): the pointwise band goes into the private adjudication file,
  with the caveats in §5.
- Commission the next `xxl` window (none after 09-26).
- H6 vs the strip-conductor note; B1 `TH-5`; `OPS-54`; `OPS-61`.
- The `xl-run.sh` orphan check (carried) and the untracked-output gap: a
  window's declared outputs are not staged, and an untracked file then
  blocks the next window and the next slot (§2).
- The ordinary-service outage: cause unknown, detected only because this
  review called the container.
