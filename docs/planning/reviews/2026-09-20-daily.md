# Daily review — 2026-09-20 03:00 (Sunday)

Model `claude-fable-5-1`, no override. Interval: 2026-09-19 03:16
(`6ff44d4`) → 2026-09-20 03:00. First review to write its journal here
rather than into PROJECT_PLAN.md §9 (daily-review.md step 6c, 2026-09-19).

## 1. What happened

| When | Who | Outcome |
|---|---|---|
| 09-19 04:30 slot | implementer | `32f4eae` `ANS-2` step 4 cost probe fired its STOP rule (566 s, 40.89 GiB at `-n 8`) → re-priced to `xl`; `f085e51` / `941fa15` `TH-17` step 1 part (i): probe cheap (47.8 s, 2.18 GiB) but no physical eigenvalue near 64 MHz, item blocked; `99356d8` **`OPS-52` ⬜ → ✅** |
| 09-19 06:00 slot | implementer | `99d99e0` `OPS-50` step 2 (two instruments, negative branch, 🟡 held); `da9db81` **`OPS-53` ⬜ → ✅**; `997a5fe` / `f96502f` `EX-57` re-render legs A and B (14 figures; clipped-legend entry retired) |
| 09-19 07:30 slot | implementer | `6f75424` `mesh:6` figure + a tag-label fix (`201–204`); `32f28d8` `mesh:7` (fallback). Census 36 → 34 |
| 09-19 08:17 | operator session | `7eac273` queued `TH-17` step 1b and `ANS-2` step 4a; pre-registered `xl` entry 10 |
| 09-19 09:00 slot | implementer | `a2e4a04` `ANS-2` step 4a — resolution knob; entry 10 READY |
| 09-19 evening | operator session | `ANS-3` AED numbers landed (private); harness / launcher / XL-isolation fixes (`38a0791`, `aaba4ef`, `35fef09`); `OPS-54`…`OPS-58` opened from an external code review; §9/§10 current-state rules, `START_HERE.md`, known-issues rotation |
| 09-19 21:00 | weekly review | `ANS-3` / `ANS-4` adjudicated, `PORT-20` / `-21` / `-22` opened, chains T / F / H enumerated, `ANS-6` commissioned, `xl` entry 11 written; six flags for this review |
| 09-20 00:20 | operator session | `6085924` `GEO-33` row: the 32-leg rung models the coil as Leontovich sheets |
| 09-20 02:00 | cron `xl` | `bce7df6` `ANS-4` step 3d — 16 passed, Status 0, 1619 s |

Four slots fired, all did chunk work, nothing parked. The 09:00 slot left
one open item (`TH-17` step 1b, 50 slot-min) — the queue this review found.

## 2. Tree and branches

Clean at review start. No `recovered/*`. The same four `attempt/*` branches
(`TH-15-step2proper`, `WF-6-step4b/4c/4e`), kept on the 2026-09-09 18:00
ruling. Nothing to clear.

## 3. Audits (§4) — two status changes to ✅, two `auditor` reports, re-cited

- **`OPS-52` ✅ (`99356d8`) — PASS.** `20260919T095615Z_OPS-52.log:51,60,64`
  (recomputed move `5.5008 %`; source-text negative control 1 at the pinned
  `19bed9a9` → 0 in the tree; 5 passed, 1.11 s — lines re-read by this
  review); `20260919T095635Z_OPS-52.log:10428,10437` (`S_driven(P17)`
  `0.407423+0.344417j` … `ASSERTED`; Elapsed 154 s — re-read). The assert is
  executed (`scripts/probes/wf7_step0_f_human_cost.py:405`), and
  `S_DRIVEN_STEP0_RECORD`, the cell band and every solver call are absent
  from the diff; the two re-dated bracket dicts feed prints only. No `src/`.
  Log headers carry `19bed9a9`, the closing commit's parent.
- **`OPS-53` ✅ (`da9db81`) — PASS, with a finding that outlived it.**
  `20260919T111004Z_OPS-53.log:68,74` (7 passed on both ranks, Status 0 —
  re-read), `…110950Z:51` (`-n 1`), `…111013Z:89` (census
  `examples=54 ok=18 missing=36 broken=0` — re-read; identical to the
  pre-state `20260918T140725Z_EX-57.log:89`), `…111033Z:952–963,975–977`
  (`mesh:5`'s `GEO-17` digits identical, 12 s). The guard is
  `write_setup_figure`'s first statement, ahead of `figures_enabled` and the
  `comm.gather` — collective by placement; the diff is additive (47 / 0) and
  touches no drawing path. The one red among its six logs
  (`…110923Z`, `142 == 150`) was the synthetic control string being too
  short, fixed by lengthening the string. Auditor caveats kept: the "18 call
  sites" figure is asserted but never printed; no test drives
  `write_setup_figure` itself over-length under `mpiexec -n 2`.
  **The finding:** `EXPECTED_EXAMPLE_CALL_SITES = 18` was true at `da9db81`
  and is false on `main` (20 since `mesh:6` / `mesh:7`) — see ruling 2.
- Not audited: `ANS-3`'s verdict (the weekly's act, §5.4); `EX-55` (glyph
  correction only — closed 09-14, audited PASS that day,
  `plan-archive.md:30687`); `OPS-50` (🟡).

## 4. Rulings

1. **`OPS-50` — the re-record is licensed.** Lines re-read by this review:
   A `4.532338e-01` and E `2.175825e-01` are identical in all three windows
   and both instruments (`…probe4.log:398,400`, `…probe4-repeat.log:398,400`,
   `…probe5.log:393,407`); B reads `4.717160e-01` / `4.717154e-01` /
   `4.717141e-01` (`:399`, `:399`, `:400`). The pre-registered 1e-6
   cross-instrument anchor failed on B only, and the slot showed the failure
   is inside each instrument's own repeat spread. One further reading: B's
   *source* `rms_src` already differs between the two step-4 windows
   (`3.739430e-06` vs `3.739427e-06`, `:385` in each) while A's and E's do
   not — the spread is in the projected `B` before either probe samples it.
   The anchor's purpose was to show the two instruments are one instrument;
   A and E show that exactly, and B's residue is attributed upstream of both.
   No bound is loosened: `PIN_REPRO_RTOL` stays 0.02 and the spread is
   5 000× inside it. `record-reconciler`'s drift gate forbids it the job, so
   the implementer does it once under this licence (item 15); the row's
   Done-when (b) is amended under rule (j). Step 4's `REFUTED` verdict is
   unchanged in all three fields on both images — no `POST-4` conclusion
   moves. The B-nondeterminism entry stays open; its `-n 1` / `-n 2` window
   is still not queued because no status rides on it.
2. **A second count pin is stale on `main`: `test_setup_figure_title.py:43`.**
   Red *by inspection* — this review tried to execute the module and the
   docker socket was denied in its sandbox
   (`20260920T080639Z_OPS-53-callsite-pin.log`, Status 1, 0 s, no test ran;
   the row is kept — the index records attempts). The cause is this
   review's own 09-19 item text ("18 call sites expected"). Known-issues
   2026-09-20; folded with the weekly's flag 5 (the `OPS-44` artifact pin)
   into **`OPS-59`**, item 16, whose first window records the red or refutes
   the entry. `OPS-53` stays ✅: its closure was true at its commit and its
   purpose (the title guard) is intact.
3. **`xl` 09-20, `ANS-4` step 3d — banked as a record** (`log-pathologist`:
   CONFIRMED countable, command CONFIRMED byte-identical to entry 5; lines
   `:4485`, `:4824`, `:4858`, `:4891–4893`, `:6033–6040` re-read). 16 passed,
   0 skipped, 3 827 268 dofs, `memory.peak` 281.1 GiB, 1619 s. On the cut at
   64 MHz the class spreads are 0.0132 / 0.0126 / 0.0081 % (≈ 15× tighter than
   step 3's) and the two-rung move 6.50 / 2.61 / 4.12 % sits within 0.012
   points of step 3's 6.51 / 2.60 / 4.11 %. By entry 5's public rule the cut
   is not a degree-2 systematic at 64 MHz either — offered to the 09-26
   weekly, which rules. **One correction the pathologist forced:** no `xl`
   log can support "orphans 0 before / after" — the harness check is silent
   when clean and nothing checks after the window. The new ledger row says
   so; the three earlier rows' wording rests on the same silence. A one-line
   echo in `run_and_log.sh` would fix it; not queued today (no status moves),
   named on the dashboard.
4. **The weekly's flags, disposed.** (1) chain steps queued in its order —
   items 10–14. (2) entries 11 and 10 queued for 09-24 / 09-25, **3g first**
   so a budget-denied night still leaves it run before the 09-26 weekly.
   (3) `EX-55` glyph fixed; index regenerated. (4) `ANS-3` / `ANS-4` rows
   carry the verdicts in this review's wording; §2.2 already did (`:299–307`).
   (5) → `OPS-59`. (6) dashboard.
5. **A privacy note, handed on, not acted on.** `ANS-3`'s row (written by
   the 09-19 operator session) states a relative agreement level against the
   external code. Same class as the open `ANS-2` question on the dashboard;
   this review redacts nothing and repeats no level.
6. **No parked work, no family near its cap.** `TH-17` step 1 family: one
   attempt since opening, 1b is the second.

## 5. §10 assessment

No gap the daily may fill beyond the chains. Slot-executable chain steps
open: T5, T5′, T9 (T9b after it), F1, F3, H3–H6 (serial on the 09-21 `xl`
price). All five that are serial on nothing are now queued. H3 is next
review's to write once `WF-7` step 0c's ledger row exists. **Example step
(§5.4):** `OPS-52` is labels, `OPS-53` a guard — no capability, no example
chunk.

## 6. Restock

Nine open items, **325 predicted slot-minutes** (floor 240, ≥ 5 items: met).
Not queued, and why: `TH-19` step 4 (T6 — waits on the 09-26 ruling); H3
(needs H1's price); `OPS-54`…`OPS-58` (the external-review chunks — real,
smoke-tier, and next in line after the chain items; five more items today
would push the queue past two active days and they carry no chain date);
the frozen families; the B-nondeterminism window; the harness orphan echo.

## 7. XL clerk

`xl` ledger gained one row (09-20) — columns filled, entry 5 → `RUN`.
Trailing 7 days: **4 of 6 windows used** (09-16, -17, -18, -20). Queued
today: entry 11 → `20260924-ANS-4-step3g.env`, entry 10 →
`20260925-ANS-2-step4.env`, both checked line-identical to the pending
text. **5 `xl` ahead (09-21 … 09-25), 1 `xxl` ahead (09-26) — floors met**,
nothing written under the daily licence. Arithmetic note: on 09-24 07:00Z
the 09-17 row (07:00:08Z) is seconds from ageing out; if the launcher
counts it, 09-24 is the sixth window and still runs; 09-25 then depends on
the 09-18 row the same way. A denied window stays in the FIFO.

## 8. §9 size (step 6c)

71 KB → **44 KB**; target ~25 KB **not met**. The 09-19 journal block and
items 1–9's execution text moved verbatim to the archive
(`20260920T080344Z_PLAN-ROTATE.log`, zero-loss check passed). What remains
over target: nine open items at full rubric grain (≈ 24 KB — the floor
itself costs that), and ≈ 18 KB of standing rules, the Ansys ladder table
and the sequencing preamble. The rule that was not enough is "standing
rules stay": preamble items 1–2 are history, but `rotate_plan_archive.py
history` refuses a span that does not end on a blank line (list items are
contiguous) — a one-line reflow the weekly's rotation can make.
