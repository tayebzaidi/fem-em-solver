# docs/private — never committed

Everything in this directory except this README is gitignored. It holds the
weekly review's numeric adjudications of the `examples/ansys_benchmarks/`
cases (`<case>-adjudication-<date>.md`) — Ansys licence terms restrict
disclosure of benchmark results, so AED-derived numbers never enter a tracked
file (operator directive 2026-09-02; see `.gitignore`). The corresponding
public record is the qualitative verdict in PROJECT_PLAN.md §7. Absent on a
fresh clone; lives only on the operator's box.
